# AI Handoff Report — Xuân Tôi

> 👉 **AI/dev mới: ĐỌC [`docs/START_HERE.md`](./START_HERE.md) TRƯỚC.** File đó định tuyến tới đúng doc theo task type (state / vision / roadmap / economy / content / balance / live ops / story).
>
> **Cấu trúc**: file này chỉ chứa **trạng thái live hiện tại** (Executive Summary + Recent Changes + Phase Status + Known Issues + Tests/CI/Smoke + Next Roadmap). **Toàn bộ lịch sử PR cũ** (Snapshots PR #33→#396, Recent Changes Legacy, Completed Features, Project Reference đầy đủ Tech Stack / Architecture / DB / Gameplay / Run Locally / Rules, Old Recommended Next Roadmap, Exact PR Plan, smoke detail table per module) tách ra [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md). **AI mới CHỈ cần đọc file này** — `ARCHIVE_HANDOFF.md` chỉ tra cứu khi cần.
>
> **Cap**: ≤ 250 dòng (HANDOFF REPORT STRUCTURE RULE — xem [`AI_WORKFLOW_RULES.md`](./AI_WORKFLOW_RULES.md)). Vượt cap → bắt buộc compact + đẩy entry cũ xuống ARCHIVE.

---

## 1. Current Executive Summary

- **This PR (Phase 31.0 — Social & Retention Foundation V1 / Mail+Mentor+Returner+AdminMail+SystemGift, branch `phase-31-social-retention-foundation-v1`, PR #561)**: ships standalone Social & Retention foundation (4 new modules — Mentor, Returner, SystemGift, AdminMail) + extends Mail with idempotent claim-all + soft-delete + MailType taxonomy enum. **Shared (`packages/shared/src/{mail-types,admin-mail,mentor,returner,system-gift}.ts`)**: `MailType` (SYSTEM/ADMIN/REWARD/EVENT/MAINTENANCE/PURCHASE/SECT/FRIEND/RETURNER/PVP) + `MailStatus` (UNREAD/READ/CLAIMED/EXPIRED/DELETED) + 4 pure validators (`validateAdminMailSendInput`/`validateSystemGiftDef`/`resolveReturnerTier`/`buildReturnerCycleKey`). **Phase 31 hard cap**: `tienNgoc = 0` enforced server-side cho mọi Returner/SystemGift/AdminMail (KHÔNG mint Tiên Ngọc qua bất kỳ admin/auto pipeline). **Additive Prisma migration `20290101000000_phase_31_0_social_retention`**: 5 model mới (`AdminMailLog`, `SystemGift`, `SystemGiftClaim` UNIQUE `(giftKey,characterId)` — anti-duplicate, `MentorProfile`, `MentorRelation`, `CharacterReturnerState`) + Mail enum/column extend (`mailType`, `deletedAt`). **API**: `MentorService` (register/request/respond/listStudents — CAS `updateMany({status:'PENDING'})` race-safe accept, tier guard MIN=9 mentor / MAX=6 student / gap≥3, cap 5 students/mentor); `SystemGiftService` (upsertDef + 6 target rule resolver — ALL_PLAYERS/REALM_RANGE/CREATED_BEFORE/ACTIVE_IN_LAST_DAYS/SECT_MEMBERS/EVENT_PARTICIPANTS, hard cap 50k targets, fan-out via `MailService.sendToCharacter`, idempotent re-run skip on UNIQUE `(giftKey,characterId)`); `ReturnerService` (`buildReturnerCycleKey = userId:tier:YYYY-MM-DD` UTC idempotent per-day-per-tier, tier resolver inactiveDays≥7→SHORT/14→MEDIUM/30→LONG, reward filter forbidden + tier-clamp); `AdminMailService` (send-one/bulk/global w/ preview, AdminMailLog audit row mỗi call, recipientsSnapshot `slice(0,50)` cap audit). Mail extend: `getById`/`softDelete`/`claimAll` (CAS via `updateMany({claimedAt:null,deletedAt:null})`). **Controllers**: `/mentor/*` 6 endpoint, `/returner/*` 2 endpoint, `/admin/mail/*` 4 endpoint (`@RequireAdmin`), `/system-gift/*` 4 admin endpoint. **Web**: 3 view mới `/mentor` (register+request+respond), `/returner` (state+check), `/admin/mail` (4-tab send-one/bulk/global/logs); API client `mentor.ts`/`returner.ts`/`adminMail.ts`; `mail.ts` extend với `fetchMail`/`deleteMail`/`claimAllMail`. **i18n VI/EN parity**: `common.{accept,decline}` + `mentor.*` + `returner.*` + `adminMail.*` blocks. **Tests (+43)**: API 34 integration tests (`mentor.service.test.ts` 13, `system-gift.service.test.ts` 8, `returner.service.test.ts` 5, `admin-mail.service.test.ts` 8) + Web 9 component tests (`MentorView.test.ts` 3, `ReturnerView.test.ts` 3, `AdminMailView.test.ts` 3) + existing `MailView.test.ts` 14 updated với new fields. **KHÔNG đụng Phase 29 (combat/pvp/arena/sect-war/territory)** — 4 module standalone (chỉ depend AuthModule + MailModule + PrismaService). Verification: `pnpm typecheck` ✅, `pnpm lint` ✅, API tests 100 PASS (67 mail + 34 Phase 31), Web tests 48 PASS (i18n parity 10 + MailView 14 + Phase 31 9 + social-rate-limit 24). Roadmap: friend system / block list / notification center / claim-all chi tiết UI sẽ trong PR sau.

- **Previous PR (Phase 29.0–29.3 — PvP, Arena, Sect War & Spirit Vein Territory V1, branch `phase-29-pvp-arena-sect-war-territory`, PR #560)**: ships PvP **Foundation V1** — single unified `PvpBattle` shape cho DUEL / FRIENDLY_SPARRING / EVENT_PVP / SECT_WAR / TERRITORY_WAR (ARENA tiếp tục dùng `ArenaMatch` Phase 14.1.B/C). Shared `packages/shared/src/pvp.ts` (~620 dòng) định nghĩa `PvpMode/Status/Result/SnapshotType` + 8 anomaly type (`PVP_POWER_JUMP_BEFORE_MATCH`, `PVP_DAMAGE_OUTLIER`, `ARENA_RATING_GAIN_OUTLIER`, `ARENA_TARGET_FARMING`, `SECT_WAR_SCORE_OUTLIER`, `TERRITORY_PRODUCTION_DUPLICATE_CLAIM`, `SEASON_REWARD_DOUBLE_CLAIM`, `ROSTER_SWAP_EXPLOIT`) với `classifyPvpAnomaly()` auto derive severity + `blockRewardClaim` từ delta, `PvpBattleSnapshot` (immutable tại queue time — anti-cheat KHÔNG cho swap equipment giữa challenge → resolve) + `PvpDefenseProfileDef` (saved formation; fallback rebuild live khi chưa lưu), `PvpBalancePolicy` (default 12/d challenge, 60min same-target cooldown, power gap warn 1.5x / block 3.0x, KHÔNG bypass cho VIP, anti-P2W `forbiddenRewardItemKeys = FORBIDDEN_REWARD_ITEM_KEYS ∪ ADMIN_FORBIDDEN_GRANT_ITEMS`), `validatePvpSnapshot` / `validatePvpBattleResolve` / `validatePvpBalancePolicy` pure-fn validator, `computeFriendlyMatch(mode)` invariant FRIENDLY_SPARRING rewardGranted=false ratingChange=0, `computePvpPowerGap(a, b) = max/min`, `shouldBlockChallengeByPowerGap`, Arena rating delta formula re-export, matchmaking helper `shouldOfferAsArenaOpponent`, league/bracket helpers. 51 shared test (rating/matchmaking/cap/snapshot/reward-policy/league-bracket/territory-buff-cap/friendly invariant). `admin-control-center.ts` extend với 20 `PVP_*` AdminActionType + risk level + permission `ADMIN_MANAGE_PVP` (count 30, +1 từ Phase 28.0). Additive Prisma migration `20290101000000_phase_29_0_pvp_foundation`: 3 model — `PvpBattle` (UNIQUE `idempotencyKey`, immutable `attackerSnapshotJson` + `defenderSnapshotJson` Json, `roundsJson` summary chỉ display, `rewardJson` nullable cho FRIENDLY_SPARRING + INVALIDATED, `seed`, `ratingChangeJson`, status RESOLVED/INVALIDATED/PENDING), `PvpDefenseProfile` (1-1 với Character, label ≤ 60 char, snapshot DEFENDER), `PvpAnomalyLog` (severity / blockedReward / resolution DISMISSED/CONFIRMED/ESCALATED). API module mới `apps/api/src/modules/pvp/` (4 service + 2 controller): `PvpSnapshotService.buildForCharacter(characterId, type)` (build từ Character row, ném `PVP_SNAPSHOT_INVALID` qua `validatePvpSnapshot`); `PvpDefenseService.get/upsert/loadOrBuild` (upsert rebuild snapshot, `loadOrBuild` fallback live khi chưa lưu); `PvpBattleService.challenge` (validate not-self / không-ARENA / target tồn tại → build attacker ATTACKER + load defender DEFENDER → check `computePvpPowerGap` chống farm newbie + lệch lực, skip SECT_WAR/TERRITORY_WAR/EVENT_PVP → same-target cooldown từ `sameTargetCooldownMinutes` → idempotencyKey check unique → resolve deterministic Mulberry32 seed `±15%` damage variance → FRIENDLY_SPARRING force `rewardGranted=false` + `ratingChange=null` → `validatePvpBattleResolve` → persist 1 row RESOLVED), `listLogs/getById/invalidate`; `PvpAnomalyService.record/list/resolve` (auto severity từ `classifyPvpAnomaly`, audit ai resolve). `PvpPlayerController` 6 endpoint `/pvp/{policy, defense, challenge, battle-logs, battle-logs/:id}` (Zod schema, hydrate character từ AccessToken). `PvpAdminController` 6 endpoint `/admin/pvp/{policy, battle-logs, battle-logs/:id/invalidate, anomalies, anomalies/:id/resolve, defense/:characterId}` (`@RequireAdminPermission(ADMIN_MANAGE_PVP)`, `AdminAuditWriter.write` với `PVP_BATTLE_INVALIDATE` / `PVP_ANTICHEAT_RESOLVE`). Web: API client `pvp.ts` (player + admin call) + `PvpView.vue` (policy/defense/challenge/logs) + `AdminPvpCenterView.vue` (policy/battle search+invalidate modal/anomaly resolve modal) + i18n VI/EN parity `pvp.*` + `adminPvp.*` + router `/pvp` + `/admin/pvp`. **Anti-cheat invariant**: snapshot lưu tại queue, KHÔNG rebuild khi invalidate (chỉ flip status); FRIENDLY_SPARRING ledger-bypass = invariant test cứng; power gap ≥ 3x = block matchmaking; idempotencyKey unique chống double-submit. Tests: shared 3619 (+51), API 3735 (+20 ở `pvp.service.test.ts` — snapshot/defense/battle/anomaly), Web 2064 (+7 ở `PvpView.test.ts` + `AdminPvpCenterView.test.ts` smoke), `pnpm build` clean. Phase 29.1–29.3 roadmap (PR sau): Arena V2 (season/bracket/leaderboard/matchmaking), Sect War (season/registration/roster/battlefield node), Spirit Vein Territory (challenge/production/buff cap).

- **Phase 28.0 ( — Event Builder & Tier-Balanced LiveOps Event System V2, branch `phase-28-0-event-builder-tier-balanced-liveops`, PR #559)**: ships runtime-configurable event scheduler với bracket-based tier isolation chống "cá lớn ăn cá bé" và full reward formula server-authoritative `rewardTier = min(playerTier, bracketTier, eventMaxTier)`. Shared `packages/shared/src/events.ts` (~2420 dòng) định nghĩa 11 `EventType` × 9 `EventStatus` lifecycle (DRAFT → SCHEDULED → ACTIVE → PAUSED/REWARD_LOCKED → ENDED → FINALIZED → ARCHIVED/CANCELLED) + 9-tier `EventBracketDef` (`realmTierForEventBracket`) + `EventBalancePolicy` (chống endgame farm: forbidden item, qty cap per tier, daily/weekly/event purchase limits, ranking score normalization) + `EventMissionDef` × 3 reset cycle (DAILY/WEEKLY/EVENT_ONCE UTC YYYYMMDD/ISO week/static) + `EventShopDef`/`EventShopItemDef` + 7 `EventBossDef` types + 5 `EventRankingDef` types + `PersonalMilestoneEventDef` (auto-trigger trên realm threshold, expire after durationDays) + 35 `PRESET_EVENT_TEMPLATES`. 85 shared test (validator/bracket/mission/shop/personal/rewardTier). `admin-control-center.ts` extend với 18 `EVENT_*` AdminActionType + risk level. Additive Prisma migration `20280101000000_phase_28_0_event_builder` (14 model). API module `apps/api/src/modules/event-builder/` (8 service + 2 controller): `EventService`/`BracketService`/`EventItemService`/`EventMissionService`/`EventShopService`/`EventBossService`/`EventRankingService`/`EventPersonalMilestoneService`. `EventBuilderAdminController` 28 endpoint `/admin/events/*` (`@RequireAdminPermission(ADMIN_MANAGE_EVENTS)`, `AdminAuditWriter.write`). `EventBuilderPlayerController` 10 endpoint `/events/*` (list/detail/missions/shop/leaderboard/personal). Web: API client `eventBuilder.ts` + 2 view (`/admin/event-builder` 8-tab + `/events` All/Personal). i18n VI/EN parity (`events.*` + `adminEvents.*`). Anti-P2W: KHÔNG runtime code/SQL; reward đi qua ledger; endgame item forbidden cho event shop; high-tier player rớt vào bracket cao → rewardTier clamp xuống → KHÔNG farm low-bracket event. Tests: shared 3568 (+85), API 3715 (full Postgres+Redis), Web 2057. Roadmap: form editor admin đầy đủ + seed script preset + WS broadcast event status ở PR sau.

- **Previous PR (Phase 27.6 — Admin Control Center V2 / Config-Driven LiveOps Admin (PR1 foundation), branch `phase-27-6-admin-control-center-v2`, PR #558)**: ships first slice of Phase 27.6 spec (14 areas, 60+ endpoint admin). Shared `packages/shared/src/admin-control-center.ts` (~1400 dòng) định nghĩa **7 admin role** (`SUPER_ADMIN` / `OPERATIONS_ADMIN` / `ECONOMY_ADMIN` / `CONTENT_ADMIN` / `SUPPORT_ADMIN` / `MODERATOR` / `QA_ADMIN`), **29 permission key** (ADMIN_VIEW_DASHBOARD, ADMIN_ADJUST_CURRENCY, ADMIN_GRANT_ITEM, ADMIN_MANAGE_REWARD_PROFILE, ADMIN_MANAGE_DROP_PROFILE, ADMIN_MANAGE_GAME_CONFIG, ADMIN_MANAGE_FEATURE_FLAGS, ADMIN_BAN_USER, ADMIN_MUTE_USER, …), **role-perm matrix** (audit-friendly explicit per-role list), **30 admin action type** với `DEFAULT_ACTION_RISK` mapping (LOW/MEDIUM/HIGH/CRITICAL), `actionRequiresConfirmation` (HIGH+), per-role `ADMIN_CURRENCY_ADJUST_LIMIT` (SUPER_ADMIN ±1B LINH_THACH, SUPPORT_ADMIN 100k LINH_THACH; TIEN_NGOC nạp = 0 cho mọi role không phải SUPER_ADMIN), pure-fn validators: `validateAdminCurrencyAdjust` / `validateAdminItemGrant` / `validateRewardProfile` / `validateDropProfile` / `validateContentStatus` — server-authoritative chặn tier leak (source T1 boss reward T9), 6 forbidden endgame item (`hau_tho_tran_hon_an`, `ban_nguyen_chi_bao`, `hu_khong_chi_bao`, `tien_huyen_kiem`, `tien_huyen_giap`, `than_dan`), TIEN_NGOC grant ban (admin KHÔNG được mint Tiên Ngọc nạp), `REWARD_QTY_CAP_BY_TIER`, weekly cap required cho rare T7+, `DROP_RARE_RATE_MAX = 0.25`, `NORMAL_MONSTER` rares T5+ forbidden, `ARTIFACT_CRAFT` rareRate > 5% forbidden. Deterministic `simulateDropProfile(spec, trials, seed)` qua Mulberry32 PRNG (`createAdminSimulatorRng`) — admin chạy 10k trials reproducible. Additive Prisma migration `20270301000000_phase_27_6_admin_control_center_v2`: 3 model mới — `RewardProfile` (UNIQUE key, versioning, active flag per `(contentType, contentKey)`), `DropProfile` (versioning, active flag per `(sourceType, sourceTier, materialCategory)`), `ContentStatus` (UNIQUE `(contentType, contentKey)` — enabled/paused/disableReward/disableClaim). KHÔNG migration `AdminAuditLog` cũ — extend qua `meta` JSON (permissionKey/riskLevel/reason/targetType/targetId/beforeJson/afterJson). API module mới `apps/api/src/modules/admin-control-center/` (8 file): `AdminPermissionGuard` (granular permission qua decorator `@RequireAdminPermission(...keys)` — back-compat map User.role: ADMIN→SUPER_ADMIN, MOD→MODERATOR), `AdminAuditWriter` (wrap `AdminAuditLog`), `AdminOverviewService` (aggregator dashboard: totalUsers, activeUsersToday, mintedToday, spentToday, rareDropsToday, suspiciousEventsCount, pendingTopupsCount, activeFlags, activeEvents, maintenanceStatus, BP active season, monthlyCardActiveCount, etc.), `RewardProfileService` / `DropProfileService` (CRUD + validate + activate (transaction-safe single-active per scope) + deactivate + simulate qua shared), `ContentStatusService` (upsert + filter list), `AdminControlCenterController` (15 endpoint dưới `/admin/control-center/*`: overview, permissions/me, permissions/matrix, audit-action-types, reward-profiles CRUD/validate/activate/deactivate, drop-profiles CRUD/validate/simulate/activate/deactivate, content-statuses list/upsert). Web view mới `/admin/control-center` (`apps/web/src/views/AdminControlCenterView.vue`) tách khỏi `/admin` cũ (KHÔNG phình `AdminView.vue` 1838 dòng) — 6 tab read-only (Overview / Permissions / Reward Profiles / Drop Profiles / Content Status / Audit Actions) với loading/empty/error/filter, i18n VI/EN parity (`adminControlCenter.*`). Tests: shared 3505 (61 test mới — role matrix invariant, action risk mapping, validator boundary, simulator determinism, sanity sets duplicate-free), API typecheck + lint pass, web typecheck + lint pass. Roadmap PR2-6 trong `docs/ADMIN_GUIDE.md §13.4`: PR2 player support endpoints, PR3 editor UI, PR4 LiveOpsSchedule + GameConfig versioning, PR5 anti-cheat actions, PR6 docs + runbook.

- **Previous PR (Phase 27.1–27.5 — Monetization Systems V1 / Monthly Card · Sweep Tickets · Battle Pass · Growth Fund · Limited Shop, branch `phase-27-1-27-5-monetization-systems-v1`, PR #557)**: extends Phase 27.0 Foundation với 5 real monetization systems server-authoritative. Shared `packages/shared/src/monetization-systems.ts` (756 lines) ships **Battle Pass V2 missions catalog** (11 missions: DAILY ×4 / WEEKLY ×4 / SEASON ×3, exp range 20–500, all anti-P2W rewards), **Growth Fund V2 `tien` variant** (5 milestones Luyện Hư → Nhân Tiên gated by `realmOrder`, tier-capped rewards), **Limited Periodic Shop V1** (9 items × 3 shops DAILY/WEEKLY/MONTHLY với `purchaseLimitCount` per-period, sold-out tracked qua `LimitedShopPurchase`), **sweep ticket key list** (4 keys: common/dungeon/daily-farm/personal-boss), và **MONETIZATION_REWARD_VALIDATOR** + **FORBIDDEN_REWARD_ITEM_KEYS** chống endgame leak (pháp bảo top, công pháp chí tôn hoàn chỉnh, đan vân endgame). Additive Prisma migration `20270215000000_phase_27_1_27_5_monetization_systems_v1`: 2 model mới — `BattlePassMissionProgress` (UNIQUE `(characterId, seasonId, missionKey, scopeBucket)` — idempotent claim per period bucket), `LimitedShopPurchase` (UNIQUE `(characterId, shopKey, itemKey, periodKey)` — CAS for purchase limit). API: `BattlePassV2Service.listMissions/addProgress/unlockPaidTrack` (mission progress tracking, period-bucketed completion, free → paid track unlock), `LimitedShopService.listShops/purchase` (Serializable transactions, per-period count enforcement), `MonetizationOverviewService.overview` (aggregator: wallet + entitlements + battle pass summary + growth funds + limited shops + sweep tickets + extra attempts). Existing `MonthlyCardService` / `SweepTicketService` / `ExtraAttemptService` / `GrowthFundService` (Phase 27.0) wire vào new catalog. `MonetizationController` thêm endpoint: `GET /monetization/overview`, `GET /monetization/battle-pass/missions`, `POST /monetization/battle-pass/missions/:key/progress`, `POST /monetization/battle-pass/unlock-paid`, `GET /monetization/limited-shops`, `POST /monetization/limited-shops/:shopKey/:itemKey/buy`. Web: typed API client (`apps/web/src/api/monetization.ts` mở rộng 6 helper mới) + Pinia store `stores/monetizationSystems.ts` + 1 view tổng `MonetizationDacQuyenView.vue` (368 lines, 7 tabs: Thẻ tháng / Vé quét / Mua lượt / Tiên Lộ Lệnh / Quỹ trưởng thành / Shop / Quyền lợi, full i18n vi/en, loading/empty/error states, sold-out disable). Anti-P2W: tất cả reward đi qua `validateMonetizationSystemsCatalog()` (chặn endgame items + `MONETIZATION_ITEM_REWARD_MAX_QTY` cap); battle pass paid track KHÔNG bán công pháp top / pháp bảo top; growth fund gate realmOrder; limited shop per-period count enforced server-side qua `LimitedShopPurchase` unique CAS. Tests: shared 3444 (21 catalog invariant Phase 27.1–27.5 mới — mission uniqueness, exp cap, growth fund realmOrder monotonic, shop period mapping, forbidden item, sweep ticket keys, progress helpers), API +37 monetization integration test (foundation 15 + systems 16 + classic 6 — BattlePassV2 addProgress idempotent per bucket, LimitedShop purchase CAS, GrowthFund tien claim gate, MonetizationOverview snapshot), Web +5 (`MonetizationDacQuyenView` — 7-tab render / limited shop disable sold-out / growth fund tien render / battle pass missions / buy flow).

- **Previous PR (Phase 27.0 — Monetization Foundation / Wallet · Shop · Entitlements · Sweep · Extra Attempts · Growth Fund, branch `phase-27-0-monetization-foundation`, PR #556)**: ships server-authoritative monetization foundation theo spec Phase 27.0. Shared `packages/shared/src/monetization-foundation.ts` định nghĩa **6 wallet currencies** (`TIEN_NGOC` / `TIEN_NGOC_KHOA` / `LINH_THACH` / `CONG_HIEN_TONG_MON` / `TRIAL_POINT` / `EVENT_TOKEN`), **9 entitlement keys** với `ENTITLEMENT_VALUE_CAPS` (anti-P2W), **monthly card variants** (`tieu_nguyet_tap`, `dai_nguyet_tap`, `tu_chan_tap`, `chien_dau_tap`), **shop product catalog** với `purchaseLimitType ∈ {DAILY/WEEKLY/MONTHLY/LIFETIME/NONE}`, **3 sweep content types** (`DUNGEON`/`FARM_MAP`/`SECT_DUNGEON`), **extra attempt limits** (daily cap), và **growth fund** với milestone gating theo `realmOrder`. `periodKey(now, type)` đồng bộ bucket UTC giữa server + client. Catalog invariants test (`monetization-foundation.test.ts`) khoá `ENTITLEMENT_VALUE_CAPS` để chống regress P2W. Additive Prisma migration `20270201000000_phase_27_0_monetization_foundation`: extend `CurrencyKind` enum (`TIEN_NGOC_KHOA` / `CONG_HIEN_TONG_MON` / `TRIAL_POINT` / `EVENT_TOKEN`), thêm `Character.trialPoint` / `Character.eventToken`, đổi `MonthlyCardSubscription` thành composite unique `(characterId, cardKey)` (default `tieu_nguyet_tap` cho legacy rows), tạo **5 model mới**: `PremiumEntitlement` (UNIQUE `(characterId, entitlementKey)`, `valueJson`, `expiresAt`), `MonetizationShopPurchase` (audit purchase + `periodKey`), `PaidLimitPurchase` (UNIQUE `(characterId, limitKey, periodKey)` — daily cap CAS), `SweepTicketLog`, `GrowthFundState` (UNIQUE `(characterId, fundKey)` + `claimedMilestonesJson`). API: `WalletService.getWallet` trả snapshot 6 tiền + `applyTx` route đúng character field (đã extend `CurrencyService.INT_CURRENCY_FIELD`), `EntitlementService.getActiveEntitlements/hasEntitlement/getEntitlementValue/grantEntitlementTx`, `MonetizationShopService.listProducts/purchase` (Serializable isolation, debit wallet + grant reward + entitlement + monthly-card upsert atomic), `SweepTicketService.useTicket` (validate sweepable content type + content cleared check + debit `TIEN_NGOC_KHOA` cost), `ExtraAttemptService.getState/buyExtraAttempt` (CAS upsert `PaidLimitPurchase` chống double-spend), `GrowthFundService.getFund/claimMilestone` (gate realmOrder + Serializable + reward grant). `MonetizationController` thêm 11 endpoint: `GET /monetization/wallet`, `GET /monetization/wallet/ledger`, `GET /monetization/entitlements`, `GET /monetization/shop`, `POST /monetization/shop/purchase`, `POST /monetization/sweep/use`, `GET /monetization/extra-attempts`, `POST /monetization/extra-attempts/buy`, `GET /monetization/growth-fund`, `POST /monetization/growth-fund/claim`. `MonetizationFoundationError` map sang HTTP status (404 `PRODUCT_NOT_FOUND`/`FUND_NOT_PURCHASED`, 409 `PURCHASE_LIMIT_REACHED`/`EXTRA_ATTEMPT_LIMIT_REACHED`/`MILESTONE_ALREADY_CLAIMED`/`CARD_ALREADY_ACTIVE`/`TRANSACTION_CONFLICT`, 400 otherwise). Inventory ledger extends `ItemLedgerReason` cho `MONETIZATION_SHOP_BUY` / `MONETIZATION_MONTHLY_CARD_BUY` / `MONETIZATION_GROWTH_FUND_CLAIM`. Web: typed API client (`apps/web/src/api/monetization.ts`) thêm 9 helper Phase 27.0 + 2 view mới (`WalletView.vue` route `/wallet` — 6-currency grid, filter ledger theo currency, entitlements list; `MonetizationShopView.vue` route `/monetization-shop` — shop grouped by `productType`, extra-attempts daily cap UI, purchase + buy-attempt flows). Tests: shared 3425/3425 (catalog invariants giữ nguyên), API 3699/3699 với 15 test integration Phase 27.0 mới (`monetization-foundation.service.test.ts` — shop list/purchase, purchase-limit cap, INSUFFICIENT_CURRENCY, monthly card → entitlement grant, extra attempt daily cap, sweep CONTENT_NOT_CLEARED + DUNGEON cleared flow, growth fund FUND_NOT_PURCHASED / MILESTONE_LOCKED / MILESTONE_ALREADY_CLAIMED, wallet snapshot + ledger filter). Anti-P2W: ENTITLEMENT_VALUE_CAPS bound runtime values, premium chỉ extend convenience/time/lượt CÓ daily-cap, KHÔNG bypass cap; growth fund gate realmOrder; tất cả purchase / claim / debit chạy trong `$transaction` Serializable.

- **Previous PR (Phase 26.5 — World Content V2 / Farm Maps, Dungeons, Bosses, Sect Content & Trial Towers, branch `phase-26-5-world-content-v2`, PR #555)**: builds the long-form PvE content layer covering farm sessions, dungeons, bosses, sect content and trial towers. Shared `packages/shared/src/monster-taxonomy.ts` ships 12 `MonsterType` (NORMAL/ELITE/MINI_BOSS/REGION_BOSS/DUNGEON_BOSS/WORLD_BOSS/EVENT_BOSS/QUEST_BOSS/SIDE_QUEST_BOSS/SECT_BOSS/HIDDEN_BOSS/TOWER_GUARDIAN) and 12 `MonsterFamily` (YEU_THU/TA_TU/MA_TU/KHOI_LOI/LINH_THE/QUY_VAT/CO_THU/THU_VE_BI_CANH/DICH_TONG_MON/THIEN_KIEP/TAM_MA/DAO_ANH) each with declarative drop affinities. `farm-maps.ts` declares `FarmMapDef` with `sourceTier`/`recommendedRealmOrder`/`unlockRealmOrder`/`autoFarmAllowed`/`sweepAllowed`/`freeSessionMinutes`/`sessionLimitMinutes`/`maxSessionMinutes`/`monsterPool`/`opportunityPool`/`dropProfileKey`/`sourceHint`/`enabled` (anti-inflation: `effectiveDropTier = min(playerRealmTier, sourceTier)` enforced server-side). `world-dungeons-v2.ts` adds 14 `DungeonDef` across 14 `DungeonCategoryV2` (ALCHEMY_MATERIAL/BODY_MATERIAL/METHOD_FRAGMENT/ARTIFACT_MATERIAL/EQUIPMENT_MATERIAL/QI_BREAKTHROUGH/BODY_BREAKTHROUGH/TRIBULATION/STORY/SIDE_QUEST/SECT/EVENT/TOWER/GENERAL) with daily/weekly attempt caps. `world-bosses-v2.ts` ships 7 boss types (REGION/HOURLY/WORLD/EVENT/QUEST/HIDDEN/TRIAL) with `dailyRewardCap`/`weeklyRewardCap`/`schedule`/`manualOnly` gates. `sect-content.ts` adds `SectDungeonDef` + `SectBossDef` (6 sect dungeons + 4 sect bosses, all gated by `requiredSectLevel` + sect contribution). `opportunities.ts` adds 8 `OpportunityEncounterDef` with daily trigger caps (cơ duyên thường ≤ 5/ngày, hiếm ≤ 1/ngày). `trial-towers.ts` declares 3 towers (DANG_TIEN_THAP/LINH_KHI_THAP/HUYET_THE_THAP) with `infiniteScaling=true`, milestone multipliers (×1.00/×1.25/×1.75/×2.50/×4.00/×6.00 at floor%10/50/100/500/1000), and `floorFormulaKey` for power generation. `world-content-summary.ts` exposes `getWorldContentSummary()` returning per-region statistics. Additive Prisma adds 7 models (`FarmSession`/`FarmEncounter`/`DailyContentCap`/`WeeklyContentCap`/`TrialTowerProgress`/`TrialTowerAttemptLog`/`SectBossAttemptLog`) wired via `WorldContentModule`. API exposes 12 endpoints under `/world/*` (summary, farm-maps, farm/start, farm/sessions/:id/claim, dungeons, bosses, sect-dungeons, sect-bosses, opportunities, towers, towers/:key/attempt) — all server-authoritative with daily/weekly cap enforcement (Compare-And-Swap atomic upsert), first-clear-only floor rewards, milestone idempotency, and zero P2W (premium sells convenience, never caps). Web adds 6 views (`WorldContentView`/`FarmMapView`/`DungeonHubV2View`/`BossHubView`/`SectContentView`/`TrialTowerView`) with full loading/empty/error/list states, Pinia `worldContent` store, typed API client, vi/en i18n parity, and 6 routes under `/world/*`. Tests: shared 3409/3409 (catalog invariants, monster taxonomy ↔ family drop cross-check, sourceTier mapping, milestone multiplier monotonicity, summary aggregation), API 24/24 world-content (Postgres integration: farm session lifecycle, cap enforce, gating, trial-tower attempt success/fail, first-clear idempotency, milestone-once, world-cap CAS atomic), Web 26/26 (6 views × 4 states + filter/tab/action paths).

- **Previous PR (Phase 26.2 — Drop Economy V2 / Realm-Tier Weighted Material Drops, branch `phase-26-2-drop-economy-v2`, PR #552)**: builds the server-authoritative material drop economy on top of Phase 26.1 metadata. Shared `packages/shared/src/drop-economy.ts` ships `MonsterType` / `DropSource` / `DropRarity` / `MaterialDropRule` / `DropRollContext` / `DropRollResult`, realm→tier helpers (`realmOrderToMaterialTier` / `bodyRealmOrderToMaterialTier` / `effectiveDropTier` / `clampDropTierForSource` / `getTierDistance`), a 2-step roll (`rollHasMaterialDrop` base rate then `rollMaterialTier` weight by `lower2OrBelow/lower1/sameTier/above1/above2`), `MATERIAL_CATEGORY_MULTIPLIER` (ALCHEMY_QI 1.0 → ARTIFACT_CRAFT 0.05), auto-derived `DROP_RULE_CATALOG` from item metadata, and `getMaterialSourceHints` for UI. Anti-inflation invariant `effectiveDropTier = min(playerRealmTier, sourceTier)` blocks high-realm players from farming low-tier maps for endgame materials. Additive Prisma models `DailyMaterialCap` / `WeeklyMaterialCap` (`@@unique([characterId, dayBucket|weekBucket, ruleKey])`) + ISO-8601 `weekBucketFor` enforce daily/weekly caps inside one `$transaction`. New `DropEconomyService.rollAndGrant` wires into combat (`NORMAL_MONSTER`/`ELITE`/`BOSS`), dungeon-run (per-encounter; `pickDungeonDropSource` maps key prefix to `BODY_DUNGEON`/`ALCHEMY_DUNGEON`/`DUNGEON`), and world boss (`WORLD_BOSS` source + weekly cap) — all server-authoritative, fail-soft, no duplicate grant on retry, no `tienNgoc`/`linhThach` side-effect. Alchemy `listAvailableRecipes` now surfaces `itemName`/`materialTier`/`materialCategory`/`sourceHint` per missing input; Web `AlchemyView.vue` renders "Rơi từ: Boss, Tinh anh, Bí cảnh" block with vi/en i18n `alchemy.sourceHint.*` / `alchemy.materialCategory.*` parity. Tests: shared 50/50 (incl. 10k-roll Trúc Cơ balance simulations proving Tier 4+ = 0 from `NORMAL`), API DropEconomyService 8 (anti-inflation, cap upsert, no tienNgoc grant, ledger audit), Web AlchemyView 5 (sourceHint render, materialTier badge, missing-sourceHint no-crash, empty/single-entry edge cases).

- **Previous PR (Phase 26.1 — Complete Alchemy V2 / Pill Grade / Body Pills / Material Economy Foundation, branch `devin/1778615335-phase-26-1-alchemy-v2`)**: expands MVP alchemy into server-authoritative 9-tier Đan Sư progression with `Character.alchemyLevel/alchemyExp/alchemyMastery`, `AlchemyAttemptLog`, recipeTier/category metadata, pill grades (`Hạ/Trung/Thượng/Cực/Đan Vân`), lower-tier/furnace/mastery success bonuses capped at 98%, alchemy EXP on success/fail, and audit logging. Shared catalog now carries qi/body/breakthrough/tribulation/injury/buff pill metadata, material tiers/categories/source hints, V2 catalog validation, drop helper foundation, effect clamps, and no qi/body EXP mixing. API `/character/alchemy/recipes` returns mastery, final success rates, possible grades, missing inputs and craft gates; craft returns grade, alchemy EXP and level before/after. Web Alchemy UI shows Đan Sư level/EXP, category/tier/quality filters, missing materials, grade caps and success rates. Item use handles bodyExp through BODY_CULTIVATION cap and safe pill effects without linhThach/tienNgoc grants.


- **Previous PR (Phase 26.0 — Production Body Cultivation / Luyện Thể Core, PR #549, branch `devin/1778607982-phase-26-body-cultivation`)**: adds a real parallel Body Cultivation axis with 28 `BODY_REALMS` mapped to Qi realm order, dedicated `Character.bodyRealmKey/bodyStage/bodyExp/bodyCultivating/bodyInjuryUntil/physiqueKey/bodyBreakthroughCount`, additive `BodyBreakthroughAttemptLog`, and server-authoritative APIs `GET/POST /character/body-cultivation(/start|/stop|/breakthrough)`. Runtime uses a separate `body-tick` BullMQ job, `BODY_CULTIVATION` daily reward-cap source, stamina cost, injury EXP multiplier, minor-stage auto-advance, manual major breakthroughs with material/pill checks, transactional item consume, CAS guards, and no linhThach/tienNgoc grants. Body bonuses are capped and wired only into safe survival/combat surfaces (`hpMax`, `power`, `def`, `staminaMax`, boss damage reduction). Shared item/recipe/drop catalogs add minimal body pills/materials; web adds `/body-cultivation` with vi/en i18n, start/stop, breakthrough confirm, missing materials, injury and stat bonus display. Tests cover shared catalog/rate/caps, WS contract, and web render/action smoke; docs updated in `API.md` + `BALANCE_MODEL.md`.

- **Previous PR (Phase 25.3 — Code-only Cosmetic Effects, PR #548 draft, branch `feat/phase-25-3-code-only-cosmetics`)**: adds a thin cosmetic monetization layer with zero P2W vector. Shared `packages/shared/src/cosmetics.ts` ships 25 cosmetics across six types; additive Prisma models `CosmeticOwnership`/`CosmeticLoadout`; API `/cosmetics/*`; web wardrobe `/cosmetics`; CSS-only effects; no combat stat reads/writes.

- **Previous PRs (Phase 25.1/25.2 monetization + 23.2/23.3 equipment realm gate/set/resonance, PR #540/#541/#546/#547 — merged)**: limited resource shop packs (`shop-packs.ts` + `ShopPackPurchase`), Battle Pass / Monthly Card / VIP Light (`monetization.ts`), 28-realm equipment ladder + 2/4/6 set bonus + 5-kind resonance + Ngũ Hành element sets, `summarizeEquipmentBuild` capped at 30%, `GET /inventory/build`, vi/en `EquipmentBuildPanel`. P2W guardrails maintained: no top equipment, no max-state pháp bảo, no realm bypass. Detail in [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md).

- **Previous PR (Phase 22.1 — Ngũ Hành Build Depth Expansion, PR #539 — merged)**: deepens elemental gameplay without expanding Books II–V story. Adds named shared relationship helpers (`getGeneratingElement`, `getCounterElement`, `computeElementDamageModifier`, `classifyElementMatchup`), build recommendation helpers, skill synergy rules, boss/dungeon resistance profile helpers, equipment affinity hooks, Spiritual Root build guidance UI with vi/en i18n, and focused shared tests. Non-destructive; no new migration, no Battle Pass/VIP, no major story quest expansion.

- **Previous PR (Phase 21 — Story Quest Content Expansion Mega PR, PR #538 — merged)**: Book I retention content foundation: 8 gated chapters, 120 main quests, 160 side quests, 64 branch quests, 40 hidden quests, daily/weekly templates above minimum via existing+Phase21 catalog, 100+ achievements/titles, 600+ dialogue entries, Quest Journal tabs/metadata/i18n, and `phase21-content-integrity.test.ts`. Reward guardrails: main > side/branch/hidden baseline, daily/weekly capped, all runtime claiming remains through existing quest/mission/ledger services.

- **Current `main` commit**: post PR #527 merged (Phase 16.4 — Market Trade Abuse Hardening) + PR #526 merged (Phase 16.3 — Gameplay Anti-cheat Deep Detection) + PR #525 merged (Phase 18.3 — Security Audit / Alert Polish) + PR #524 merged (Phase 18.2 — Session Management Hardening) + PR #523 (Phase 17.2 — Backup/Restore Weekly Verification) + PR #522 (Phase 17.1 — Deploy Verify Gate) + PR #521 (Phase 18.1 — Security Rate Limit + Abuse Protection) + PR #520 (Phase 16.1.B Ledger Checker Daily + Economy Range Report) + PR #519 (Phase 15.8 LiveOps Cron Run Log + Sect Season Champion Snapshot) + PR #518 (Phase 15.7 Sect Season Reward Grant) + PR #517 (Phase 15.6 Config Version Rollback) + PR #516 (Phase 15.5 Maintenance Window) + PR #515 (Phase 15.4 Feature Flag) + PR #514 (Phase 15.3.B LiveOps Announcement) + PR #528 (Phase 19.1 Social Foundation) + PR #529 (Phase 19.1.B Social Rate Limit & Abuse Guard merged). **Previous PR (Phase 19.1 — Social System Foundation, branch `feat/phase-19-1-social-system-foundation`, PR #528)**: Foundation cho friend / private chat / group chat. **Migration `20260815000000_phase_19_1_social_system_foundation`** additive: `FriendRequest`(senderUserId, receiverUserId, status `PENDING`/`ACCEPTED`/`DECLINED`/`CANCELLED`, optional message ≤140ch, respondedAt) + `Friendship`(userAId<userBId invariant) + `PlayerBlock`(blockerUserId, blockedUserId) + `PrivateChatThread`(userAId<userBId) + `PrivateChatMessage` + `GroupChat`(ownerUserId, name 3-60ch) + `GroupChatMember`(unique groupId+userId) + `GroupChatMessage`. Tất cả KHÔNG FK (soft-ref pattern, fail-soft). **Shared `packages/shared/src/social.ts`**: enum `FriendRequestStatus` 4-state + `ChatThreadType`; DTO row interfaces; `SOCIAL_LIMITS` (FRIEND_REQUEST_MESSAGE_MAX=140, PRIVATE_MESSAGE_MAX=500, GROUP_MESSAGE_MAX=500, GROUP_NAME_MIN=3/MAX=60, GROUP_MEMBER_MAX=30); pure validators `validateFriendRequestMessage` / `validateChatMessageBody` / `validateGroupName` + `sortUserPair` (lexicographic low/high cho thread invariant). **Backend services** (3 module mới, không sửa WORLD/SECT chat hiện có): `SocialService` 11 method (send/accept/decline/cancel friendRequest, removeFriend, block/unblock, list friends/incoming/outgoing/blocks, isBlockedBetween, areFriends helpers); `ChatPrivateService` 4 method (getOrCreatePrivateThread, sendPrivateMessage, listPrivateMessages, listPrivateThreads); `ChatGroupService` 8 method (createGroupChat, addGroupMember, removeGroupMember, sendGroupMessage, listGroupMessages, listGroups, listGroupMembers). **Server-authoritative invariants** test-enforced: cấm self-friend / self-thread / self-block; cấm duplicate FriendRequest PENDING; block 2 chiều cancel mọi pending request + xoá Friendship + chặn FriendRequest mới + reject private message `BLOCKED`; non-member thread/group → 404 mask (KHÔNG 403 để không leak existence); GROUP_MEMBER_MAX cap; owner KHÔNG thể tự remove (dùng deleteGroup follow-up); message empty/whitespace/>500ch reject `INVALID_INPUT`. **Realtime fanout**: `RealtimeService.emitToUser` event `private-chat:msg` (2 user) + `group-chat:msg` (loop member). Best-effort fail-soft — realtime down KHÔNG rollback insert. **REST API** 16 endpoint: `/social/{friends,friend-requests/{incoming,outgoing},friend-requests,friend-requests/:id/accept,friend-requests/:id/decline,friend-requests/:id,friends/:friendUserId,blocks,block,block/:userId}` + `/chat/private/{threads,threads/:threadId/messages}` + `/chat/groups/{,:groupId/members,:groupId/members/:targetUserId,:groupId/messages}`. **FE** `SocialView.vue` (route `/social`, nav link 友 Xã Giao) với 3 tab Friends / PrivateChat / GroupChat → `SocialPanel.vue` (friends list + online dot + send/accept/decline/cancel + remove/block/unblock + ConfirmModal danger) + `PrivateChatPanel.vue` (threads sidebar + messages + send + open by userId + draft length counter cap 500) + `GroupChatPanel.vue` (groups list + create + members sidebar + add/remove + messages). i18n vi/en parity `social.*` / `chatPrivate.*` / `chatGroup.*` / `shell.nav.social`. FE API client `apps/web/src/api/{social,chatPrivate,chatGroup}.ts`. **Test**: shared 0 (catalog only), API service 61 integration test (social 31 + chat-private 16 + chat-group 14) cover friend lifecycle + block invariants + 404 mask + length cap + realtime fanout assertion; FE SocialPanel 5 unit test (loading/empty, accept, send, block-confirm flow, error toast). **Docs**: AI_HANDOFF_REPORT (this entry), API.md `SocialController`/`ChatPrivateController`/`ChatGroupController`, SECURITY.md §19 block + thread membership 404 mask, RUNBOOK.md §2 social moderation, CHANGELOG.md Phase 19.1. **Follow-up KHÔNG nằm trong PR này**: admin moderation (delete message / dissolve group / ban from chat), rate-limit decorator gắn vào friend request + chat send, voice/media upload, online presence cross-shard, gifting/trading từ friend list.

— Previous (merged) PR #527 (Phase 16.4 — Market Trade Abuse Hardening): **Detection-first, guard-light** anomaly layer cho market trade flow — KHÔNG block giao dịch, KHÔNG auto-cancel listing, KHÔNG auto-rollback trade, KHÔNG auto-refund, KHÔNG khoá tài khoản. Bổ sung Phase 16.6 Market Price Band (input gate) bằng cách observe pattern post-mutation. **Migration `20260801000000_phase_16_4_market_trade_anomaly`** additive: `MarketTradeAnomaly`(type 6-enum / severity / status / source 4-enum / listingId / seller-buyer-itemKey / unitPrice-Decimal / referencePrice-Decimal / deviationRatio-Float / windowKey / detailsJson + ack/resolve metadata + `@@unique([type, listingId, windowKey])` idempotency + 5 index, KHÔNG FK fail-soft). **Shared catalog** `packages/shared/src/market-trade-abuse.ts` 6 type + threshold (PRICE_EXTREME ratio ≥5/≥20 hoặc ≤0.2/≤0.05, REPEATED_PAIR 24h 3/10 + 7d 10/30, LISTING_SPAM 1h 30/80, MARKET_VOLUME 24h Σ ≥500k/≥5M LT, UNKNOWN_REFERENCE_PRICE INFO) + classify helpers + `estimateItemReferencePrice` (geomean band-quality) + `buildMarketAbuseWindowKey` (1h/24h/7d ISO UTC). **`MarketTradeAbuseService`** `scanAll({now?,windowKey?,windowMs?})` dispatch 6 rule fail-soft per-rule + idempotent P2002; hook real-time `recordListingCreate({listingId})` POST-tx `MarketService.post()` + `recordListingBuy({tradeId,listingId})` POST-tx `MarketService.buy()` wrap try/catch ở caller (throw KHÔNG rollback trade). Detection-first invariants test-enforced: scan + hook KHÔNG mutate Listing/MarketTrade/CurrencyLedger/ItemLedger/Character/User. **Admin API** 5 endpoint `/admin/market/abuse/{summary,scan,anomalies,anomalies/:id/ack,anomalies/:id/resolve}` gắn `@RequireAdmin()` + `@RateLimitPolicy('ADMIN_MUTATION')` + audit `ADMIN_MARKET_ABUSE_*`. **FE** `AdminMarketAbusePanel.vue` (summary cards + filter severity/status/type/source + sellerCharacterId/buyerCharacterId/itemKey + Run scan + Ack/Resolve table + i18n vi/en `admin.marketAbuse.*`) wire vào tab `marketAbuse` ở `AdminView.vue` cạnh `gameplayAntiCheat`. Test: shared 26 + service 16 + controller 18 unit tests.
- **Current phase**: Phase 10 **CLOSED** ✅. Phase 11 Progression Depth **COMPLETE** ✅. Phase 12 World Map & Dungeon **CLOSED** ✅. Phase 13.0/13.1.A–D LiveOps **CLOSED** ✅. Phase 13.2.A/B/C Sect Season **CLOSED** ✅. Phase R1 Production Readiness **CLOSED** ✅. Phase 12 Story Dialogue Foundation + 12.8.A–E + 12.9 **CLOSED** ✅. **Phase 12.10.A NPC Affinity Foundation** **CLOSED** ✅ (PR #476). **Phase 12.10.B NPC Gift + Quest Affinity Rewards** **CLOSED** ✅ (PR #484). **Phase 12.10.C NPC Shop + Hidden Unlocks** **CLOSED** ✅ (PR #492). **Phase 12.10.D NPC Relationship Quest Chains** **CLOSED** ✅ (PR #493). **Phase 14.0.A Sect Territory Influence Foundation** **CLOSED** ✅ (PR #479). **Phase 14.0.B Sect Territory Settlement + Region Ownership** **CLOSED** ✅ (PR #481). **Phase 14.0.C Sect Territory Region Buff + Influence Decay** **CLOSED** ✅ (PR #486). **Phase 14.0.D Territory Weekly War Loop** **CLOSED** ✅ (PR #487). **Phase 14.2.A Elemental Combat Foundation** **CLOSED** ✅ (PR #477). **Phase 14.2.B Elemental Combat Data + Balance** **CLOSED** ✅ (PR #482). **Phase 14.2.C Elemental Skill Tree Expansion** **CLOSED** ✅ (PR #490). **Phase 14.2.D Elemental Dungeon and Boss Identity** **CLOSED** ✅ (PR #491). **Phase 14.3.A Breakthrough Tribulation Foundation** **CLOSED** ✅ (PR #478). **Phase 14.3.B Tribulation Support Providers + Redirect UX** **CLOSED** ✅ (PR #483). **Phase 14.3.C Tribulation Support Item Consumption** **CLOSED** ✅ (PR #488). **Phase 14.3.D Tribulation Encounter System** **CLOSED** ✅ (PR #489). **Phase 16.5 Daily Reward Cap** **CLOSED** ✅ (PR #499). **Phase Audit-1 Post-5 Integration Regression Audit** **CLOSED** ✅ (PR #500). **Phase 16.6 Economy Anti-cheat Suite** **CLOSED** ✅ (PR #501). **Phase 17.5 Metrics + Load Test Baseline** **CLOSED** ✅ (PR #502). **Content Scale 2 — High-Realm Skills Pack** **CLOSED** ✅ (PR #509). **Phase 15.0.A — Equipment Reforge / Enchant Foundation** **CLOSED** ✅ (PR #511). **Phase 15.1–15.2 LiveOps Event Scheduler** **CLOSED** ✅. **Phase 15.3.A LiveOps Runtime Expansion + Festival Gift** **CLOSED** ✅ (PR #512). **Phase 15.3.B LiveOps Announcement + WS Broadcast / Marquee** **CLOSED** ✅ (PR #513). **Phase 15.4 — Feature Flag DB-backed** **CLOSED** ✅ (PR #514) — admin có thể bật/tắt 11 flag (Arena, Tribulation Mini-Battle, Reforge/Enchant, LiveOps Events, Festival Gift, Announcements, Territory War, Market, Shop/Sect Shop Discount) qua DB-backed catalog với cache 2-tier (L1 30s + L2 Redis 30s) + admin panel + audit. Server-authoritative `FEATURE_DISABLED` 503. **Phase 15.5 — Maintenance Window** **CLOSED** ✅ (PR #515). **Phase 15.6 — Config Version + Rollback** **CLOSED** ✅ (PR #516). **TZ Hotfix — sectWarWeekKey + Territory periodKey TZ-aware unify** **CLOSED** ✅ (PR #517). **Phase 15.7 — Sect Season + Territory Auto-Cron + Champion/MVP Reward Distribution** **CLOSED** ✅ (PR #518). **Phase 15.8 — LiveOps / Maintenance Polish Bundle** (this PR — #519) — polish hệ LiveOps / Maintenance / Cron cho closed-beta vận hành: (1) Maintenance realtime broadcast event `MAINTENANCE_STATUS` qua WS channel `maintenance:status` (payload public-safe, no admin leak; no-op transition không broadcast; broadcast fail không rollback DB); (2) Admin Maintenance `PATCH /admin/maintenance-windows/:id` với dangerous-update confirm + ConfigVersion + AdminAuditLog; (3) LiveOps reward form picker `RewardFormEditor.vue` (linhThach/tienNgoc/itemRewards với cap validate) + Raw JSON toggle; (4) Cron health/stale — bảng mới `LiveOpsCronRunLog` + helper `computeLiveOpsCronHealth` returns `OK`/`STALE`/`DEGRADED`/`DISABLED`, threshold territory >8d / sect-season >2d; admin status endpoints extend `health` field; FE badges color-coded; (5) Champion membership snapshot — bảng mới `SectSeasonChampionSnapshot` UNIQUE `(seasonKey, sectId, rank)`; reward service grant theo snapshot (audit-perfect: member rời sect SAU snapshot vẫn nhận champion) với fallback legacy current membership. KHÔNG đổi cron auto-run Phase 15.7. KHÔNG rewrite Maintenance / LiveOps / Sect Season. **Phase 15.7 — Sect Season + Territory Auto-Cron — LEGACY** — auto weekly cron settle sect season (Mon 00:05 ICT) + sect season snapshot (daily 00:15 ICT) qua BullMQ repeat job. 2 lớp idempotency: Redis lease (best-effort, TTL 5min) + DB UNIQUE (authoritative, P2002 swallow). Champion reward auto-grant cho mọi member sect rank-1 (cap 100 theo characterId ASC, 5000 LT + 2000 EXP + 2× `linh_lo_dan`) + MVP reward auto-grant top-1 cá nhân (15000 LT + 6000 EXP + 1× `co_thien_dan`). Bảng `SectSeasonRewardGrant` UNIQUE `(seasonKey, rewardType, characterId)`. Admin manual trigger `POST /admin/liveops/run-weekly-cycle` + `POST /admin/territory/cron/run-now` + `POST /admin/sect-season/cron/run-now` cùng code path với cron (fallback ops). Mới Phase 15.7 polish: `GET /admin/territory/cron/status` + `GET /admin/sect-season/cron/status` read-only snapshot. FE Admin LiveOps Panel hiện Champion/MVP mail count + i18n VI/EN parity. Tests: 11 sect-season-reward service + 30 liveops-cron service + 15 admin-liveops-cron controller (4 mới cho status endpoint) + 12 web AdminLiveOpsPanel. Membership snapshot rule: member rời sect SAU snapshot, TRƯỚC grant → KHÔNG nhận Champion (test verify) — defer audit-perfect cho Phase 15.8. KHÔNG issue tienNgoc, KHÔNG title runtime, KHÔNG auto-ban. **Phase 15.5 — Maintenance Window — LEGACY** — admin lập lịch hoặc bật khẩn cấp cửa sổ bảo trì để chặn traffic player trong khi vẫn cho admin / health / metrics / `/maintenance/status` đi qua. Severity (`INFO`/`WARNING`/`CRITICAL`) + target (`ALL_PLAYERS`/`NON_ADMIN_USERS`/`API_WRITE_ONLY`/`FULL_LOCKDOWN`) + 5-status state machine (`DRAFT`/`SCHEDULED`/`ACTIVE`/`ENDED`/`DISABLED`). `MaintenanceWindowGuardMiddleware` 9 bypass rule + cache L1 in-memory TTL 10s. Cron transition piggy-back trên `LiveOpsEventSchedulerCronProcessor` 5'-tick (reuse — không thêm queue/lease). FE poll `/maintenance/status` 30s + axios interceptor 503 `MAINTENANCE_ACTIVE` → overlay. Tests: shared 12 + api 53 (33 service / 12 admin / 2 public / 6 middleware) + web 17 (5 overlay / 2 banner / 6 admin panel / 4 store) + i18n parity. **Next task recommendation**: Phase 15.6 Config Version + Rollback (Medium PR) — atomic snapshot economy/balance/liveops config với version + admin promote/rollback endpoint + audit `ADMIN_CONFIG_*` + admin panel diff giữa version. Tận dụng `AdminAuditLog`. KHÔNG làm gacha banner / KHÔNG làm battle pass tier / KHÔNG WS broadcast realtime config change.
- **Phase 12.10.C (PR #492)**: NPC Shop and Hidden Unlocks. Biến NPC Affinity tier thành cổng mở khoá nội dung. **Shared (`packages/shared/src/npc-affinity-shop.ts` mới + `npc-hidden-unlocks.ts` mới)**: `NpcAffinityShopItemDef` (npcKey, itemKey, requiredAffinityTier, cost, currency LINH_THACH/TIEN_NGOC, stockType unlimited/daily/weekly, dailyLimit/weeklyLimit, unlockHint vi/en), `NPC_AFFINITY_SHOPS` 15 entry / 5 NPC. Helper `npcShopForAffinity(npcKey, tier)`, `npcAffinityShopItem`, `toNpcAffinityShopItemView`. Validator `validateNpcAffinityShopCatalog()` invariants (item/npc/tier ref tồn tại, cost positive int, stockType ↔ daily/weekly limit consistent, per-NPC daily-limit sum ≤30 anti-grind cap, tier cost cap). `NpcHiddenDialogueUnlockDef`/`NpcHiddenQuestUnlockDef` + helper `npcHiddenUnlocksForAffinity` trả combined list locked-first. **API (`npc-affinity-shop.service.ts` mới)**: `GET /story/npc-affinity/:npcKey/shop` (list locked/unlocked tier-aware + window count), `POST /story/npc-affinity/:npcKey/shop/buy` (atomic Prisma `$transaction`: re-check tier, count purchased trong window, verify limit, spend currency qua `CurrencyService.applyTx` negative delta, grant qua `InventoryService.grantTx`, ledger `reason='NPC_SHOP_BUY'` + `refType='NpcAffinityShop'` + `refId='${npcKey}:${itemKey}'`), `GET /story/npc-affinity/:npcKey/unlocks` (combined dialogue+quest hidden unlocks). Error codes 403 INSUFFICIENT_AFFINITY_TIER, 400 INSUFFICIENT_FUNDS, 429 DAILY/WEEKLY_LIMIT_REACHED. `CurrencyLedgerReason` + `ItemLedgerReason` thêm `'NPC_SHOP_BUY'`. **FE**: `NpcAffinityPanel.vue` thêm toggle "Cửa hàng" mỗi NPC card, lazy load shop+unlocks khi mở; render entry name + cost + currency + locked badge với required tier label + daily/weekly stock indicator + buy button disabled khi locked/limit/loading; hidden unlocks list dưới shop với dialogue/quest entry + tier label + reason + ✓ marker. i18n `npcAffinity.shop.*` + `shopErrors.*` + `unlocks.*` vi/en. **Tests**: shared `npc-affinity-shop.test.ts` + `npc-hidden-unlocks.test.ts` PASS; api `npc-affinity-shop.service.test.ts` 17 PASS (list tier filter, buy success path currency+inventory+ledger, INSUFFICIENT_AFFINITY_TIER, INSUFFICIENT_FUNDS, DAILY/WEEKLY_LIMIT_REACHED, item grant once, listUnlocks tier-aware, startOfLocalWeek Monday helper); web `NpcAffinityPanel.test.ts` 7 test mới PASS (toggle collapse, lazy load, locked dimmed + disabled buy, click buy, error state, unlocks list, daily/weekly stock). Verification: shared typecheck 0, api typecheck 0, web typecheck 0, all required test suites green. Forbidden actions: không làm full quest chain dài, không romance/marriage, không gacha, không phá economy.

- **Phase 14.2.D**: Elemental Dungeon and Boss Identity. Biến dungeon/boss thành nội dung có bản sắc Ngũ Hành rõ. **Shared (`packages/shared/src/elemental-identity.ts` mới)**: `DungeonDef` thêm `dominantElement?` / `recommendedCounterElement?` / `rewardElementHint?` (optional, derive từ `element` legacy nếu không set); `BossDef` thêm `weaknessElement?` / `resistElements?` / `rewardElementHint?` (UI hint thuần, **combat damage không đọc** — vẫn qua `elementalMultiplier` + `composeMonsterElementalResist`). Helpers `getDungeonElementProfile()` / `getBossElementProfile()` (override > legacy fallback > null), `computePlayerElementWarning(playerEl, targetEl)` → `recommended | warning | caution | none`, validators `validateDungeonElementProfile()` / `validateBossElementProfile()` cho catalog test invariant, query helpers `dungeonsByDominantElement` / `bossesWeakTo`. **API (`combat.service.ts`, `boss.service.ts`)**: `listDungeons()` trả mỗi entry kèm `elementProfile`; `BossView` interface thêm `elementProfile` field (boss legacy không catalog → all-null sentinel). No-double-multiplier invariant: response không expose multiplier numeric — FE compute warning qua relation logic. **FE**: `ElementIdentityPanel.vue` (dungeon card) + `BossElementTooltip.vue` (boss header) mới render dominant + counter + weakness + resists + reward hint + warning. i18n keys `elementIdentity.*` vi/en. SpiritualRoot store fire-and-forget hydrate trong onMounted (không block list load). **Tests**: shared `elemental-identity.test.ts` 32 PASS (relation matrix, profile derive + override, warning logic, catalog invariant với mọi element ≥ 1 dungeon/boss, weakness === counter(element), no-double-multiplier shape); api `combat.service.element-identity.test.ts` 5 PASS + `boss.service.element-identity.test.ts` 7 PASS; web `ElementIdentityPanel.test.ts` 9 PASS + `BossElementTooltip.test.ts` 8 PASS. Verification: shared typecheck 0 errors, shared dungeon/boss/elemental tests 89+62+72 PASS; api typecheck 0 errors, `--run combat boss` 261 PASS; web typecheck 0 errors, `--run Dungeon Boss` 155 PASS.

- **Phase 14.2.C**: Elemental Skill Tree Expansion. Biến Ngũ Hành từ damage/resist multiplier (Phase 14.2.A/B) thành **hệ kỹ năng có hướng chơi riêng**. **Shared (`packages/shared/src/elemental-skills.ts` mới)**: `SkillDef.tags?: SkillTag[]` (HEAL/DOT/BURST/SHIELD/CRIT/CONTROL backward-compat); `SKILL_ELEMENT_IDENTITY` 5-entry catalog (Mộc=hồi/độc, Hoả=burst/thiêu, Thổ=khiên/khống, Kim=xuyên/chí, Thủy=control/hồi linh) + helpers `getSkillElementIdentity` / `describeSkillElementIdentity` / `validateSkillTag` / `findElementIdentityCoverageGaps` / `computeSkillAffinityDelta` / `skillsForTag`; tag side-effect dial `SKILL_TAG_DOT_DAMAGE_RATIO=0.15` / `SKILL_TAG_DOT_TURNS=3` / `SKILL_TAG_SHIELD_HP_RATIO=0.10`; **11 signature skill mới** (2-3 mỗi hệ) trong `combat.ts` + skill-templates.ts entry. **API (`apps/api/src/modules/combat/combat.service.ts`)**: `EncounterMonsterDot` interface + `EncounterState.monsterDot?` optional field (multi-turn DOT persist). Skill cast block dispatch tag DOT (set monsterDot perTurn × 3 lượt) + SHIELD (compute `floor(hpMax × 0.10)` áp same-turn TRƯỚC buff shield, KHÔNG persist). DOT clear khi monster killed/encounter WON/LOST. **KHÔNG đổi**: combat damage formula, character affinity bonus (đã wire 14.2.A), monster resist + equipBonus (đã wire 14.2.B). **FE**: `SkillTagBadge.vue` mới (Wuxia palette), SkillBookView thêm filter "Loại pháp" + render tag badges + ElementBadge tooltip identity + identity description italic. i18n vi/en. **Tests**: shared `elemental-skills.test.ts` 48 PASS; api `combat.service.test.ts` +6 Phase 14.2.C tests (DOT cast/tick/clear, SHIELD log/non-persist, legacy no-op) — 102 PASS tổng; web `SkillTagBadge.test.ts` 17 PASS. Verification: shared typecheck 0 errors; api `--run combat.service` 102 PASS; web `--run Skill` 38 PASS + `--run SkillTagBadge` 17 PASS; pnpm build ✅.
- **In-flight**: none. Roadmap kế tiếp ưu tiên **13.2.D Season Settlement Automation** + **14.0.E Territory Weekly Settlement Cron + Reward Mail** + **14.3.E Tribulation Encounter Mini-Battle Mechanics** (xem §6).

- **Phase 14.3.D (PR #489)**: Tribulation Encounter System. Biến Thiên Kiếp từ attempt xác suất thành mini-encounter có gameplay — mỗi tribulation transition giờ có 1 encounter spec (element + effectType: Hỏa BURST / Thủy SUSTAIN / Mộc POISON_RECOVERY / Kim ARMOR_CRIT / Thổ DEFENSE_ENDURANCE) và 2-phase flow `start → resolve` thay 1-shot attempt cũ. **Shared (`packages/shared/src/tribulation-encounter.ts` mới)**: `TribulationEncounterDef` interface (element / effectType / phaseCount / successThreshold / failPenaltyMultiplier / rewardHintMultiplier), `TRIBULATION_ENCOUNTER_DEFS` 5-entry catalog, helpers `getTribulationEncounterDefByElement`/`dominantTribulationWaveElement`/`resolveTribulationEncounterDef`/`computeTribulationEncounterPhaseCount`/`computeTribulationEncounterPowerHint`/`describeTribulationEncounterAdvantage`, validators. Element advantage semantics: +2 đồng hệ / +1 player counter encounter (hoặc encounter sinh player) / 0 trung tính / -1 player sinh encounter / -2 encounter counter player. **API**: 3 routes mới `GET /character/tribulation/encounter/current` (snapshot spec + pending + cooldown + taoMa), `POST /character/tribulation/encounter/start` (validate at-peak + create pending row, idempotent re-call cùng tribulationKey trả pending hiện có), `POST /character/tribulation/encounter/resolve` (atomic resolve via `runAttemptInTx` extracted helper, double-resolve trả cached outcome từ `TribulationAttemptLog` — KHÔNG double breakthrough/consume/reward). **Prisma**: `TribulationEncounter` model (state machine `pending → resolved`) với `id/characterId/tribulationKey/encounterKey/effectType/element/difficulty/selectedSupportItemKeys[]/state/startedAt/resolvedAt/resolvedAttemptLogId` + indexes; migration `20260611000000_phase_14_3_d_tribulation_encounter`. **Error codes mới**: `NO_PENDING_ENCOUNTER` (404), `ENCOUNTER_ALREADY_PENDING` (409). **FE**: TribulationView Encounter panel — element + effectType + advantage + pending badges, phase count + difficulty + power hint + success chance, start/resolve button mutually exclusive theo `encounterPending` flag, CTA "Quay lại tu luyện" sau success. Store extend với encounter state + 3 actions `fetchEncounter/startEncounter/resolveEncounter`. API client thêm types + 3 functions. **i18n**: 28 keys vi/en parity + 2 error keys. **Tests +69**: shared `--run tribulation` 161 PASS (+36), api `--run tribulation` 105 PASS (+15), web `--run Tribulation` 180 PASS (+15) + api/store/view client tests; api `--run cultivation` 95 PASS + `--run combat` 30 PASS regression. **Out of scope**: realtime combat, animation/cutscene, item support mới, penalty nặng làm mất nhân vật, multi-encounter chain. **Risk/rollback**: encounter là layer view-only over `runAttemptInTx` extracted private helper — KHÔNG thay đổi simulation logic; legacy `POST /character/tribulation` endpoint vẫn hoạt động; rollback bằng disable 3 routes + drop `TribulationEncounter` table (no character data loss).

- **Phase 14.3.C (PR #488)**: Tribulation Support Item Consumption. Đóng vòng chơi Thiên Kiếp — player chọn item hỗ trợ → server consume in tx → server-side recalc bonus từ 4 nguồn (selected items + equipment + buffs + talents) → KHÔNG tin FE bonus value. **Shared (`packages/shared/src/tribulation-support-validate.ts` mới)**: `TRIBULATION_MAX_SELECTED_SUPPORT_ITEMS = 3`, `isTribulationSupportConsumable()` (PILL_HP / PILL_MP / PILL_EXP / MISC, KHÔNG equipment), `listTribulationSupportConsumables()`, `validateTribulationSupportSelection()`, `buildSelectedSupportItemEntries()`, `TribulationSupportSelectionError` enum. **API (`POST /character/tribulation`)**: body mới `{ selectedSupportItemKeys?: string[] }` (≤ 3 keys, optional). Server: (1) verify peak gate + cooldown, (2) verify selected items ∈ catalog + ownership + qty>0 in tx, (3) **consume items in tx atomic** — ledger `reason=TRIBULATION_SUPPORT_CONSUME` `refType=TribulationAttemptLog`, (4) **recalc bonus server-side** từ 4 nguồn (composedSupports cho FE display KHÔNG bị double-count với talent's elementResistFn; damageSupports cho damage multiplier exclude talents), (5) simulate kiếp deterministic + ghi log. **Idempotent**: pre-check ownership → consume fail → throw `SUPPORT_ITEM_MISSING` → tx rollback → KHÔNG mất EXP. **Dual consumption**: cả success + fail path đều consume (no free retry — design choice). **API preview** `GET /character/tribulation/preview` thêm fields: `availableSupportItems[]` (server-resolved label + bonus + qty từ catalog × inventory), `maxSelectedSupportItems` (= 3). **FE (`apps/web/src/views/TribulationView.vue`)**: selection panel với checkbox multi-select (cap 3), label + qty + bonus per item, predicted total bonus, "items consumed regardless of outcome" hint; outcome banner thêm consumed items list (success + fail path); sau attempt clear selection + refetch preview. **i18n**: 11 keys vi/en parity (selectionTitle/selectionHint/selectionEmpty/selectionLimitReached/selectionPredictedTotal/selectionItemQty/selectionItemBonus/consumedTitle/consumedItem/consumedNone) + 6 error keys (INVALID_SUPPORT_SELECTION/TOO_MANY_SUPPORT_ITEMS/DUPLICATE_SUPPORT_ITEM/INVALID_SUPPORT_ITEM/SUPPORT_ITEM_MISSING/INVENTORY_UNAVAILABLE). **Verification**: shared typecheck + `--run tribulation` 124 PASS (+24 validator) / api typecheck + `--run tribulation` 90 PASS (+12 Phase 14.3.C) + `--run cultivation` 95 PASS + `--run inventory` 91 PASS / web typecheck + `--run Tribulation` 157 PASS (+10 selection UI) + `--run Breakthrough` 49 PASS / pnpm build ✅. KHÔNG migration / KHÔNG schema (chỉ thêm code + ledger reason mới). Docs: `docs/CHANGELOG.md`, `docs/API.md` (preview shape + POST endpoint), `docs/BALANCE_MODEL.md` §5.6.3, `docs/ECONOMY_MODEL.md` §3.6, `docs/AI_HANDOFF_REPORT.md`.

- **Phase 12.10.B (PR #484)**: NPC Gift + Quest Affinity Rewards. **Shared (`packages/shared/src/npc-gift.ts` mới)**: `NpcGiftPreferenceDef` catalog 5 NPC × `acceptedItems[{itemKey, affinityMin, affinityMax, flavor*}]` + `dailyLimit` per-NPC (cap `NPC_GIFT_DAILY_LIMIT_CAP=5`); helpers `npcGiftPreferenceForKey`/`acceptedGiftItemFor`/`computeGiftAffinityDelta` (clamp per-gift `NPC_GIFT_AFFINITY_DELTA_CAP_PER_GIFT=8`); `validateNpcGiftCatalog` ép key uniqueness + item resolve + cap. **Quest reward**: `QuestRewardDef.affinity?: { npcKey, delta }` + `claim()` apply qua `addAffinityTx` cap `AFFINITY_DELTA_CAP_PER_QUEST_REWARD=40`. **Prisma migration `20260609000000_phase_12_10_b_character_npc_gift_log`**: table `CharacterNpcGiftLog` (`id, characterId, npcKey, itemKey, dayBucket, sequence, affinityDelta, createdAt`) + composite UNIQUE `(characterId, npcKey, dayBucket, sequence)` race-safe + 2 index. **API mới `POST /story/npc-affinity/:npcKey/gift`**: `NpcAffinityService.giftNpcTx` atomic — count daily logs (UTC `YYYY-MM-DD` bucket) + compare `dailyLimit` reject `DAILY_LIMIT_EXCEEDED`; consume 1 item qua `inventory.consumeTx` reject `ITEM_NOT_FOUND`/`WRONG_ITEM`; `addAffinityTx` apply delta; insert log row P2002 retry once với updated count. Quest claim idempotent qua existing CAS guard `updateMany({status, claimedAt})` — affinity reward KHÔNG double-apply. **FE**: `apps/web/src/api/npcAffinity.ts` thêm `giftNpc()` fetcher; `stores/npcAffinity.ts` thêm `giftAction` in-place update (no full reload) + `giftToast{npcKey,delta,newTier?}` auto-dismiss 3.5s + `giftError` code map; `NpcAffinityPanel.vue` thêm gift section per-NPC: select item (badge affinity range + flavor), gift button locked khi `dailyState.locked`, daily counter `used/limit` tabular-nums, success toast với delta + tier-up nếu có, error message dismiss-on-success. **Tests +52**: shared `--run npc-gift` 9 PASS (catalog seed + helper + cap invariant); api `--run npc-affinity` 25 PASS (gift success / no item / wrong item / daily limit / race UNIQUE retry / multi-NPC isolation); api `--run quest` 73 PASS (+4 affinity reward idempotent: claim once apply, claim twice KHÔNG double, multi-NPC reward, low-tier null-NPC skip); web `--run NpcAffinity` 18 PASS (+6 gift flow: button enabled/disabled, select renders accepted items, success toast, daily limit lock, error display, dismiss toast). Verification: shared typecheck + 1732 PASS / api typecheck + `--run npc-affinity` 25 PASS + `--run quest` 73 PASS / web typecheck + `--run NpcAffinity` 18 PASS / pnpm build ✅. Daily bucket UTC `YYYY-MM-DD` reset 00:00 UTC (cân nhắc local-day Phase 12.10.C nếu cần); KHÔNG ledger riêng (consume + insert log thay thế).

- **Phase 14.3.B (PR #483)**: Tribulation Support Providers + Redirect UX. **Shared (`packages/shared/src/tribulation-support-providers.ts` mới)**: 4 provider pure read-only — `collectItemTribulationSupports`/`collectBuffTribulationSupports`/`collectEquipmentTribulationSupports`/`collectTalentTribulationSupports`. **API**: `TribulationService.previewTribulation()` populate `successChance.supportBonus` (cap total `0.30`) + `supports[]` per source `{source, key, bonus, label?, element?}`. **KHÔNG consume item / KHÔNG decrement buff / KHÔNG mutate state** — preview read-only; consumption defer Phase 14.3.C. **FE — BreakthroughView**: catch `TRIBULATION_REQUIRED` 409 → toast info + `router.push('/tribulation')`. **FE — TribulationView preview**: render mỗi support entry badge source + element + bonus%; cap warning ceil/floor. **Verification**: shared 23 provider test PASS / api `--run tribulation` 78 PASS / web `--run Tribulation` 147 PASS + `--run Breakthrough` 49 PASS / pnpm build ✅. KHÔNG migration / KHÔNG schema.

- **Phase 14.2.B (PR #482)**: nâng Phase 14.2.A Elemental Combat Foundation từ pipeline trống thành gameplay thật. **Shared catalog**: `boss.ts` thêm `elementalResist` 6 boss (floor 0.7); `monsters.ts` thêm 8 monster mid/late; `items.ts` thêm `bonuses.elementalAtkBonus` 6 equipment (per-item ceil 0.10). **Invariant tests** `elemental-data.test.ts` 35 case ép catalog tuân floor/ceil + gear-stack ≤ 0.20 + countered + max bonus + min resist < 1.0× anti-power-creep. **API combat tests** +5 case: baseline / equipment bonus / monster resist / no double-apply (Math.random=0.5 deterministic 659) / null-element noop. **FE InventoryView**: tooltip `elementalAtkBonus` parity vi/en; +3 test. KHÔNG migration / KHÔNG đụng combat pipeline runtime. Verification: shared 1685+ PASS / api `--run combat` PASS / web `--run InventoryView` 57 PASS / pnpm build ✅.

- **Phase 14.0.D (PR #487)**: Territory Weekly War Loop. Biến Territory thành vòng chơi cạnh tranh theo tuần — period key UTC ISO week, settlement chốt owner mới mỗi tuần, FE countdown + region standings + history. **Shared (`packages/shared/src/territory-period.ts`, `territory-war.ts`)**: helpers `currentTerritoryPeriodKey`/`nextTerritoryResetAt`/`territoryPeriodWindow`/`previousTerritoryPeriodKey`/`isTerritoryPeriodKey` (UTC week boundary deterministic, fake-date testable); types `TerritoryWarStateView`/`TerritoryRegionWarSummaryView`/`TerritoryRegionWarStandingView`/`TerritoryRegionWarStatusView`/`TerritoryWarHistoryEntry`/`TerritoryWarHistoryView`/`TerritoryRegionOwnerSnapshotView`/`TerritoryWarSettleCurrentResult`. **API runtime (`apps/api/src/modules/territory/territory-war.service.ts`)**: `getCurrentTerritoryWarState()` (top 3 standings per region + countdown timeRemainingMs server-authoritative), `getRegionWarStatus(regionKey)` (top 10 + 5 recent settlements), `getWarHistory(limit?)` (entries DESC settledAt, group periodKey, cap 32), `settleCurrentPeriod({settledBy?})` (idempotent qua UNIQUE `(regionKey, periodKey)` race-safe; no-influence rule = sticky owner; tie-break `sectId.localeCompare()` ASC; trả `ownersAfter` cho FE refresh không round-trip). **API endpoints**: `GET /territory/war/current`, `GET /territory/war/regions/:regionKey`, `GET /territory/war/history?limit=`, `POST /admin/territory/war/settle-current` (RequireAdmin, audit `settledBy = req.userId`). **KHÔNG cron tự động** (defer 14.0.E — cần Redis lease / DB guard); admin trigger only. **FE (`apps/web/src/views/TerritoryView.vue`)**: tab `war` mới với countdown panel (1000ms ticker `nowMs`, `onBeforeUnmount(clearInterval)`, server `endsAt` source of truth, format `Xd HH:MM:SS`); period header `periodKey + window + previousPeriodKey badge`; 9 region cards (contested badge + owner + top 3 standings + leader tag + lead margin + empty state); history panel (entries DESC, expand snapshots); admin settle button role-gated `auth.user.role === 'ADMIN'` với loading + result render; lazy fetch qua `watch(tab)` chỉ khi vào tab `war` (cache aware via pinia store). **i18n**: `territory.tab.war` + `territory.war.*` 20 key parity vi/en (title/subtitle/countdownLabel/period/window/standings/history/admin*). **Pinia store (`stores/territory.ts`)**: actions `fetchWarCurrent`/`fetchWarHistory`/`adminSettleCurrentWar` + state `warState/warStateLoading/warHistory/warHistoryLoading/warSettleLoading/lastWarSettleResult/warSettleError`. **Tests +35**: shared `--run territory-war` 24 PASS (period key boundary + window + previous + isTerritoryPeriodKey + types invariant); api `--run territory` 83 PASS (+14 territory-war.service: empty/2-sect-tie/DESC-points/owner-snapshot/REGION_INVALID/recent-settlements/history-DESC/idempotent-snapshot-id/no-influence-skipped/settledBy-audit/limit-cap); api `--run admin` 359 PASS (+3 admin-territory war/settle-current: settledBy passthrough/PERIOD_INVALID 400/error rethrow); web `--run Territory` 30 PASS (+6 weekly-war: tab clickable/content render/9 cards + standings/history empty/role gate/admin click → API + history refresh). Verification: shared typecheck + `--run territory` 79 PASS / api typecheck + `--run territory` 83 + `--run admin` 359 / web typecheck + `--run Territory` 30 / pnpm build ✅. **KHÔNG siege/diplomacy/PvP realtime/auction**; **KHÔNG decay tự động trước/sau settle** (tách Phase 14.0.C admin trigger riêng); **KHÔNG reward/mail cho owner** (defer 14.0.E).

- **Phase 14.0.C (PR #485)**: Region Buff + Influence Decay. **Shared (`packages/shared/src/territory-buffs.ts` mới)**: `TerritoryRegionBuffDef` interface + `TERRITORY_REGION_BUFFS` catalog 5 buff (son_coc EXP_BONUS 5% / hac_lam LINH_THACH_BONUS 5% / moc_huyen_lam ELEMENTAL_DAMAGE 5% moc / kim_son_mach ELEMENTAL_DAMAGE 5% kim / hoang_tho_huyet DEFENSE_BONUS 5%); helpers `territoryRegionBuffsForRegion`/`territoryRegionBuffForOwner`/`activeTerritoryBuffsForSect`/`validateTerritoryBuffCatalog`/`computeTerritoryDecay`; envelope `TERRITORY_BUFF_VALUE_MAX=0.10` (cap value), `TERRITORY_DECAY_DEFAULT_BPS=2500` (25%), `TERRITORY_DECAY_MAX_BPS=5000` (50%); 32 unit test PASS (catalog UNIQUE + region exists + value ≤ cap + decay deterministic floor + helper coverage). **Prisma + Migration `20260610000000_phase_14_0_c_territory_decay_log`**: model `SectTerritoryDecayLog` (`id, periodKey UNIQUE, decayBps, rowsAffected, pointsBefore, pointsAfter, delta, triggeredAt, triggeredBy?`) — race-safe + idempotent qua UNIQUE `(periodKey)`. **API runtime**: `TerritoryDecayService.runDecay({periodKey, decayBps})` validate range `1..5000` reject `DECAY_BPS_INVALID`; insert log row P2002 retry → fetch tồn tại → return `{skipped:true}`; aggregate influence decay theo `computeTerritoryDecay` (floor, không âm); cập nhật `SectTerritoryInfluence.points`; 17 test PASS. `DungeonRunService.claimRun()` integrate `applyTerritoryDungeonRewardBuffs(rewards, regionKey, ownerSectId, characterSectId)` fail-soft owner-only, KHÔNG double-apply khi retry; 6 test mới (no-region/no-sect/non-owner/owner-EXP/owner-LinhThach/double-claim-no-double). **API endpoints mới/extend**: `POST /admin/territory/decay` (admin-only, `RequireAdmin`); `GET /territory/regions` extend `buffs/ownerBuffActive/currentPeriodKey/previousPeriodKey`; `GET /territory/me` extend `activeBuffs/currentPeriodKey`. **FE**: `apps/web/src/api/territory.ts` thêm `TerritoryRegionBuffPreviewLite/TerritoryDecayResult/adminTerritoryDecay`; `stores/territory.ts` thêm `decayLoading/decayError/lastDecayResult/adminDecay()` invalidate leaderboard cache + refetch regions/me; `TerritoryView.vue` thêm region buff list per-region (active/inactive badge dựa `ownerBuffActive`) + my active buffs panel + admin decay panel role-gated với input `decayBpsInput` + skipped/error/result render; i18n vi/en parity full `territory.overview.buffSection*/buff.*/myBuffs.*/admin.decay*/errors.DECAY_BPS_INVALID`. **Tests +9 web (15→24)** + 32 shared + 23 api (17 decay + 6 dungeon buff). Verification: shared typecheck + `--run territory` 55 PASS / api typecheck + `--run territory` 66 PASS + `--run dungeon` 88 PASS + `--run combat` 126 PASS / web typecheck + `--run Territory` 24 PASS / pnpm build ✅. **KHÔNG cron** (defer 14.0.D); **KHÔNG combat-elemental wire** (catalog ship sẵn nhưng giữ Phase 14.2.x envelope, defer); **KHÔNG xóa influence cũ** (decay aggregate qua tính lại points, log per-period idempotent).
- **Phase 14.0.B (PR #481)**: Settlement & Region Ownership. Migration `20260608000000_phase_14_0_b_territory_settlement` thêm `SectTerritoryRegionState` (1 row/region) + `SectTerritorySettlementSnapshot` UNIQUE `(regionKey, periodKey)` race-safe. `TerritorySettlementService.settleRegion/settleAllRegions/getRegionHistory/getOwnerStateMap` idempotent + tie-break `sectId.localeCompare`; admin endpoint `POST /admin/territory/settle` + per-region. FE: TerritoryView overview owner badge + history panel + admin panel role-gated. Tests +56 (shared 6 + api 49 + admin 12 + web 15). **Region buff khi rank 1 chưa wire** (defer 14.0.C); decay/season reset chưa có (defer 14.0.C/13.2.D).
- **Phase 12.10.A (PR #476)**: NPC Affinity Foundation. `CharacterNpcAffinity` per-(char, npcKey) row atomic; `addAffinityTx` cap clamp `[minScore, maxScore]`; tier unlocks; FE NpcAffinityPanel read-only view. Foundation cho 12.10.B gift + quest reward.
- **Test baseline (post Phase 14.0.D this PR)**: shared `--run territory` **79** PASS ✅ (territory 23 + territory-buffs 32 + territory-war 24) / api `--run territory` **83** PASS ✅ (+14 territory-war.service) / `--run admin` **359** PASS ✅ (+3 settleWarCurrent) / `--run dungeon` **88** PASS / `--run combat` **126** PASS / `--run npc-affinity` **25** PASS / `--run quest` **73** PASS / `--run tribulation` **78** / web `--run Territory` **30** PASS ✅ (+6 weekly war tab) / `--run NpcAffinity` **18** / `--run Tribulation` **147** / `--run Breakthrough` **49** / `--run InventoryView` **57** / pnpm build ✅.
- **Top priority next session**: (1) **Phase 14.0.E Territory Weekly Settlement Cron + Reward Mail** — cron auto-settle previousTerritoryPeriodKey với Redis lease / DB guard, owner reward mail (linh thach + contrib), Hall of Fame archive snapshot cũ; (2) **Phase 14.3.C Tribulation Attempt Item Consumption** — 14.3.B preview read-only; consumption khi attempt thật (decrement item + apply buff); (3) **Phase 12.10.C NPC Affinity Shop + Hidden Unlocks** — affinity tier unlock hidden dialogue branch + NPC affinity shop với contrib currency; (4) **Phase 13.2.D Season Settlement Automation** — cron auto-settle season + Hall of Fame archive. Detail §6.
- **Open Critical/High issues**: none. Live medium issues: M7 (CSP production verify khi deploy). Detail §4.
- **Blocker**: none.

---

## 2. Recent Changes

10 PR gần nhất merged trên main (newest đầu, mỗi entry 1 dòng). PR cũ hơn → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Snapshots.

| PR | Title | Type |
|---|---|---|
| (qa) | **test(qa): Full Project Regression QA — PR #562** (branch `test/full-project-regression-qa`, cut from `main` @ `b1683e83`). Tests Phase 27.1–27.5 (Monetization V1 PR #557), Phase 27.6 (Admin Control Center V2 PR #558), Phase 28.0 (Event Builder V2 PR #559), Phase 29.0–29.3 (PvP/Sect War/Territory V1 PR #560) merged on `main`. **Phase 31 PR #561 explicitly excluded** (still draft). 10-phase QA procedure executed: install/typecheck/lint/build, shared/api/web tests, Playwright full `E2E_FULL=1`, smoke `economy.mjs`, manual web QA at 360px + i18n VI↔EN + admin guard, security/abuse/economy audit. Automated assertions: **shared 3619 ✓ + api 3735 ✓ + web 2064 ✓ + Playwright 23 ✓ + smoke 20 ✓ ≈ 9.5k tests**. **2 fixes shipped**: `QA-001` chat sliding-window flake (widen window 50→200ms, sleep 80→300ms) + `QA-002` E2E `flushAuthRateLimits` key prefix drift (add `ratelimit:AUTH_*:*` for unified `@RateLimitPolicy` guard introduced in Phase 15.x). **1 medium UX finding** (`QA-004` AdminControlCenterView `onMounted` guard fires before auth store hydrates on direct URL navigation; SPA nav works; server-side admin payload still protected) deferred to follow-up micro-PR. **1 dev-tooling finding** (`QA-003` smoke scripts share host IP → exhaust AUTH_REGISTER 5/IP/15min when run sequentially; manual `redis-cli FLUSHDB` workaround). **Recommendation: YES for closed beta (conditional on CI green)**. Full report `docs/full-project-regression-qa-report.md`. | test/qa |
| (this) | feat(event-builder,api,web,docs,shared,prisma): **Phase 28.0 — Event Builder & Tier-Balanced LiveOps Event System V2** (branch `phase-28-0-event-builder-tier-balanced-liveops`, PR #559). Runtime-configurable event scheduler với bracket-based tier isolation chống "cá lớn ăn cá bé". Shared `packages/shared/src/events.ts` (~2420 dòng): 11 `EventType` + 9 `EventStatus` lifecycle (DRAFT→SCHEDULED→ACTIVE→PAUSED/REWARD_LOCKED→ENDED→FINALIZED→ARCHIVED/CANCELLED) + 9-tier `EventBracketDef` (`realmTierForEventBracket`) + `EventBalancePolicy` (chống endgame farm: forbidden item, qty cap per tier, daily/weekly/event purchase limits, ranking score normalization) + `EventMissionDef` (DAILY/WEEKLY/EVENT_ONCE reset cycles) + `EventShopDef`/`EventShopItemDef` (token currency + purchase limits) + `EventBossDef` (7 boss types) + `EventRankingDef` (5 ranking types, finalize per bracket) + `PersonalMilestoneEventDef` (auto-trigger trên realm threshold, expire after durationDays) + 35 `PRESET_EVENT_TEMPLATES`. Reward formula server-authoritative: `rewardTier = min(playerTier, bracketTier, eventMaxTier)`. 85 shared test (validator/bracket/mission/shop/personal/rewardTier). `admin-control-center.ts`: thêm 18 `EVENT_*` AdminActionType (CREATE/UPDATE/DELETE/ACTIVATE/PAUSE/LOCK_REWARDS/FINALIZE/ARCHIVE/CANCEL/BRACKET_UPSERT/BALANCE_UPSERT/ITEM_UPSERT/MISSION_UPSERT/SHOP_UPSERT/BOSS_UPSERT/RANKING_UPSERT/RANKING_LOCK/PERSONAL_UPSERT) + risk level. Additive Prisma migration `20280101000000_phase_28_0_event_builder` (14 model: EventDef/EventBracket/EventBalancePolicy/EventItem/EventMission/EventMissionProgress/EventShop/EventShopItem/EventShopPurchase/EventBoss/EventBossAttempt/EventRanking/EventRankingEntry/PersonalEventProgress). API module mới `apps/api/src/modules/event-builder/` (8 service + 2 controller): `EventService` (CRUD + status lifecycle transition map), `BracketService` (9-tier CRUD + balance policy + `computePlayerContext` resolve bracket + rewardTier + tokenMultiplier + rankingEligible per player), `EventMissionService` (DAILY/WEEKLY/EVENT_ONCE reset cycles UTC YYYYMMDD/ISO week/static, progress increment, idempotent claim), `EventShopService` (shop+item CRUD + purchase với daily/weekly/event limits + bracket/realm guard + token ledger), `EventRankingService` (score submit + bracket-aware leaderboard + idempotent finalize), `EventItemService` (item config + loot table validation), `EventBossService` (boss spec foundation), `EventPersonalMilestoneService` (idempotent trigger + claim với expiry). `EventBuilderAdminController` 28 endpoint dưới `/admin/events/*` (`@RequireAdminPermission(ADMIN_MANAGE_EVENTS)`, `AdminAuditWriter.write` cho mỗi mutation, prompt reason cho HIGH risk). `EventBuilderPlayerController` 10 endpoint dưới `/events/*` (list/detail/missions/shop/leaderboard/personal). Web: API client `apps/web/src/api/eventBuilder.ts` + 2 view (`/admin/event-builder` 8-tab read-only + `/events` player với tab All/Personal + bracket-aware reward tier display + mission claim + leaderboard). i18n VI/EN parity (`events.*` + `adminEvents.*`). Anti-P2W: KHÔNG event tạo runtime code/SQL; admin KHÔNG nhập SQL/script; reward đi qua ledger (chia sẻ với existing rewardProfile); endgame item forbidden cho event shop; high-tier player rớt vào bracket cao hơn → rewardTier clamp xuống → KHÔNG farm low-bracket event. Tests: shared 3568 (85 mới), API 3715 (full pass với Postgres+Redis), Web 2057. Migration deploy clean. | feat |
| (prev) | feat(monetization,api,web,docs,shared,prisma): **Phase 27.1–27.5 — Monetization Systems V1** (branch `phase-27-1-27-5-monetization-systems-v1`, PR #557). Extend Phase 27.0 Foundation: shared `monetization-systems.ts` (Battle Pass V2 missions 11 × DAILY/WEEKLY/SEASON, Growth Fund V2 `tien` 5 milestones, Limited Shop 9 items × 3 shops DAILY/WEEKLY/MONTHLY, sweep ticket keys, anti-P2W forbidden list + reward validator + qty cap). Additive Prisma: 2 model (`BattlePassMissionProgress` UNIQUE `(characterId, seasonId, missionKey, scopeBucket)` idempotent claim per period bucket, `LimitedShopPurchase` UNIQUE `(characterId, shopKey, itemKey, periodKey)` CAS). API: `BattlePassV2Service`/`LimitedShopService`/`MonetizationOverviewService` (Serializable transactions, period-bucketed). `MonetizationController` thêm 6 endpoint (`/monetization/overview`, `/monetization/battle-pass/missions[/:key/progress]`, `/monetization/battle-pass/unlock-paid`, `/monetization/limited-shops[/:shopKey/:itemKey/buy]`). Web: Pinia store `monetizationSystems` + 1 view tổng `MonetizationDacQuyenView` (7 tab + i18n vi/en + loading/empty/error). Anti-P2W: `validateMonetizationSystemsCatalog()` chặn endgame leak; battle pass paid track KHÔNG bán công pháp/pháp bảo top; limited shop per-period count enforced server. Tests: shared 3444 (21 catalog invariant mới), API +37 (foundation 15 + systems 16 + classic 6), Web +5. | feat |
| (prev) | feat(monetization,api,web,docs,shared,prisma): **Phase 27.0 — Monetization Foundation** (branch `phase-27-0-monetization-foundation`, PR #556). Server-authoritative wallet/shop/entitlement/sweep/extra-attempt/growth-fund layer theo spec Phase 27.0. Shared `monetization-foundation.ts` (6 wallet currencies + 9 entitlements với `ENTITLEMENT_VALUE_CAPS` anti-P2W + monthly card variants + shop products với purchase limits + sweepable content types + extra attempt limits + growth fund). Additive Prisma: extend `CurrencyKind` enum (`TIEN_NGOC_KHOA`/`CONG_HIEN_TONG_MON`/`TRIAL_POINT`/`EVENT_TOKEN`), `Character.{trialPoint,eventToken}`, composite unique `(characterId,cardKey)` cho `MonthlyCardSubscription`, 5 model mới (`PremiumEntitlement`/`MonetizationShopPurchase`/`PaidLimitPurchase`/`SweepTicketLog`/`GrowthFundState`). API: `WalletService`/`EntitlementService`/`MonetizationShopService`/`SweepTicketService`/`ExtraAttemptService`/`GrowthFundService` (Serializable `$transaction` cho mọi debit + reward grant), `MonetizationController` thêm 11 endpoint. Web: typed API client + `WalletView`/`MonetizationShopView`. Anti-P2W: premium chỉ extend convenience/time/lượt có daily-cap, gate realmOrder cho growth fund. Tests: shared 3425, API 3699 (15 mới). | feat |
| (older) | feat(world-content,api,web,docs,shared,prisma): **Phase 26.5 — World Content V2 / Farm Maps, Dungeons, Bosses, Sect Content & Trial Towers** (PR #555, branch `phase-26-5-world-content-v2`). Long-form PvE content layer. Shared catalogs: `monster-taxonomy.ts` (12 MonsterType × 12 MonsterFamily với drop affinities), `farm-maps.ts` (FarmMapDef gated by sourceTier/unlockRealmOrder + auto-farm SAFE-only rule), `world-dungeons-v2.ts` (DungeonDef × 14 categories), `world-bosses-v2.ts` (BossDef × 7 categories — REGION/HOURLY/WORLD/EVENT/QUEST/HIDDEN/TRIAL), `sect-content.ts` (SectDungeonDef + SectBossDef gated by `requiredSectLevel`), `opportunities.ts` (cơ duyên thường ≤ 5/ngày, hiếm ≤ 1/ngày), `trial-towers.ts` (3 towers DANG_TIEN_THAP/LINH_KHI_THAP/HUYET_THE_THAP với `infiniteScaling` + milestone multipliers ×1.00/1.25/1.75/2.50/4.00/6.00 ở floor%10/50/100/500/1000), `world-content-summary.ts` (`getWorldContentSummary()` aggregator). Anti-inflation: `effectiveDropTier = min(playerRealmTier, sourceTier)`. Additive Prisma: 7 model (`FarmSession`/`FarmEncounter`/`DailyContentCap` UNIQUE `(characterId, capKey, source, date)`/`WeeklyContentCap` UNIQUE `(characterId, capKey, source, weekKey)`/`TrialTowerProgress`/`TrialTowerAttemptLog`/`SectBossAttemptLog`). API `/world/*` 12 endpoints (summary, farm-maps, farm/:mapKey/start, farm/sessions/:id/claim, dungeons, bosses, sect-dungeons, sect-bosses, opportunities, towers, towers/:key/attempt). `WorldCapService` enforce daily/weekly cap qua CAS atomic upsert; `FarmService.startSession/claimSession` apply tier-clamped reward; `TrialTowerService.attemptFloor` first-clear-only + milestone-once. Anti-P2W: premium chỉ extend session minutes, KHÔNG bỏ cap. Web 6 view `/world/{,/farm-maps,/dungeons,/bosses,/sect,/towers}` (loading/empty/error/list states + Pinia store + typed API client + vi/en i18n parity). Tests: shared 3409, API world-content 24 (farm 10 / trial-tower 8 / world-cap 6), Web 26 (6 views × 4 states + filter/tab/action paths). | feat |
| (prev) | feat(character,cultivation,web,docs,shared,prisma): **Phase 26.3 — Cultivation Method V2 / Công Pháp Progression & Fragment System** (PR #553, branch `phase-26-3-cultivation-method-v2`). Multi-slot server-authoritative method progression. Shared `cultivation-methods-v2.ts` (36 methods · 9 grades PHAM→CHI_TON · 7 categories · 5 equip slots). Helpers: `methodUpgradeLinhThachCost`/`methodUpgradeExpCost`/`computeMethodStatBonus`/`aggregateEquippedMethods`/`canEquipMethod`/`canUpgradeMethod`/`canStarUpMethod`. Auto-derived `method_fragment_<methodKey>` items wired into Drop Economy V2 (`METHOD_FRAGMENT` category, matching tier). Additive Prisma: `CharacterCultivationMethod{level,star,methodExp,equippedSlot,updatedAt}` + new `MethodUpgradeLog`. New `CultivationMethodV2Service` (703 lines) with `grantStarterV2IfMissing`/`getV2State`/`unlock`/`equipV2`/`unequipV2`/`upgrade`/`starUp`/`getEquippedSnapshot(Tx)` — all `$transaction`, idempotent, slot conflict + realm + sect gating. Controller exposes 5 endpoints `/character/cultivation-methods-v2[/unlock|/equip|/unequip|/upgrade|/star-up]` with Zod input + `CultivationMethodV2Error→HTTP` mapping. Web `/cultivation-method-v2` (View + Pinia store + API client) full UI: slot summary, aggregated bonus chips, category/slot/unlocked filters, per-method cards with unlock/equip/upgrade/star-up actions, loading/error/empty/list states, vi/en i18n parity. Stat wire safe surfaces (qiExp/bodyExp/hpMax/mpMax/atk/def/spirit/stamina/bossDmgReduction/elementalAtk/tribulationSupport). Tests: shared 47, API service 19. | feat |
| (prev) | fix(body-cultivation): **Phase 26.0.1 — Runtime Verification & Fixes**. Verifies Phase 26.0 with real Postgres/Redis migration + API smoke, moves body tick to its own BullMQ queue, adds DB-backed BodyCultivationService/Processor regression tests, CAS-guards concurrent breakthrough mutation, records BODY_CULTIVATION cap isolation, and hardens `/body-cultivation` UI test ids/confirm coverage. | fix |
| (prev) | feat(shop,api,web,docs): **Phase 25.2 — Limited Resource Shop Packs** (PR #547, branch `feat/phase-25-2-shop-packs-limited-resources`). Adds shared `shop-packs.ts` catalog/helpers, additive `ShopPackPurchase` schema, server-authoritative purchase/admin grant APIs, ledger reasons `SHOP_PACK_PURCHASE`/`SHOP_PACK_REWARD`, `/shop-packs` web UI, and tests/docs for limits/idempotency/fairness. | feat |
| #545 | feat(artifact): **Phase 23.7 — Pháp Bảo Star-up + Awaken Persistence** merged. Persisted phapBao star/awaken/refine + `POST /character/phap-bao/:id/{star-up,awaken,refine}` + FE confirm modal. | feat |
| #541 | feat(equipment,api,web,docs): **Phase 23.3 — Set Bonus + Gear Resonance** merged. 10-set Ngũ Hành catalog + 5-kind gear resonance + `summarizeEquipmentBuild` capped at 30% + `GET /inventory/build` + `EquipmentBuildPanel.vue`. | feat |
| #540 | feat(equipment,api,shared,web,docs): **Phase 23.2 — Realm-scaled Equipment Progression System** merged. | feat |
| (prev) | chore(release,docs,scripts,web): **Phase 24.1 — Closed Beta QA / Handoff Sync / Regression Smoke** (PR #537, branch `chore/phase-24-1-closed-beta-qa-handoff-sync`). Stabilization phase trước closed-beta launch. Audit `AI_HANDOFF_REPORT.md` + `CHANGELOG.md` sync Phase 20.3 merged + Phase 24.1 entry. Refresh `QA_CHECKLIST.md` Closed-Beta Regression Matrix (auth/character/cultivation/inventory/mission/dungeon/boss/market/economy/topup/admin/social/chat/moderation/party/party-dungeon/co-op boss/co-op reward cap+season/admin dashboards/backup-restore/deploy-verify/security/rate-limit/mobile). Add `scripts/smoke-social.mjs` + `scripts/smoke-coop.mjs` + `pnpm smoke:social`/`smoke:coop` graceful-skip infra unavailable. Mobile responsive sanity audit + critical fix (SocialView/PrivateChatPanel/GroupChatPanel/PartyPanel/CoopBossPanel/CoopWeeklyLeaderboardPanel/Inventory/Market/AppShell) — không redesign, fix overflow/tap-target/modal/table-scroll. Update `RUNBOOK.md` Closed Beta Operator Playbook + `DEPLOY.md` Production Readiness Notes (env/secret/rate-limit/monitoring/known blocker). **KHÔNG**: gameplay/system/migration lớn/rewrite/skip/fake test. Test: typecheck/lint/test/build PASS. Rollback = revert per-commit (docs/scripts/CSS only). | chore |
| #536 | feat(coop,api,shared,web,docs): **Phase 20.3 — Co-op Reward Cap / Anti-leech / Weekly Contribution Season** (PR #536, branch `feat/phase-20-3-coop-reward-cap-season`). Anti-abuse + weekly season cho CoopBoss + PartyDungeon. Migration `20261115000000_phase_20_3_coop_reward_cap_season` additive: 4 model (`CoopRewardCapCounter` UNIQUE `(userId,source,dayKey)`, `CoopWeeklyContributionSeason` UNIQUE `weekKey`, `CoopWeeklyContributionEntry` UNIQUE `(seasonId,userId)`, `CoopWeeklyRewardClaim` UNIQUE `(seasonId,userId)`) + 2 anomaly enum (`COOP_REWARD_CAP_HIT`/`COOP_LEECH_HIGH`). Shared `coop-reward-cap.ts` (43 test): enum source/risk/season-status/claim-status/reward-tier + DTO + caps (boss 8/d 30/w, dungeon 12/d 50/w) + helpers `buildCoopRewardDayKey/WeekKey` ISO UTC+7 + `canClaimCoopRewardWithinCap` + `classifyCoopLeechRisk` + `applyLeechRiskDowngrade` + `computeWeeklyContributionPoints` (boss 1× dungeon 0.5× MVP +25%) + `classifyWeeklyRewardTier`. Backend `CoopRewardCapService`: `checkDailyWeeklyCap` + `incrementRewardCapCounterTx` (CAS UPSERT trong tx grant) + `classifyLeechRisk` + `recordWeeklyContribution` + `getWeeklyLeaderboard` + `settleWeeklySeason` admin + `claimWeeklyReward` (CAS PENDING→CLAIMED + ledger grant linhThach+exp) + `getMyCoopRewardStatus`. REST `GET /coop/rewards/status` + `GET /coop/rewards/weekly-leaderboard` + `POST /coop/rewards/weekly-claim` + Admin `/admin/coop/rewards/*` (AuditLog mọi mutation). Integrate: `CoopBossService.finishRun` leech downgrade tier inline tx + best-effort weekly record/audit ngoài tx; `CoopBossService.claimReward` + `PartyDungeonService.claimReward` kiểm cap trước tx (reject `DAILY_CAP_REACHED`/`WEEKLY_CAP_REACHED` → 409) + increment counter trong tx + weekly record ngoài tx. Module DI optional (graceful test). FE: `coopRewardCap.ts` client + `CoopWeeklyLeaderboardPanel.vue` + tab `coopWeekly` `SocialView.vue` + i18n vi/en parity `coopRewardCap.*` + cap error keys. Test: shared 43 / api focus 64+18 / web parity 10 / lint+typecheck+build PASS. **KHÔNG** realtime broadcast cap/leech (Phase 20.4), **KHÔNG** shop weekly reward beyond linhThach+exp, **KHÔNG** per-dungeon contribution (party-dungeon truyền 0). Rollback = revert PR + migration down. | feat |
| #535 | feat(boss,api,shared,web,docs): **Phase 20.2 — Co-op Boss Party Contribution Foundation** (PR #535, branch `feat/phase-20-2-coop-boss-contribution`). Foundation co-op boss event gắn party (Phase 19.4). `CoopBossRun` + `CoopBossParticipant` + `CoopBossContribution` + `CoopBossRewardClaim` (UNIQUE soft-ref). `CoopBossService` 11 method (create/join/leave/recordContribution clamp anti-cheat/finishRun tier NONE/LOW/NORMAL/HIGH/MVP/claimReward CAS) + 4 rate-limit policy + 4 WS broadcast + FE `CoopBossPanel.vue` + parent tab SocialView. Migration `20261101000000_phase_20_2_coop_boss_contribution`. Foundation cho Phase 20.3. | feat |
| #516 | feat(api,web,shared): **Phase 15.6 — Config Version + Rollback**. Hệ versioning + rollback an toàn cho 4 entity vận hành (`LIVEOPS_EVENT` / `LIVEOPS_ANNOUNCEMENT` / `FEATURE_FLAG` / `MAINTENANCE_WINDOW`). Mỗi mutation admin ghi `ConfigVersion` snapshot before/after; rollback có 3 mức safety `SAFE` (1 confirm) / `NEED_CONFIRM` (phrase do server trả) / `BLOCKED` (server reject). **Shared (`packages/shared/src/config-version.ts`)**: `ConfigVersionEntityType`/`ConfigVersionAction`/`ConfigRollbackSafetyLevel`/`ConfigRollbackStatus` enum + `ConfigVersionSnapshot`/`ConfigSnapshotDiffEntry`/`ConfigRollbackSafetyContext`/`ConfigRollbackSafetyResult`/`ConfigRollbackResponse` types + `sanitizeSnapshot` (strip key chứa secret/password/token/cookie — defense-in-depth) + `computeRollbackSafety` (pure-fn, 4 entity branch: LIVEOPS_EVENT BLOCKED khi đổi reward/window sau khi đã có claim, FEATURE_FLAG NEED_CONFIRM cho critical flag, LIVEOPS_ANNOUNCEMENT SAFE default, MAINTENANCE_WINDOW NEED_CONFIRM khi đổi target/severity) + `diffSnapshots` (JSON-level diff per-key). **Prisma migration `20260624000000_phase_15_6_config_version_rollback`**: model `ConfigVersion` (id/entityType/entityId/version/action/beforeJson/afterJson/changedByAdminId nullable/reason nullable/createdAt + UNIQUE composite `(entityType, entityId, version)` + 3 index `(entityType, entityId, createdAt desc)`/`(action, createdAt desc)`/`(changedByAdminId, createdAt desc)`) + `ConfigRollbackRun` (id/entityType/entityId/fromVersion/targetVersion/status/safetyLevel/warningsJson/reason nullable/adminUserId/targetVersionId FK ConfigVersion/newVersionId FK ConfigVersion nullable/createdAt + 2 index). Additive — không sửa bảng cũ. **API service (`apps/api/src/modules/config-version/`)**: `ConfigVersionService.recordVersion / listVersions / getVersion / getLatestVersion / diffVersions / recordRollbackRun`. Version auto-increment per `(entityType, entityId)` qua `max(version)+1` + retry 1 lần khi P2002 (race). Sanitize trước khi persist via shared `sanitizeSnapshot`. Skip no-op (`beforeJson` deep-equal `afterJson`) trừ khi `skipNoOp=false`. **API admin module (`apps/api/src/modules/config-version-admin/`)**: `ConfigRollbackService.dryRun / apply` — load target version → compute safety qua shared `computeRollbackSafety` → orchestrate mutate entity → record version mới + ghi `ConfigRollbackRun` audit. `ConfigRollbackOrchestratorService` dispatch theo `entityType` (LiveOps Event qua `LiveOpsEventService.updateMany`, Announcement qua `LiveOpsAnnouncementService.updateMany`, Feature Flag qua `FeatureFlagService.setFlag`, Maintenance Window qua `MaintenanceWindowService.updateMany`). `AdminConfigVersionController` 5 endpoint dưới `AdminGuard + @RequireAdmin`: `GET /admin/config-versions?entityType&entityId&limit` + `GET /admin/config-versions/:id` + `GET /admin/config-versions/diff?fromVersionId&toVersionId` + `POST /admin/config-versions/:id/dry-run-rollback` (body `{reason?}`) + `POST /admin/config-versions/:id/rollback` (body `{reason?, confirmPhrase?}`). HTTP status: SAFE rollback 200, NEED_CONFIRM thiếu phrase 409, BLOCKED 409, TARGET_IS_LATEST/INVALID 400, NOT_FOUND 404. Audit chain: `ADMIN_CONFIG_VERSION_VIEW` (list/get/diff) + `ADMIN_CONFIG_ROLLBACK_DRY_RUN` + `ADMIN_CONFIG_ROLLBACK` (success) + `ADMIN_CONFIG_ROLLBACK_BLOCKED` (BLOCKED hoặc confirm mismatch). **Wiring vào 4 module**: `FeatureFlagService.setFlag`, `LiveOpsEventService.create/update/recomputeStatus`, `LiveOpsAnnouncementService.create/update/disable`, `MaintenanceWindowService.create/update/disable/recomputeStatuses` đều gọi `ConfigVersionService.recordVersion` fire-and-forget try/catch + log warn — không phá flow gốc nếu version record fail. **FE Admin Panel** (`AdminConfigVersionPanel.vue`): tab mới `configVersion` trong AdminView. Entity type picker (4 enum) + entity ID input → click refresh → list versions newest-first table (v/action/changedAt/reason) → button "Diff với latest" + "Dry-run rollback" trên mỗi row khác latest → dry-run section hiển thị safety badge (SAFE green / NEED_CONFIRM amber / BLOCKED rose) + warnings + changed fields + apply button (ẩn khi BLOCKED) → ConfirmModal cho apply (reason input + confirm phrase input khi NEED_CONFIRM, FE check phrase match trước khi gọi API để fail-fast UX). **FE API client** (`api/configVersion.ts`): 5 endpoint wrapper. **i18n** `adminConfigVersion.*` VI/EN parity. **Tests**: shared 30 (validator + sanitize + diff + computeRollbackSafety per entity type) + api 11 (config-version.service: recordVersion 6/list-get 2/diff 1/rollback run 2) + web 5 (panel render list + empty + dry-run SAFE/BLOCKED branch + NEED_CONFIRM confirm phrase flow). Lint/typecheck/i18n parity 10 test pass. | feature |
| #515 | feat(api,web,shared): **Phase 15.5 — Maintenance Window**. Hệ Maintenance Window cho phép admin lập lịch hoặc bật khẩn cấp cửa sổ bảo trì để chặn traffic player trong khi vẫn cho admin / health / metrics / `/maintenance/status` đi qua. Severity `INFO`/`WARNING`/`CRITICAL` + target `ALL_PLAYERS`/`NON_ADMIN_USERS`/`API_WRITE_ONLY`/`FULL_LOCKDOWN` + 5-status state machine (`DRAFT`/`SCHEDULED`/`ACTIVE`/`ENDED`/`DISABLED`). **Shared (`packages/shared/src/maintenance-window.ts`)**: types + `validateMaintenanceWindowInput` (key pattern, severity/target enum, title 120/message 1000 + safe-text + locale parity vi/en, window `startsAt < endsAt`, min 60s, max 30 ngày) + `nextMaintenanceWindowStatus` state machine + `MAINTENANCE_ACTIVE_ERROR_CODE`. **Prisma migration `20260623000000_phase_15_5_maintenance_window`**: model `MaintenanceWindow` (id/key UNIQUE/severity/target/status/titleVi/titleEn nullable/messageVi/messageEn nullable/startsAt/endsAt/allowAdminBypass/allowHealthcheck/allowMetrics/createdByAdminId nullable/createdAt/updatedAt/disabledAt nullable + 3 index). Reuse `AdminAuditLog` cho `ADMIN_MAINTENANCE_*`. **API service (`apps/api/src/modules/maintenance-window/`)**: `MaintenanceWindowService.listWindows/getWindow/getActiveWindow/createWindow/updateWindow/disableWindow/recomputeStatuses/isMaintenanceActiveForRequest`. Cache L1 in-memory TTL 10s per pod; idempotent recompute qua `updateMany` CAS guard status + window. **API middleware (`maintenance-window.middleware.ts`)**: `MaintenanceWindowGuardMiddleware` resolve role từ cookie `xt_access` (banned/missing/AuthService throw → ANONYMOUS, fail-open nếu service throw), 9 bypass rule (`/maintenance/status` luôn pass; `/health*`/`/metrics*` theo flag; `/_auth/*` luôn pass trừ FULL_LOCKDOWN; ADMIN/MOD bypass theo `allowAdminBypass`+`target`; `API_WRITE_ONLY` chỉ block non-GET; `NON_ADMIN_USERS` block player+anonymous; `ALL_PLAYERS` block player nhưng admin pass; fail-closed default), render 503 envelope `{ ok: false, error: { code: 'MAINTENANCE_ACTIVE', meta: { severity, target, titleVi/En, messageVi/En, endsAt, serverTime } } }` + `Retry-After` header. **API admin endpoints**: `GET /admin/maintenance-windows` + `POST /admin/maintenance-windows` (audit `ADMIN_MAINTENANCE_CREATE`) + `PATCH /admin/maintenance-windows/:id` (audit `ADMIN_MAINTENANCE_UPDATE`, block khi đã ACTIVE/ENDED/DISABLED) + `POST /admin/maintenance-windows/:id/disable` (audit `ADMIN_MAINTENANCE_DISABLE`, idempotent) + `POST /admin/maintenance-windows/recompute-status` (audit `ADMIN_MAINTENANCE_RECOMPUTE`, idempotent). ADMIN-only. **API public endpoint**: `GET /maintenance/status` anonymous-safe `MaintenanceWindowPublicView` (không leak `id`/`createdByAdminId`/`disabledAt`/internal flags). Endpoint luôn pass kể cả khi maintenance ACTIVE. **Cron transition**: `LiveOpsEventSchedulerCronProcessor` piggy-back recompute maintenance status mỗi tick 5 phút sau khi recompute LiveOps event + announcement; idempotent. **FE Player UI** (`MaintenanceOverlay.vue` + `MaintenanceBanner.vue`): overlay full-screen render khi store `isBlocked` → severity badge + title/message theo locale + countdown tới `endsAt` + nút Thử lại; banner admin-only trong `AppShell` cho biết admin đang bypass maintenance window đang ACTIVE. **FE Admin Panel** (`AdminMaintenancePanel.vue`): tab mới trong `AdminView`, list windows + create form + recompute + disable confirm modal cho action major. **FE Store** (`stores/maintenance.ts`): Pinia poll `/maintenance/status` 30s + axios interceptor 503 `MAINTENANCE_ACTIVE` → `markBlockedByApi(error.payload)`. **i18n**: namespace mới `maintenance.*` (overlay/banner/admin panel — title/severity/target/status/form/actions/confirm/toast/errors). VI/EN parity. **Tests**: shared 12 validator/state machine + api 53 (33 service / 12 admin / 2 public / 6 middleware) + web 17 + i18n parity 10. Không regress 447 admin / 79 auth / 13 health / 138 web admin. **Docs**: `docs/API.md` (endpoint + bypass rule + error codes), `docs/CHANGELOG.md` (Phase 15.5 section), `docs/RUNBOOK.md` §2.23 (incident playbook), `docs/DEPLOY.md` (migration note). | feat |
| #514 | feat(api,web,shared): **Phase 15.4 — Feature Flag DB-backed**. Hệ Feature Flag DB-backed cho phép admin bật/tắt nhanh các hệ thống lõi (Arena, Tribulation Mini-Battle, Equipment Reforge/Enchant, LiveOps Events, Festival Gift, LiveOps Announcements, Territory War, Market, Shop/Sect Shop discount runtime) **mà không cần deploy code**. Catalog 11 flag hardcoded trong `packages/shared/src/feature-flags.ts` chia 5 category (`GAMEPLAY`/`ECONOMY`/`LIVEOPS`/`ADMIN`/`SAFETY`). **Shared**: 11 flag + helper `getFeatureFlagDef`/`getDefaultFeatureFlagEnabled`/`isPublicFeatureFlag` + types `FeatureFlagAdminView`/`FeatureFlagPublicView` + `FEATURE_DISABLED_ERROR_CODE`. **Prisma migration `20260622000000_phase_15_4_feature_flag`**: model `FeatureFlag` (id/key UNIQUE/enabled/category/descriptionVi/descriptionEn nullable/updatedByAdminId nullable/createdAt/updatedAt + index). Reuse `AdminAuditLog` cho `ADMIN_FEATURE_FLAG_*`. **API service (`apps/api/src/modules/feature-flag/`)**: `FeatureFlagService.isEnabled/getFlag/listFlags/setFlag/ensureDefaultFlags/clearCache` cache 2-tier (L1 in-memory Map TTL 30s + L2 Redis TTL 30s) Redis fail-soft, `assertFeatureEnabled(key)` throw `FeatureFlagDisabledError` → controller layer map 503 `FEATURE_DISABLED` payload `{flag,message}`. **Module extraction**: tách `FeatureFlagAdminModule` riêng (mirror Phase 14.1.D `ArenaAntiWintradeAdminModule`) để break circular `AdminModule → CharacterModule → FeatureFlagModule → AdminModule`. **API admin endpoints (`feature-flag-admin/admin-feature-flag.controller.ts`)**: `GET /admin/feature-flags` + `PATCH /admin/feature-flags/:key` (audit `ADMIN_FEATURE_FLAG_UPDATE`, reject `FEATURE_FLAG_KEY_INVALID`) + `POST /admin/feature-flags/refresh-defaults` (idempotent seed) + `POST /admin/feature-flags/clear-cache`. ADMIN-only. **API public endpoint**: `GET /feature-flags/public` whitelist subset (Arena/Tribulation/Reforge/Enchant/LiveOps Events/LiveOps Announcements/Market) anonymous-safe, KHÔNG trả flag SAFETY/ADMIN. **Runtime gates**: 11 flag wire vào module tương ứng, tất cả trả `FEATURE_DISABLED` 503 khi off. **FE**: `AdminFeatureFlagsPanel.vue` (tab mới `AdminView`, list/filter/search/toggle, confirm modal cho flag major Arena/Market/LiveOps Events/Territory War/Festival Gift), `stores/featureFlags.ts` Pinia public store fetch 30s TTL fail-open (undefined/chưa load → enabled=true), `FeatureDisabledBanner.vue` reusable banner wire vào `ArenaView`/`EquipmentUpgradePanel`/`LiveOpsActiveEventsPanel`. **i18n**: `adminFeatureFlags.*` + `featureFlags.disabled.*` + `arena.disabled.message` + `inventory.upgrade.{reforge,enchant}.disabledMessage` + `liveopsActiveEvents.disabled.festivalGiftMessage` + `admin.tab.featureFlags`. VI/EN parity. **Tests**: shared (catalog invariants), api (service cache fallback Redis-down + ensureDefaultFlags idempotent + setFlag clear cache + audit + admin controller list/update/refresh/clear-cache + admin guard reject PLAYER + invalid key reject + runtime gates arena/tribulation/reforge/enchant/festival-gift/market/liveops-events flag off → 503 + public whitelist), web (`AdminFeatureFlagsPanel` 6 case render+filter+toggle+confirm-modal+refresh-defaults+clear-cache, `FeatureDisabledBanner` 4 case default+messageKey+fallback+testId, `useFeatureFlagsStore` 6 case ensureLoaded+TTL+refresh+fail-open+reset). Server-authoritative qua `FEATURE_DISABLED` 503 — FE chỉ hint UX. Risk: cache Redis fail-soft (downgrade tới L1 → DB → default catalog). Rollback: revert PR — `FeatureFlagService` không còn import → runtime không gate, hệ thống hoạt động bình thường (default-on). **KHÔNG** thêm flag key ngoài catalog (service reject), **KHÔNG** dùng flag thay test/fix bug, **KHÔNG** dựa FE flag cho security gate (server là source of truth). Branch `devin/phase-15-4-feature-flag-db-backed`. | Feature |
| #513 | feat(api,web,shared): **Phase 15.3.B — LiveOps Announcement + WS Broadcast / Marquee**. Bổ sung hệ thống thông báo thời gian thực: admin tạo announcement có severity (`INFO`/`EVENT`/`WARNING`/`MAINTENANCE`) + window, server tự động chuyển status `DRAFT→SCHEDULED→ACTIVE→ENDED` theo cron 5 phút (piggy-back trên LiveOps event recompute) và broadcast WS `liveops:announcement` (`ANNOUNCEMENT_ACTIVE`/`ANNOUNCEMENT_ENDED`) + `liveops:event` (`LIVEOPS_EVENT_ACTIVE`/`LIVEOPS_EVENT_ENDED`). Player thấy banner/marquee trên `HomeView` với severity badge + countdown + dismiss local (sessionStorage), `LiveOpsActiveEventsPanel` watch `lastEventBroadcastAt` để tự refresh khi event transition. **Shared (`packages/shared/src/liveops-announcement.ts` + `ws-events.ts`)**: `LIVEOPS_ANNOUNCEMENT_SEVERITIES` + `LIVEOPS_ANNOUNCEMENT_STATUSES` + `LIVEOPS_ANNOUNCEMENT_TARGETS` + `LIVEOPS_BROADCAST_EVENT_TYPES` + `validateLiveOpsAnnouncementInput` (cap title 120, message 500, vi/en parity, HTML/script reject, `startsAt < endsAt`, severity/target/status enum) + `nextLiveOpsAnnouncementStatus` (state machine, idempotent guard cho cron) + `LiveOpsAnnouncementBroadcastPayload`/`LiveOpsEventBroadcastPayload` types + `LiveOpsAnnouncementPublicView` strip `id`/`adminId`/`disabledAt`/timestamps. **Prisma migration `20260621000000_phase_15_3_b_liveops_announcement`**: model `LiveOpsAnnouncement` (id/key UNIQUE/severity/status/target/titleVi/titleEn nullable/messageVi/messageEn nullable/startsAt/endsAt/createdByAdminId nullable/createdAt/updatedAt/disabledAt nullable + 3 index). Reuse `AdminAuditLog`. **API (`apps/api/src/modules/liveops-announcement/`)**: `LiveOpsAnnouncementService` (`listAnnouncements`/`getActivePublic`/`createAnnouncement`/`updateAnnouncement`/`disableAnnouncement`/`recomputeStatusesWithTransitions`) — recompute idempotent qua `updateMany` CAS, race-safe; `AdminLiveOpsAnnouncementsController` 5 endpoint `RequireAdmin` (`GET /admin/liveops/announcements` + `POST` + `PATCH :id` + `POST :id/disable` + `POST recompute-status`) audit `ADMIN_LIVEOPS_ANNOUNCEMENT_*`; `LiveOpsAnnouncementsPublicController` 1 endpoint `GET /liveops/announcements/active` (chỉ `ACTIVE`, public-safe, exclude `ADMIN_ONLY`). `LiveOpsBroadcastService` reuse `RealtimeService.io.emit` cho 2 channel (chat/cultivation gateway pattern), fail-safe try/catch log warn không throw. **Cron piggy-back**: `LiveOpsEventSchedulerCronProcessor` gọi `recomputeStatusesWithTransitions` cho cả event + announcement, broadcast khi và chỉ khi status thật transition (anti-spam). `AdminLiveOpsEventsController.recompute` cũng broadcast khi admin force-run. **FE**: `LiveOpsAnnouncementMarquee.vue` mới (severity color/badge + countdown 30s tick + dismiss sessionStorage + render NULL khi empty không chiếm chỗ) gắn vào `HomeView` cho cả anonymous viewer; Pinia store `stores/liveopsAnnouncements.ts` (visibility filter ACTIVE+window+!dismissed, WS handler upsert/remove, `lastEventBroadcastAt` bump cho event panel refresh, auto refresh 60s); `apps/web/src/api/liveopsAnnouncement.ts` client public; `AdminLiveOpsAnnouncementsPanel.vue` admin form list (status/severity badge, time window display, create form + disable + recompute + audit log via BE); `LiveOpsActiveEventsPanel` watch `lastEventBroadcastAt` để refetch khi event transition. **i18n**: `liveopsAnnouncementMarquee.*` (severity label vi/en, dismiss aria, countdown, toast) + `adminLiveOpsAnnouncements.*` (form fields, errors `ANNOUNCEMENT_*`, actions). **Tests**: shared 16 (validator + transition + payload safety); api 32 (service CRUD + idempotency + status machine + race-safe + admin guard + public exclude DISABLED/ENDED/ADMIN_ONLY + broadcast called once on transition + payload no admin field); web 11 (marquee empty/render/dismiss/WS upsert/WS event refresh + admin panel CRUD + i18n parity). Risk: WS broadcast fail-safe (status transition vẫn commit). Rollback: revert PR — admin panel/marquee gone, cron piggy-back tự ẩn (không gọi nếu module không import). **KHÔNG** spam broadcast mỗi tick, **KHÔNG** gửi raw configJson, **KHÔNG** cho HTML/script injection, **KHÔNG** broadcast `ADMIN_ONLY` ra public room. Branch `devin/phase-15-3-b-announcement-ws-marquee`. | Feature |
| #512 | feat(api,web,shared): **Phase 15.3.A — LiveOps Runtime Expansion + Festival Gift Claim**. Mở rộng runtime cho 5/7 event type chỉ có enum/storage trong Phase 15.1–15.2 — `SHOP_DISCOUNT`, `SECT_SHOP_DISCOUNT`, `DAILY_LOGIN_BONUS`, `BOSS_REWARD_BOOST`, `FESTIVAL_GIFT`. Sau merge: **7/7 event type wired runtime thật**. **Shared**: thêm `LIVEOPS_RUNTIME_SUPPORTED_TYPES` + `LIVEOPS_FESTIVAL_GIFT_REWARD_CAPS` + `validateLiveOpsEventRewardJson` + `parseLiveOpsEventReward` + `LiveOpsEventReward` type. Validator cũ extend gọi `validateLiveOpsEventRewardJson` khi `type='FESTIVAL_GIFT'`. **API runtime**: `ShopService.buyFromShop` áp `SHOP_DISCOUNT` (`finalPrice = ceil(originalPrice × (1 − mul))`, ledger ghi `finalPrice` thực chi); `SectShopService.buyFromSectShop` áp `SECT_SHOP_DISCOUNT` cho cost contribution + linh thạch (giữ daily/weekly limit + contribution requirement); `DailyLoginService.claimToday` áp `DAILY_LOGIN_BONUS` sau Daily Reward Cap (idempotent UNIQUE `(characterId, dateLocal)`); `BossRewardService.distributeRewards` áp `BOSS_REWARD_BOOST` vào reward attribution rank với mail metadata `liveOpsBoostMultiplier`+`liveOpsEventKey`; `LiveOpsEventSchedulerService.claimEventReward(characterId, eventKey)` idempotent qua UNIQUE `(eventId, characterId)`, transactional grant CurrencyLedger/ItemLedger. **API public endpoints (`liveops-events.controller.ts`)**: `GET /liveops/events/active` (public-safe) + `POST /liveops/events/:eventKey/claim` (FESTIVAL_GIFT one-time idempotent). **FE**: panel mới `LiveOpsActiveEventsPanel.vue` (countdown `endsAt`, multiplier label `x1.5`/`30% off`, reward summary, claim button + confirm + toast, auto-refresh 60s, loading/empty/error) gắn vào `HomeView`; `AdminLiveOpsEventsPanel.vue` thêm runtime support badge per row + cap-aware multiplier input + helper text reward JSON + FE-side `validateLiveOpsEventRewardJson`. **i18n**: namespace mới `liveopsActiveEvents.*` + 7 error code `EVENT_REWARD_*` + 3 admin runtime keys + `multiplierWithCap`/`rewardJsonHelp`/`runtimeNotWiredWarn`. Compose policy max-only (no stack) giữ nguyên — caps không đổi (drop/exp ≤ 2.0, discount ≤ 0.5, FESTIVAL_GIFT reward `linhThach` ≤ 1000, `tienNgoc` ≤ 50, ≤ 10 items × qty ≤ 50). **Tests**: shared validator add (empty/over-cap/invalid-item/wrong-currency reject) + runtime support invariant; api shop/sect/daily-login/boss runtime + festival claim happy-path/idempotent/non-active reject/wrong-type reject/invalid reward config reject; web `LiveOpsActiveEventsPanel` 6 cases + `AdminLiveOpsEventsPanel` 3 cases mới + api client `liveops` 6 cases (URL encoding + envelope error). Risk: `BossRewardService` mail metadata add — backwards compat (extra fields). Rollback: revert PR; runtime modifiers fail-soft 1.0/no-discount nếu service unavailable. KHÔNG bypass Daily Reward Cap, KHÔNG rewrite scheduler core, KHÔNG gacha/pet/wife/battle pass. Branch `devin/phase-15-3-a-liveops-runtime-expansion`. | Feature |
| #511 | feat(api,web,shared): **Phase 15.1–15.2 — LiveOps Event Scheduler Core**. Admin-driven event scheduler vận hành sự kiện theo thời gian KHÔNG cần deploy code. 7 type (`DOUBLE_DUNGEON_DROP`, `CULTIVATION_EXP_BOOST`, `SHOP_DISCOUNT`, `SECT_SHOP_DISCOUNT`, `DAILY_LOGIN_BONUS`, `BOSS_REWARD_BOOST`, `FESTIVAL_GIFT`). Status machine `DRAFT → SCHEDULED → ACTIVE → ENDED` (+ `DISABLED` kill switch). Cron 5-phút auto-transition theo `startsAt`/`endsAt`. Multiplier capped server-side: drop/exp ≤ 2.0, discount ≤ 0.5. **Shared (`packages/shared/src/liveops-event-scheduler.ts`)**: `LIVEOPS_EVENT_TYPES` + `LIVEOPS_EVENT_STATUSES` + `LIVEOPS_EVENT_TYPE_CAPS` + `LIVEOPS_EVENT_KEY_PATTERN` + `validateLiveOpsScheduledEventInput` + `clampLiveOpsMultiplier` + `pickActiveLiveOpsMultiplier` (max-only, no-stack) + `nextLiveOpsScheduledEventStatus` (state machine). **Prisma migration `20260620000000_phase_15_1_liveops_event_scheduler`**: 2 model additive `LiveOpsScheduledEvent` (id/key UNIQUE/type/title/description/status/startsAt/endsAt/configJson JSONB/createdByAdminId/createdAt/updatedAt + 3 index) và `LiveOpsEventRewardClaim` (UNIQUE `(eventId, characterId)`). **API (`apps/api/src/modules/liveops-event-scheduler/`)**: `LiveOpsEventSchedulerService` (`listEvents`/`getActiveEvents`/`getRuntimeModifiers`/`getActiveMultiplier`/`createEvent`/`updateEvent`/`disableEvent`/`recomputeStatuses`) — recompute idempotent qua `updateMany` CAS guard, race-safe multi-instance. `AdminLiveOpsEventsController` 5 endpoint `RequireAdmin` (`GET /admin/liveops/events` + `POST` create + `PATCH :id` update + `POST :id/disable` + `POST recompute-status`) audit `ADMIN_LIVEOPS_EVENT_*`. **Cron BullMQ (`liveops-event-scheduler.cron.{config,scheduler,processor}.ts`)**: queue `liveops-event-scheduler-cron` repeatable `*/5 * * * *` UTC, Redis lease `xt:liveops-event-scheduler:recompute` (60s TTL). Disabled by default — bật qua env `LIVEOPS_EVENT_SCHEDULER_CRON_ENABLED=true`. **Runtime integration**: `DungeonRunService.claimRun` query `getActiveMultiplier('DOUBLE_DUNGEON_DROP')` → áp `floor(linhThach × mul)` + `max(1, floor(itemQty × mul))`; `CultivationProcessor.process` query `getActiveMultiplier('CULTIVATION_EXP_BOOST')` 1× per tick → compose vào `requestedGain` cuối. Cả 2 fail-soft try-catch fallback 1.0. **FE (`apps/web/src/components/AdminLiveOpsEventsPanel.vue`)**: panel mới gắn vào AdminView tab `liveops` — render table event (key/type/status badge/window/configJson) + form tạo + nút Disable + nút Recompute. i18n VI/EN keys `adminLiveOpsEvents.*` (parity test passed). **Tests**: shared 78 (existing pass), API 16 service + 10 admin controller + 7 runtime integration, web 5 panel. Risks: cron disabled by default, manual ACTIVE/ENDED reject (chỉ qua cron), wire runtime hạn chế (5/7 type defer Phase 15.3+), reward claim chưa làm. Next: wire SHOP_DISCOUNT/SECT_SHOP_DISCOUNT/DAILY_LOGIN_BONUS/BOSS_REWARD_BOOST/FESTIVAL_GIFT + reward claim API + UI editor JSON cho FESTIVAL_GIFT rewardJson. | feat |
| #511.5 | feat(api,web,shared): **Phase 15.0.A — Equipment Reforge / Enchant Foundation**. 2 sink late-game tối ưu trang bị, song song refine (Phase 11.5) và gem (Phase 11.4). **Reforge** (`tẩy luyện`): re-roll substat phụ trong `ALLOWED_SUBSTAT_KINDS = atk/def/hpMax/mpMax/spirit`, slot count + value range theo quality (PHAM 1 / LINH 2 / HUYEN 3 / TIEN 3 / THAN 4). **Enchant** (`phụ ma`): gắn 1 hệ Ngũ Hành (`kim/moc/thuy/hoa/tho`) lên trang bị, level 0..`MAX_ENCHANT_LEVEL=5`, mỗi level cộng bonus nhỏ theo identity (Mộc +12 hpMax/level, Hỏa +2 atk/level, Thổ +2 def/level, Kim +1 atk/level, Thủy +6 mpMax/level). Cost linhThach + material (`tinh_thiet`/`yeu_dan`/`han_ngoc`) tăng theo quality + level. **Shared (`packages/shared/src/equipment-upgrade.ts`)**: `EQUIPMENT_REFORGE_CONFIG` + `EQUIPMENT_ENCHANT_CONFIG` + `ELEMENTAL_ENCHANT_EFFECTS` + `MAX_ENCHANT_LEVEL` + helpers `getReforgeCost`/`getEnchantCost(quality, currentLevel)` (geometric `base × (level+1)`)/`rollReforgedSubstats`/`composeSubstatBonus`/`composeEnchantBonus`/`parseEnchantElement`/`isUpgradableItemKind`. **Prisma migration `20260619000000_phase_15_0_a_equipment_reforge_enchant`**: `InventoryItem` thêm 3 cột `substatsJson` JSONB default `[]`, `enchantElement` TEXT NULL, `enchantLevel` INT default 0; tạo `EquipmentReforgeHistory` + `EquipmentEnchantHistory` audit table với FK `Character(id)` ON DELETE CASCADE. **API (`apps/api/src/modules/character/equipment.service.ts` + `character.controller.ts`)**: `EquipmentService.reforge / enchant / upgradePreview` + 3 endpoint POST `/character/equipment/{reforge,enchant,upgrade-preview}`. Atomic `prisma.$transaction`: verify ownership → consume material `updateMany gte` guard → consume linhThach `CurrencyService.applyTx` (gte guard) → mutate `InventoryItem` → ghi history audit row. Errors: `EQUIPMENT_NOT_FOUND/INVALID_EQUIPMENT/INSUFFICIENT_FUNDS/INSUFFICIENT_MATERIAL/MAX_ENCHANT_REACHED/INVALID_ELEMENT/ELEMENT_LOCKED`. **Concurrent-safe**: 2 thread reforge cùng item → 1 thread thắng, thread kia rollback (no double spend / no corrupt stat). **Combat integration**: `InventoryService.equipBonus` cộng substat + enchant → `CombatService.derivedStats` tự include qua pipeline cũ. Arena snapshot Phase 14.1.B capture `equipBonus` đã tính reforge + enchant. **Ledger reason mới**: `CurrencyLedger.reason='EQUIPMENT_REFORGE'/'EQUIPMENT_ENCHANT'`; `ItemLedger.reason='EQUIPMENT_REFORGE_COST'/'EQUIPMENT_ENCHANT_COST'`. **FE (`apps/web/src/components/EquipmentUpgradePanel.vue`)**: panel render reforge cost preview + Reforge button (overwrite confirm modal) + 5 element chip (lock khi level≥1) + Enchant button (confirm modal, disabled ở MAX). Toast success/error theo error code. KHÔNG crash khi item thiếu substat/enchant. i18n VI/EN `equipment.upgrade.*`. **Tests added: 58** (shared `equipment-upgrade.test.ts` 25 + api `equipment.service.test.ts` 20 + web `EquipmentUpgradePanel.test.ts` 13). **Anti-balance-break**: substat ranges + enchant per-level bonus cố ý giữ < +20% baseline tier — không lật meta Arena/PvE. KHÔNG gacha, KHÔNG phá hủy trang bị, KHÔNG auction house, KHÔNG rewrite inventory. **Known limitations**: chưa hỗ trợ chuyển hệ enchant (`ELEMENT_LOCKED` từ level 1); chưa có protection charm; chưa reforge main stat. **Next recommendation**: Phase 15.1–15.2 LiveOps Event Scheduler Core. |
| #506 | feat(api,web,shared): Phase 14.1.B Async Arena Foundation. **Arena PvP bất đồng bộ — wire combat snapshot Phase 14.1.A** vào REST endpoints + UI. **Shared (`packages/shared/src/arena.ts` mới)**: enums (`ArenaMatchStatus` PENDING/RESOLVED/CANCELLED, `ArenaMatchOutcome`, `ArenaErrorCode`), `ARENA_RATING_DEFAULT=1000`, win +10 / lose -5 / draw 0 (attacker; defender half magnitude), daily limit 10/day Asia/Ho_Chi_Minh, helpers `arenaRatingDeltaFor`/`clampArenaRating`/`arenaDayBucket`/`arenaRankTierFor`. **Prisma**: `ArenaProfile { characterId @unique, rating, wins, losses, draws, attacksToday, lastAttackDayBucket, defenseSnapshotJson? }` + `ArenaMatch { attackerCharacterId, defenderCharacterId, status, result, winnerCharacterId?, attackerSnapshotJson, defenderSnapshotJson, seed, battleLogJson, ratingDeltaJson?, createdAt, resolvedAt? }`. Migration `20260616000000_phase_14_1_b_arena_foundation`. **API endpoints**: `GET /arena/profile` (lazy create), `GET /arena/opponents?limit=N` (rating ±200 + fallback random), `POST /arena/matches` (build snapshot → resolve qua `resolveCombatWithSnapshot` Phase 14.1.A → tx update profile + match), `GET /arena/matches/history?limit=N&side=all|attacker|defender`. Auth cookie + character required. Errors 401/404/400/429. `ARENA_DAILY_LIMIT_PER_DAY` env (0=unlimited). Resolution sync trong cùng request (PENDING insert → derive seed `hashSeed("arena-match:<id>")` → resolve → RESOLVED update). **FE**: `ArenaView.vue` + route `/arena` (profile card + last-result banner + opponents list + history) + Pinia store + API client. i18n vi/en parity (`arena.*` + `common.dismiss` + `apiFallback.arena*`). **Tests added: 76** (shared 16 ✅ + api 19 ✅ + web 38 ✅ + i18n parity ✅). **KHÔNG**: realtime PvP, season/ELO/reward, anti-wintrade, cross-server. Next: Phase 14.1.C Arena Season + ELO + Reward. | feat |
| #505 | test(api,shared): Phase 14.1.A combat determinism audit. Shared `combat-rng.ts` (mulberry32 + helpers), `combat-snapshot.ts` (CombatActorSnapshot + CombatSimulationSnapshot + buildCombatActorSnapshot/normalizeCombatSnapshot/resolveCombatWithSnapshot pure deterministic 1v1 reference resolver). RNG injection backward-compat (optional `rng` param). 71 determinism tests added. KHÔNG đổi balance. | test |
| #504 | feat(web): Phase 14.3.E.2 tribulation mini battle frontend. **FE wire mini-battle Thiên Kiếp** sau khi backend Phase 14.3.E.1 đã ship 4 endpoint state-machine. **API client** (`apps/web/src/api/tribulation.ts`): 4 function (`fetchCurrentTribulationBattle`, `startTribulationBattle`, `submitTribulationBattleAction`, `resolveTribulationBattle`) + view types (`TribulationMiniBattleView`/`TribulationMiniBattleStateView`/`TribulationBattleActionKey`/`TribulationMiniBattleEffectTypeView`/`TribulationBattleEventView`/`TribulationMiniBattleSummaryView`). **Pinia store extend** (`apps/web/src/stores/tribulation.ts`): state `miniBattle{,Loading,Starting,ActionLoading,Resolving,Error,Available,LastResult}` + computed `miniBattleCanAct`/`miniBattleIsTerminal` + actions `fetchCurrentBattle`/`startBattle`/`submitBattleAction`/`resolveBattle`/`resetMiniBattleError`/`clearMiniBattle`; race-safety in-flight guard mỗi action; `clientNonce` per submit (server idempotent dedupe); 501 `TRIBULATION_MINI_BATTLE_UNAVAILABLE` → `miniBattleAvailable=false` (không raise UI error). **5 Vue components** (`apps/web/src/components/`): `TribulationMiniBattlePanel.vue` (orchestrator: start/action/auto-resolve khi terminal/dismiss modal), `TribulationBattleStatus.vue` (realm/element/effect badge + state + phase progress + HP/shield/DOT/focus chip), `TribulationBattleActions.vue` (5 action button + double-click guard + per-action tooltip hint), `TribulationBattleLog.vue` (event log: damage/shield/heal/DOT/crit chip + i18n message lookup), `TribulationBattleResultModal.vue` (win/lose + CTA "Quay lại tu luyện" / "Thử lại" / Esc-to-close). **TribulationView integration**: conditional `miniBattlePanelVisible` chỉ true khi `miniBattleAvailable === true` + `atPeak` + có encounter; `null`/`false` (initial hoặc 501) → giữ nguyên encounter resolve UI Phase 14.3.D (backward compat); `onMounted` gọi `fetchCurrentBattle` hydrate snapshot; handlers `onMiniBattleErrored` (toast i18n) + `onMiniBattleReturnCultivation` (refetch state/history/preview/encounter rồi `router.push('/cultivation')`). **i18n VI/EN parity**: namespace `tribulation.miniBattle.*` ~70 key (title/subtitle, action labels+short+hints, state labels, log labels + 11 message keys, result modal + 3 CTAs, 5 effect-type hint) + 7 error code mới (`MINI_BATTLE_DISABLED`, `MINI_BATTLE_NOT_FOUND`, `MINI_BATTLE_ALREADY_ACTIVE`, `MINI_BATTLE_TERMINAL`, `MINI_BATTLE_NOT_TERMINAL`, `MINI_BATTLE_INVALID_ACTION`, `TRIBULATION_MINI_BATTLE_UNAVAILABLE`). **Tests added**: 11 API client + 15 store + 11 component + 4 view = 41 mini-battle test mới (web total 1656/1656 pass, 0 regression). FE-only PR, **không** đụng backend logic; FE thuần orchestrator + render. **Next task**: Phase 14.1–14.2 Async Arena Foundation hoặc Combat Determinism Audit. | feat |
| #503 | feat(api,shared): Phase 14.3.E.1 tribulation mini battle backend. **Backend mini-battle với state machine + 5 effectType + idempotency + race-safety** — KHÔNG đụng FE (deferred Phase 14.3.E.2). Shared catalog `tribulation-mini-battle.ts` 980 LOC: state enum (PENDING/ACTIVE/RESOLVED/FAILED/EXPIRED), action enum (ATTACK/DEFEND/FOCUS/CLEANSE/CHANNEL), effectType enum (BURST/SUSTAIN/POISON_RECOVERY/ARMOR_CRIT/DEFENSE_ENDURANCE), pure helpers `computeTribulationPhaseResult`/`computeTribulationBattlePower`/`applyTribulationEffectType`/`validateTribulationBattleAction`/`summarizeTribulationBattleResult`/`makeInitialMiniBattleSnapshot`, deterministic seeded RNG `mulberry32`+`composeBattlePhaseSeed` (KHÔNG `Math.random` core calc), anti-cheat caps (HP/DAMAGE/HEAL/SHIELD/DOT_STACKS). API `tribulation-mini-battle.service.ts` 800+ LOC: 4 endpoint mới `/character/tribulation/battle/{current,start,action,resolve}` (Nest controller wired in `character.controller.ts`); idempotency qua `lastClientNonce` dedupe + `resultJson.attemptLogId` pointer (2nd resolve reconstruct outcome từ existing `TribulationAttemptLog` row, KHÔNG double reward/consume/breakthrough); race-safety qua optimistic update `WHERE state=<expected> AND currentPhase=<expected>` (concurrent action throw `MINI_BATTLE_INVALID_ACTION`); `TribulationService.runAttemptInTxWithForcedOutcome` extend Phase 14.3.A path (backward-compat — call site cũ KHÔNG đổi) drive realm advance + reward + consume support + penalty 1:1 với attemptTribulation. Prisma model `TribulationMiniBattle` (`id/characterId/encounterId/tribulationKey/realmKey/effectType/element/difficulty/state/currentPhase/phaseCount/playerHp[Max]/tribulationHp[Max]/shield/dotStacks/focusCharge/seed/actionLogJson/resultJson/lastClientNonce/startedAt/resolvedAt/createdAt/updatedAt`) + 3 index + Character relation Cascade; migration `20260615000000_phase_14_3_e_1_tribulation_mini_battle`. **Feature flag `TRIBULATION_MINI_BATTLE_ENABLED` default OFF** — flow legacy Phase 14.3.D `POST /character/tribulation/encounter/{start,resolve}` vẫn nguyên vẹn; bật flag → mini-battle endpoints active, tắt → 4 endpoint trả 501 `MINI_BATTLE_DISABLED` (FE fallback). Metrics singleton `MINI_BATTLE_METRICS{started,resolved,failed}` mirror Phase 17.5 request-metrics middleware pattern (read-only export `readTribulationMiniBattleMetrics()`). Logger structured: `tribulation_battle_started/resolved/failed` với battleId+characterId+realmKey+result. Test added: 41 shared (5 effectType mechanics + RNG determinism cùng seed + caps enforcement + state machine + power ratio) + 23 API integration (feature flag enabled/disabled fallback + start guards NOT_AT_PEAK/ALREADY_ACTIVE/CHARACTER_NOT_FOUND + getCurrent null/active + action validation NOT_FOUND/INVALID_ACTION/PHASE_OVERFLOW + clientNonce idempotent + concurrent advance race + terminal block + resolve NOT_TERMINAL/NOT_FOUND/win path realm advance/lose path cooldown/idempotent re-resolve + effectType wiring) — total 64 tests mới. wipeAll thêm `prisma.tribulationMiniBattle.deleteMany({})` cho test cleanup. KHÔNG fake green / KHÔNG `it.skip` / KHÔNG sửa CI / KHÔNG modify test cũ. Risk: 🟡 medium — migration thêm bảng (rollback = drop table), feature flag default OFF nên KHÔNG ảnh hưởng prod nếu chưa bật. Docs API.md / BALANCE_MODEL.md / CHANGELOG.md / AI_HANDOFF_REPORT.md updated trong cùng PR. **Next task**: Phase 14.3.E.2 Tribulation Mini-Battle Frontend. |
| #502 | feat(api,ops): Phase 17.5 metrics and load test baseline. **Closed-beta capacity prep** — admin-only metrics endpoint + 5 fail-soft collectors + k6 load test scaffold + docs threshold. **API endpoint** `GET /api/admin/metrics` (`MetricsController`, `@UseGuards(AdminGuard)` + `@RequireAdmin()` — PLAYER+MOD reject 403). JSON `{ ok: true, data: MetricsSnapshot }` (schema=1) gồm `system { uptimeMs, node, memory, cpu, pid, appVersion }` + `api.request { totalRequests, totalDurationMs, avgDurationMs, byMethod, byStatusBucket, inFlight, lastResetAt }` + `ws { onlineUsers, serverBound }` + `queue { available, queues[] }` (7 BullMQ queue qua Redis `llen`/`zcard` prefix `bull:<name>:<state>`) + `cron { available, jobs[] }` (4 model: EconomyLedgerCheckRun / SectTerritorySettlementSnapshot / SectTerritoryDecayLog / SectSeasonSnapshot) + `errors[]` fail-soft. **API service** `apps/api/src/modules/metrics/metrics.service.ts` 5 collector — Redis down → `queue.available=false`, Prisma 1 model lỗi → chỉ row đó null, KHÔNG throw 500. **Request middleware** `request-metrics.middleware.ts` singleton in-memory counter idempotent qua `res.on('finish'\|'close')`, bounded set method/status (KHÔNG track per-path để tránh memory leak), skip `/api/healthz` `/api/readyz` `/api/admin/metrics`. **Load test scripts** `scripts/load/`: `k6-smoke.js` (1 VU × 10s healthz/readyz/version, threshold p95<800ms), `k6-api-baseline.js` (3 VUs × 30s flow login → state → daily-login → mission → dungeon → territory, custom counter `xt_login_failures`/`xt_flow_success_rate`, threshold p95<1500ms / error<5% / success>90%), `k6-ws-baseline.js` (5 VUs × 20s engine.io v4 raw WebSocket connect/wait-frame/disconnect, threshold ws_connect_success>95%). **Package scripts**: `load:smoke`/`load:api`/`load:ws` delegate `k6 run`. **Env vars** k6: `BASE_URL`, `WS_URL`, `TEST_EMAIL`, `TEST_PASSWORD`, `AUTH_TOKEN`, `VUS`, `DURATION`. **Tests added**: 31 (`metrics.service.test.ts` 11 + `metrics.controller.test.ts` 3 + `metrics.service.integration.test.ts` 6 + `request-metrics.middleware.test.ts` 11). **Security**: payload scan substring KHÔNG có cookie/password/jwt/refreshToken/userId/characterId/email; KHÔNG audit log endpoint metrics (poll cao tần). **Docs updated**: API.md (Phase 17.5 section), DEPLOY.md (§12.4 metrics + §12.5 load test env), RUNBOOK.md (§4.5 k6 cài+chạy+threshold+pitfalls), CHANGELOG.md, scripts/load/README.md (mới). **Known limitations**: per-path histogram để follow-up Phase 17.6; k6 raw WS chỉ verify handshake (full E2E protocol cần Artillery). **Risk/rollback**: revert single PR — middleware skip default Phase 17.4 endpoints không thay đổi shape API existing. **Next task**: Phase 14.3.E.1 Tribulation Mini-Battle Backend. | feat |



**Phase summary tables (PR #1 → #396) + smoke detail per-module + PR #414/#415 + PR #430/#437 entries**: tách sang [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Phase Summary Migrated 2026-05-05 + § Smoke Detail Migrated 2026-05-05.

---

## 3. Current Phase Status

| Phase | Title | Status | Note |
|---|---|---|---|
| 27.1–27.5 | Monetization Systems V1 (Monthly Card / Sweep / Battle Pass / Growth Fund / Limited Shop) | **OPEN** ⏳ (PR #557 — this PR) | Real monetization systems extending Phase 27.0 Foundation. Battle Pass V2 missions catalog, Growth Fund V2 `tien` variant, Limited Periodic Shop (DAILY/WEEKLY/MONTHLY), `MonetizationOverviewService`. Anti-P2W validator + forbidden endgame item list. Server-authoritative qua `BattlePassMissionProgress` + `LimitedShopPurchase` unique CAS. |
| 27.0 | Monetization Foundation (Wallet / Shop / Entitlements / Sweep / Extra Attempts / Growth Fund) | **CLOSED** ✅ (PR #556) | Foundation layer dùng cho Phase 27.1–27.5. 6 wallet currencies, 9 entitlements với `ENTITLEMENT_VALUE_CAPS` anti-P2W, monthly card variants, shop catalog với purchase limit, sweep, extra-attempt CAS daily-cap, growth fund. |
| 25.1 | Battle Pass / Monthly Card / VIP Light | **CLOSED** ✅ (PR #546) | Light monetization foundation (superseded by Phase 27.0/27.1–27.5): seasonal BP free/premium tracks, 30-day Monthly Card daily claim, VIP Light read-only perks/admin grant. Guardrails: no top gear/max pháp bảo sale, no `requiredRealmOrder` bypass, full ledger + anti-duplicate. |
| 26.0.1 | Body Cultivation Runtime Verification | **DONE in this PR** | Postgres/Redis migration clean; API/runtime smoke verified default `pham_than`, start/stop, tick EXP + stamina, `BODY_CULTIVATION` cap, missing-exp failure, no currency leak; added regression coverage for breakthrough material/log/injury/Qi gate/concurrency rollback and UI confirm modal. |
| 0–8 | Foundation: schema + auth + core gameplay | **Done** ✅ | Full feature catalog ở [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Completed Features. |
| 9 | Beta readiness (Phase 9.A→9.E sub-phases polish) | **11/15 Done, 3 Partial** | Detail [`BETA_CHECKLIST.md`](./BETA_CHECKLIST.md). Mail UI gap closed by PR #391. |
| 10 | Content scale | **5/5 CLOSED** ✅ | All sub-tracks merged. |
| 11 | Progression Depth | **catalog 11/11 + runtime 10/10 + UI merged** | Phase 11 nâng cao (§2/§3/§5/§6 + 11.6.C/D/E + 11.1.E) tất cả CLOSED. |
| 11.X | UI E2E smoke Playwright | **DONE** | Spec #19 talent learn→cast→cooldown + #20 breakthrough flow merged via #394/#420. |
| 12 | World Map & Dungeon | **CLOSED** ✅ | 12.1 catalog CLOSED (#397). 12.2.A `dailyLimit` enforcement CLOSED (#421). Story PR-1→PR-6 + Foundation Extension + Late-game wire CLOSED (#425–#433). **12.2.B/C + 12.3** CLOSED ✅ (#434–#436). **E2E spec #22 dungeon-run flow** CLOSED ✅ (#437). **12.4 per-monster `MonsterDef.lootTable` polish** CLOSED ✅ (#438). **Story Foundation Late-game encounter wire** CLOSED ✅ (#439). **Story discoverability — QuestView dungeon hint cho kill+monster step** CLOSED ✅ (#440). **docs handoff sync** CLOSED ✅ (#441). **12.5 dungeon balance tuning — 8 late-game story monster stat + lootTable** CLOSED ✅ (#442). **12.6 Boss-by-region auto-spawn** CLOSED ✅ (#448 — closes Phase 12 exit criterion "Auto-spawn boss working with idempotency test"). Next (optional): dialogue branch / DungeonTemplate story-instance. |
| Concurrency phase 2 | Race condition fix sweep | **CLOSED** ✅ | All 6 races fixed: Inventory `use()` (#422), Inventory `revoke()` (#443), Realtime ban-during-connection (#444), Cultivation tick CAS (#445), Chat Redis failover (#446), Boss spawn cron auto race (#447). Concurrency phase 2 **DONE**. |
| 13.0 | LiveOps & Retention Suite | **CLOSED** ✅ | Daily-login + Quest cadence + Title catalog + Buff HUD live trên main (post #456 audit pass #5). |
| 13.1.A | Sect War Core / Contribution / Leaderboard / Weekly Reward | **CLOSED** ✅ | PR #457 merged + PR #458 hardening audit (daily cap timezone fix + doc UNIQUE order fix). |
| 13.1.B | Sect Missions + Sect Shop + Admin LiveOps Controls | **CLOSED** ✅ | PR #459 merged. Server-audited mission ledger + contribution-currency shop với caps/CAS/RATE_LIMITED + admin event toggle + sect-war recalc. Migration `20260601000000_phase_13_1_b_sect_missions_shop_liveops`. |
| 13.2.A/B/C | Sect Season Foundation / Milestones+Rewards / History+Hall of Fame | **CLOSED** ✅ | PR #472 + #475 + #474 merged. `SectSeasonClaim` UNIQUE CAS guard + 5-tier milestone reward grant atomic. |
| R1 | Production Readiness CSP/env/healthcheck | **CLOSED** ✅ | PR #473 merged. Helmet 11 directive prod CSP + bootstrap-config helper + secret-leak guard. M7 deploy verify still open §4. |
| 17.1 | Deploy Verify Gate (env schema + verify:deploy + CI smoke) | **CLOSED** ✅ | PR #522 merged. Zod strict env validation (7 critical) + `pnpm verify:deploy` 7-step orchestrator + CI job `verify-deploy` (Postgres+Redis service). 32 lock-in test. Closes Phase R1 §4 M7 backlog. |
| 17.2 | Backup/Restore Weekly Verification (admin tracking layer trên shell scripts) | **OPEN** ⏳ (PR #523) | `BackupRun`/`BackupVerifyRun` Prisma model + `BackupService` spawn safe + BullMQ weekly cron default disabled + Admin API `/admin/backup/{status,run,verify}` + `AdminBackupPanel` FE. 31 BE + 11 FE test. KHÔNG expose restore endpoint (manual theo RUNBOOK). S3/MinIO offsite defer sang 17.3. |
| 12.10.A | NPC Affinity Foundation | **CLOSED** ✅ | PR #476 merged. `CharacterNpcAffinity` per-(char, npcKey) + `addAffinityTx` cap clamp + tier unlocks; FE NpcAffinityPanel read-only. |
| 12.10.B | NPC Gift + Quest Affinity Rewards | **CLOSED** ✅ | PR #484 merged. `npc-gift.ts` catalog + `POST /story/npc-affinity/:npcKey/gift` atomic + Quest reward `affinity?{npcKey, delta}` idempotent + FE gift panel. Migration `20260609000000_phase_12_10_b_character_npc_gift_log`. Daily bucket UTC 00:00. |
| 14.0.A | Sect Territory Influence Foundation | **CLOSED** ✅ | PR #479 merged. Region leaderboard + dungeon/boss hooks fail-soft + idempotent UNIQUE + 9 region. Migration `20260607000000_phase_14_0_a_sect_territory_influence`. |
| 14.0.B | Sect Territory Settlement + Region Ownership | **CLOSED** ✅ | PR #481 merged. `SectTerritoryRegionState` + `SectTerritorySettlementSnapshot` UNIQUE `(regionKey, periodKey)` race-safe + `settleRegion`/`getOwnerStateMap` + admin route. Region buff khi rank 1 chưa wire (defer 14.0.C); decay/season reset chưa có. Migration `20260608000000_phase_14_0_b_territory_settlement`. |
| 14.2.A | Elemental Combat Foundation | **CLOSED** ✅ | PR #477 merged. `applyElementalCombatAdjustment` pipeline clamp `[0.5, 1.6]` wire skill + active talent path. |
| 14.2.B | Elemental Combat Data + Balance | **CLOSED** ✅ | PR #482 merged. Catalog `elementalResist` 6 boss + 8 monster + `bonuses.elementalAtkBonus` 6 equipment + invariant tests floor/ceil/anti-power-creep + InventoryView tooltip. KHÔNG migration. |
| 14.3.A | Breakthrough Tribulation Foundation | **CLOSED** ✅ | PR #478 merged. `TRIBULATION_REQUIRED` gate + `previewTribulation` + supports signature rỗng. |
| 14.3.B | Tribulation Support Providers + Redirect UX | **CLOSED** ✅ | PR #483 merged. 4 provider read-only (item/buff/equipment/talent) + `successChance.supportBonus` cap 0.30 + FE redirect `/tribulation` + preview badge source/element/cap warning. KHÔNG consume / KHÔNG migration — preview read-only, item consumption defer Phase 14.3.C. |
| 14.0.C | Sect Territory Region Buff + Influence Decay | **CLOSED** ✅ | PR #486 merged. 5 region buff catalog (EXP/drop/element/def) cap ≤5% + `getActiveBuffsForCharacter` wire combat + admin `POST /admin/territory/decay` idempotent UNIQUE periodKey race-safe + `GET /admin/territory/decay/history`. |
| 14.0.D | Territory Weekly War Loop | **CLOSED** ✅ | PR #487 merged. ISO week period (`YYYY-Www`) + `GET /territory/war/current/regions/:k/history` public + admin `settle-current` + FE TerritoryView war panel countdown server-authoritative. |
| 14.0.E | Territory Owner Reward Mail Service | **CLOSED** ✅(this PR) | Catalog `TERRITORY_OWNER_REWARDS` (9 region) + service `grantWeeklyOwnerRewardMail(periodKey)` + admin endpoint `POST /admin/territory/rewards/grant-weekly` + FE TerritoryView admin button. Idempotent qua `TerritoryOwnerRewardGrant` UNIQUE `(periodKey, regionKey, characterId)` + race-safe (P2002 swallow). dryRun support. Migration `20260612000000_phase_14_0_e_territory_owner_reward_grant`. **NO cron** — admin trigger only; cron handoff Phase 14.0.F. |
| 14.2.C | Elemental Skill Tree Expansion | **CLOSED** ✅ | PR #490 merged. `SkillDef.tags` (HEAL/DOT/BURST/SHIELD/CRIT/CONTROL) + `SKILL_ELEMENT_IDENTITY` 5-entry + 11 signature skill + DOT 3-turn/SHIELD floor 10% HP combat dispatch + FE SkillTagBadge + filter "Loại pháp". |
| 14.2.D | Elemental Dungeon and Boss Identity | **CLOSED** ✅ | PR #491 merged. `DungeonDef.dominantElement/recommendedCounterElement/rewardElementHint` + `BossDef.weaknessElement/resistElements/rewardElementHint` (UI hint, KHÔNG đọc combat) + `computePlayerElementWarning` matrix + FE ElementIdentityPanel + BossElementTooltip. |
| 14.3.C | Tribulation Support Item Consumption | **CLOSED** ✅ | PR #488 merged. `POST /character/tribulation` body `{selectedSupportItemKeys[]}` (≤3) atomic consume in tx → ledger `TRIBULATION_SUPPORT_CONSUME` `refType=TribulationAttemptLog` + server recalc bonus 4 nguồn + cả success/fail consume (no free retry) + idempotent rollback. |
| 14.3.D | Tribulation Encounter System | **CLOSED** ✅ | PR #489 merged. 5-entry encounter catalog (Hỏa BURST / Thủy SUSTAIN / Mộc POISON_RECOVERY / Kim ARMOR_CRIT / Thổ DEFENSE_ENDURANCE) + 2-phase API `start → resolve` + `TribulationEncounter` model state machine `pending → resolved` + double-resolve trả cached outcome (KHÔNG double breakthrough/consume/reward). Migration `20260611000000_phase_14_3_d_tribulation_encounter`. |
| 14.3.E.1 | Tribulation Mini-Battle Backend | **CLOSED** ✅(this PR) | Backend mini-battle với state machine PENDING/ACTIVE/RESOLVED/FAILED/EXPIRED + 5 effectType (BURST burst+crit / SUSTAIN heal / POISON_RECOVERY DOT+CLEANSE / ARMOR_CRIT pierce / DEFENSE_ENDURANCE shield+heal) + 4 endpoint `/character/tribulation/battle/{current,start,action,resolve}` + idempotency (`lastClientNonce` dedupe + `resultJson.attemptLogId` pointer reconstruct outcome) + race-safety (optimistic `state+currentPhase` guard) + feature flag `TRIBULATION_MINI_BATTLE_ENABLED` default OFF (legacy 14.3.D vẫn chạy). Migration `20260615000000_phase_14_3_e_1_tribulation_mini_battle`. KHÔNG đụng FE (defer 14.3.E.2). |
| 12.10.C | NPC Shop + Hidden Unlocks | **CLOSED** ✅ | PR #492 merged. 15-entry shop catalog 5 NPC × tier-gated + atomic buy + per-NPC daily-limit ≤30 + `NpcAffinityPanel` shop tab + hidden dialogue/quest unlocks tier-aware. Ledger `NPC_SHOP_BUY`. |
| 12.10.D | NPC Relationship Quest Chains | **CLOSED** ✅ | PR #493 merged. 3-chain catalog (sect-path/truce/lineage) + `claimChain` JSON-path CAS race-safe + tier gate + reward atomic + storyFlags merge + `NpcAffinityPanel` quest-chain tab + `QuestView` "Duyên phận" badge. KHÔNG migration. |
| 13+ | Real-time PvP / pet gacha / voice / streaming | **Not started** | Per [`LONG_TERM_ROADMAP.md`](./LONG_TERM_ROADMAP.md) §0 — explicitly DO NOT build yet. |

**Smoke coverage**: 28 module (~15 step avg, +`smoke:quest` 20 step PR-3, +`smoke:npc` 11 step PR-4, +`smoke:dungeon-run` 16 step PR #434). 8 module có cả negative + positive path: skill / shop / mail / inventory / spiritual-root / breakthrough / auth / cultivation-method. Defer: daily-login multi-day positive (cần admin advance-day). Full per-module step count + endpoint coverage → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Smoke Detail Migrated 2026-05-05.

---

## 4. Known Issues / Risks

### Live (Open) — cần action

| # | Severity | Issue | Status / Plan |
|---|---|---|---|
| M7 | Medium | CSP production-ready nhưng chưa test deploy với CDN/asset domain khác. | **Open** — khi deploy production cần review `script-src`, `connect-src`. |
| QA-004 | Medium | `AdminControlCenterView.vue` `onMounted` guard chạy trước khi Pinia auth store hydrate từ `/api/auth/me` → admin reload trực tiếp `/admin/control-center` bị toạt "Chỉ admin được vào trang này" + redirect `/home`. SPA nav qua sidebar vẫn ok. Server-side `AdminGuard` vẫn bảo vệ `/api/admin/**` nên **không leak payload** — chỉ là UX bug. | **Open** — micro-PR follow-up: `await authStore.ensureUserLoaded()` trong `onMounted` hoặc chuyển role-check sang Vue router `beforeEnter`. Phát hiện qua QA PR #562. |
| QA-003 | Low | Smoke scripts (`scripts/smoke-*.mjs`) share host IP `127.0.0.1` → chạy nhiều script liên tiếp luũy cộng `AUTH_REGISTER 5/IP/15min` rất nhanh. Per-script `redis FLUSHDB` workaround. | **Open** — add `pnpm smoke:flush-rate-limits` helper. Phát hiện qua QA PR #562. |

### Implementation notes / debt (open) — không phải bug, là defer

- **NPC Gift daily bucket** (Phase 12.10.B / PR #484): hiện reset theo UTC 00:00 (`YYYY-MM-DD` slice từ ISO timestamp). Cân nhắc chuyển sang local-day (Asia/Ho_Chi_Minh ICT) parity với mission/daily-login Phase 12.10.C nếu player phản hồi reset 07:00 ICT bất tiện.
- **Tribulation encounter resolve = RNG snapshot (no realtime mini-battle)** (Phase 14.3.D / PR #489): `POST /character/tribulation/encounter/resolve` hiện chỉ resolve qua `runAttemptInTx` snapshot RNG, KHÔNG có turn-by-turn mini-battle mechanic theo `effectType` (BURST/SUSTAIN/POISON_RECOVERY/ARMOR_CRIT/DEFENSE_ENDURANCE). UI panel render advantage matrix nhưng gameplay loop không đọc — defer Phase 14.3.E (mini-battle phase loop + animation/event log).
- **Territory automation chưa có cron** (Phase 14.0.B/C/D / PR #481/#486/#487): admin endpoint `POST /admin/territory/settle` + `/admin/territory/decay` + `/admin/territory/war/settle-current` đều idempotent UNIQUE race-safe NHƯNG phải bấm tay. Cron weekly auto-trigger + reward mail chưa có — defer Phase 13.2.D + 14.0.E.
- **Quest claim affinity reward idempotency** (Phase 12.10.B): apply qua existing `QuestProgress.claim()` CAS guard `updateMany({status: COMPLETED → CLAIMED, claimedAt})` — tested OK. Nếu sau này thêm un-claim hoặc retry path mới, cần verify lại affinity KHÔNG double-grant.
- **NPC relationship chain reward idempotency** (Phase 12.10.D / PR #493): qua JSON-path CAS raw SQL `IS DISTINCT FROM '1'` race-safe + 409 `CHAIN_ALREADY_CLAIMED` retry. Test parallel verify exactly 1 winner. Nếu thêm un-claim hoặc admin grant path, cần verify lại storyFlags JSON-path không bị overwritten.

### Resolved (recent — full list ở ARCHIVE)

- Phase 26.0 local runtime gap → Resolved this PR (26.0.1): Postgres/Redis migration + API smoke completed; added service/processor/UI regression tests; body tick now uses its own queue; breakthrough update now CAS-guarded with rollback on concurrent attempts.
- Phase 23.2 equipment realm gate → Resolved PR #540 draft: `packages/shared/src/equipment-progression.ts` defines 10 tiers, grade mapping, power budgets, socket/enhance/gem/set caps; inventory equip rejects realm-locked equipment; unequip/consumable unaffected.
- M10 Shop daily limit + per-user rate limit → Resolved PR #450 (`ShopEntryDef.dailyLimit` opt-in + `rl:shop-buy` 30 req/60s qua FailoverRateLimiter; pre-check trước tx, KHÔNG trừ tiền khi reject; +20 vitest).
- Inventory `use()` JS-capture race → Resolved PR #422 (atomic `updateMany` + post-decrement delete + INVENTORY_ITEM_NOT_FOUND translation + 5 regression test).
- Phase 12.2.A daily limit enforcement → Resolved PR #421.
- M9 Settings logout-all `passwordVersion` → Resolved PR #154/#155 (intentional trade-off documented `docs/SECURITY.md §1`).
- M11 `GET /character/profile/:id` rate-limit → Resolved PR #62 (`PROFILE_RATE_LIMITER` 120 req/IP/15min).
- M8 Admin guard MOD broad quyền → Resolved PR E (`@RequireAdmin()` decorator + reflector AdminGuard).
- M6 LogsModule chưa build → Resolved PR #88 BE + #91 FE (`/logs/me` keyset pagination + ActivityView.vue + 24 ledger reason i18n).

> **Full historical issues** (~50 entries Critical/High/Medium/Low + Resolved) ở [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Section 16 Known Issues / Risks.

---

## 5. Tests / CI / Smoke / E2E

### Vitest baseline (post this PR)

| Workspace | Test count | Notes |
|---|---|---|
| `apps/api` | **3736+** | Phase 27.1–27.5 adds 16 `monetization-systems.service.test.ts` (BattlePassV2 addProgress idempotent per period bucket, LimitedShop purchase CAS, GrowthFund tien claim gate, MonetizationOverview snapshot aggregator). Phase 27.0 added 15 `monetization-foundation.service.test.ts` (wallet, shop, entitlement, sweep, extra-attempt, growth fund). Phase 26.3 19 `cultivation-method-v2.service.test.ts`. Earlier 14/12.10 + combat/dungeon/boss/sect suites remain. Redis-dependent tests require Redis up. |
| `packages/shared` | **3444** | Phase 27.1–27.5 adds 21 `monetization-systems.test.ts` (mission uniqueness, exp cap, growth fund realmOrder monotonic, shop period mapping, forbidden item, sweep ticket keys, progress helpers). Phase 27.0 added catalog invariant for foundation. Phase 26.3 47 `cultivation-methods-v2.test.ts`. Earlier Phase 23.2 equipment progression, Phase 14 arena/combat/territory/tribulation/elemental, NPC tests. |
| `apps/web` | **2057** | Phase 27.1–27.5 adds 5 `MonetizationDacQuyenView.test.ts` (7-tab render / limited shop disable sold-out / growth fund tien render / battle pass missions / buy flow). Phase 26.0.1 `/body-cultivation` confirm-modal submit regression. Earlier Body Cultivation render/action smoke plus Arena/Territory/Tribulation/Skill/Dungeon/Boss/NpcAffinity/Quest. |
| **Total** | **~9237 vitest** | All green local (PG+Redis up). Phase 27.1–27.5 `(this)` adds **42 test** (shared 21 + api 16 + web 5). |

### CI

- Workflow `.github/workflows/ci.yml`: `build` + `e2e-smoke` jobs (PG+Redis services). Chạy mỗi PR + push (no path filter — docs-only PR vẫn trigger CI). Yêu cầu xanh trước khi merge.
- 5 redis-dependent test (rate-limiter + health controller) cần Redis local — pass khi Redis container up.

### Smoke Scripts (manual, không nằm CI matrix)

30 smoke scripts, ~15 step avg. Yêu cầu local stack: `pnpm infra:up` + `pnpm --filter @xuantoi/api exec prisma migrate deploy` + `pnpm --filter @xuantoi/api run bootstrap` + `pnpm --filter @xuantoi/api dev`. Module list includes achievement/admin/auth/beta/boss/breakthrough, **body-cultivation** (Phase 26.0.1 register/onboard/status/start/tick/cap/stop/breakthrough fail), combat/cultivation/dungeon/economy/ws, and other gameplay modules.

**Positive-path coverage** (8 module): skill (PR #388/#390), shop (#390), mail (#391), inventory (#385), spiritual-root (#386), breakthrough (#417), auth (#384), cultivation-method (#394). Còn defer: daily-login multi-day positive — pending admin advance-day endpoint hoặc service helper extension.

**Phase 26.0.1 manual runtime smoke**: local Postgres/Redis + API dev verified register/onboard → `GET /character/body-cultivation` default `pham_than`, `POST /start`, scheduled body tick adds EXP and decrements stamina without negative, `BODY_CULTIVATION` bucket accumulates EXP-only, `POST /stop`, `POST /breakthrough` insufficient EXP returns `INSUFFICIENT_EXP`, and currency ledger remains untouched (`linhThach=0`, `tienNgoc=0`).

**Per-module detail table** (step count, negative/positive flag, notes) → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Smoke Detail Migrated 2026-05-05.

### E2E (Playwright)

- `apps/web/e2e/golden.spec.ts` — 22 spec golden path (auth smoke + 21 full-stack gated `E2E_FULL=1`). Notable: #19 talent learn→cast→cooldown badge (`golden.spec.ts:759-895`), #20 breakthrough attempt → outcome banner + history reload-persist (`golden.spec.ts:898-981`), #21 Phase 12 Story PR-5 main storyline Chapter 1 playable (`golden.spec.ts:1031-1125`), **#22 Phase 12.3 DungeonRun flow** (`golden.spec.ts:1162-end` — son_coc start→next×3→claim, verify per-encounter loot span + reward delta).
- CI job `e2e-smoke`: chạy spec #1 AuthView smoke mỗi PR (matrix postgres+redis, build api+web, `E2E_SMOKE=1`).
- `E2E_FULL=1` gate cho 19 full-stack spec **chưa wire CI** — runtime manual với `pnpm infra:up` + `pnpm --filter @xuantoi/api dev` + `pnpm --filter @xuantoi/web dev`.

---

## 6. Recommended Next Roadmap

Per [`AI_WORKFLOW_RULES.md`](./AI_WORKFLOW_RULES.md) §SESSION PR LIMIT (1-3 PR/session) + §GOM TRƯỚC KHI TÁCH 4b. Ưu tiên Medium PR > Hotfix > Large.

### Top priority — next session

0. **Phase 26.1 — Alchemy V2 + PillGrade + Body Pill Expansion**: now that Phase 26.0 runtime is verified, expand body pill crafting/drop loops without rewriting core Luyện Thể.

1. ~~**Content Scale 2 — High-Realm Skills Pack**~~ — **DONE** (PR #509). ~~**Equipment Reforge / Enchant Foundation**~~ — **DONE** (#511). ~~**Phase 15.1–15.2 LiveOps Event Scheduler**~~ — **DONE**. ~~**Phase 15.3.A LiveOps Runtime Expansion + Festival Gift**~~ — **DONE** (PR #512). ~~**Phase 15.3.B LiveOps Announcement + WS Marquee**~~ — **DONE** (PR #513). ~~**Phase 15.4 — Feature Flag DB-backed**~~ — **DONE** (PR #514). ~~**Phase 15.5 — Maintenance Window**~~ — **DONE** (PR #515). ~~**Phase 15.6 — Config Version + Rollback**~~ — **DONE** (PR #516). ~~**TZ Hotfix — sectWarWeekKey + Territory periodKey TZ-aware unify**~~ — **DONE** (PR #517). ~~**Phase 15.7 — Sect Season + Territory Auto-Cron + Champion/MVP Reward Distribution**~~ — **DONE** trong PR này (#518): cron auto-run weekly cycle (Mon 00:05 ICT territory + daily 00:15 ICT sect season) qua BullMQ repeat job, 2 lớp idempotency Redis lease + DB UNIQUE, Champion mail cap 100/sect + MVP mail top-1, admin manual fallback giữ lại, status read endpoint, FE LiveOps panel hiện reward count, i18n VI/EN parity. Tiếp theo nên là **Phase 15.8 — LiveOps Maintenance Polish Bundle** (Medium PR): metric alarms cho cron health (last-run staleness > 1 week → alert), Champion membership snapshot table (audit-perfect rule — member rời sect SAU snapshot vẫn nhận reward nếu policy đổi), cron grafana dashboard, FE Hall of Fame view cho admin xem top sect & MVP history qua các season. HOẶC **Phase 16.1.B — Ledger Checker Daily Cron + Economy Report**: extend `LedgerCheckerService` lên cron daily + economy report mail cho admin tổng kết source/sink/anomaly week-over-week. ~~**Phase 16.1.B**~~ — **DONE** (PR #520). ~~**Phase 18.1 — Security Rate Limit + Abuse Protection**~~ — **DONE** trong PR này (#521). Sau Phase 18.1 nên là **Phase 17.1 + 17.2 — Deploy Verify + Backup/Restore Weekly** (Medium PR): smoke check deployment readiness (Helmet CSP, secret leak guard, env validation, admin bootstrap, healthcheck on every instance) + weekly cron `pg_dump` upload offsite + verify restore staging mỗi tuần. HOẶC **Phase 18.2 — Security Session Management Hardening**: rotate-on-use refresh token, session list/revoke admin endpoint, suspicious-session WS notify user, geo-distance check khi login multiple country trong 5 phút. KHÔNG làm gacha / battle pass / real-time PvP.

2. ~~**Phase 13.2.D + 14.0.F Season/Territory Automation Cron**~~ — **DONE** (gộp vào PR #518 Phase 15.7 cùng champion/MVP reward).

3. ~~**Phase 14.3.E.1 Tribulation Mini-Battle Backend**~~ — **DONE** (PR #503). ~~**Phase 14.3.E.2 Tribulation Mini-Battle Frontend**~~ — **DONE** (PR #504). ~~**Phase 14.1.A Combat Determinism Audit for Arena**~~ — **DONE** (PR #505). ~~**Phase 14.1.B Async Arena Foundation**~~ — **DONE** (PR #506). ~~**Phase 14.1.C Arena Season + ELO + Reward**~~ — **DONE** (PR #507). ~~**Phase 14.1.D Arena Anti-Wintrade Detection**~~ — **DONE** trong PR này (#508). Arena profile + opponent matching + deterministic match resolution (sync trong POST request, dùng `resolveCombatWithSnapshot`) + match history + ArenaView UI. **Known limitations**: chưa có season reward lớn, chưa có anti-wintrade đầy đủ (chỉ no-self-attack + daily limit), chưa có end-season mail reward, chưa có ELO progression curves. **Next priority** — **Phase 14.1.C Arena Season + ELO + Reward** (Medium PR): season cycle (weekly/monthly snapshot rank), ELO update curve thay rating delta cố định, end-season mail reward + Hall of Fame, anti-wintrade (cooldown giữa cùng cặp + IP/device fingerprint + min level/realm gate), defense AI snapshot khi player offline. Sau đó: Phase 14.1.D Arena Leaderboard + Spectate. Defer: realtime PvP, cross-server, party arena.

3. ~~**Phase 14.0.E Territory Reward Mail**~~ — **DONE** (PR #495). Cron trigger còn lại tách qua roadmap #1 (Phase 13.2.D + 14.0.F Season/Territory Automation Cron).

4. **Phase 12.10.E NPC Romance / Marriage Path** (Out of scope hiện tại — defer Phase 16) — backlog.

### Phase 12 Story chain status

Design source ở [`docs/story/TU_TIEN_LO_STORY_BIBLE.md`](./story/TU_TIEN_LO_STORY_BIBLE.md) + [`docs/story/PHASE12_STORY_PROGRESS.md`](./story/PHASE12_STORY_PROGRESS.md). 5-PR core roadmap + Foundation Late-game wire + encounter wire + discoverability hint **all CLOSED** ✅:

- **PR-1** → **PR-6** — Catalog foundation, Quest runtime, Claim, NPC dialogue UI, Chapter 1 playable, Combat kill hook. **CLOSED** ✅ (#425–#432).
- **Foundation Late-game wire** — 8 monster catalog (`tich_linh_anh`, `tam_ma_anh`, `tich_linh_quy`, `tich_thien_sat_thu`, `tam_ma_nguyen_anh`, `chap_niem_anh`, `ky_uc_meo`, `huyet_anh`). **CLOSED** ✅ (#433).
- **Foundation Late-game encounter wire** — wire 8 placeholder vào 4 dungeon `monsters[]`. **CLOSED** ✅ (#439).
- **QuestView dungeon hint** — close discoverability gap. **CLOSED** ✅ (#440).
- **Phase 12.5 dungeon balance tuning** — stat HP/ATK/DEF/SPD/level + monsterType promotion (3 ELITE/BOSS) + 3 lootTable override + 11 invariant test. **CLOSED** ✅ (#442).

Next nâng cao Phase 12 Story (optional, nếu cần): Phase 12.10 Story dialogue smoke + Playwright E2E để khoá branching coverage cuối; HOẶC mở rộng Hàn Dạ tree thành multi-arc reward (give_reward gắn theo path); HOẶC thêm dialogue branch cho 8 placeholder kill milestone (Phase 12 Foundation Late-game). Sau mỗi PR Phase 12 Story merged, AI **bắt buộc** cập nhật `docs/story/PHASE12_STORY_PROGRESS.md` trong cùng PR (DOCS UPDATE RULE).

### Backlog / Anti-feature-creep

- **Doc compaction maintenance**: khi Recent Changes vượt 10 entry → đẩy entry cũ nhất xuống ARCHIVE § Recent Changes Legacy. Khi `AI_HANDOFF_REPORT.md` vượt **250 dòng** → compact ngay theo HANDOFF REPORT STRUCTURE RULE.
- **Concurrency tests phase 2 (low priority remainder)**: Boss spawn cron auto race.
- **Do not build yet**: Real-time PvP, party/co-op dungeon mở rộng ngoài scope, pet/wife gacha, voice chat, video streaming.
