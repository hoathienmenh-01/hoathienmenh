# AI Handoff Report — Xuân Tôi

> 👉 **AI/dev mới: ĐỌC [`docs/START_HERE.md`](./START_HERE.md) TRƯỚC.** File đó định tuyến tới đúng doc theo task type (state / vision / roadmap / economy / content / balance / live ops / story).
>
> **Cấu trúc**: file này chỉ chứa **trạng thái live hiện tại** (Executive Summary + Recent Changes + Phase Status + Known Issues + Tests/CI/Smoke + Next Roadmap). **Toàn bộ lịch sử PR cũ** (Snapshots PR #33→#396, Recent Changes Legacy, Completed Features, Project Reference đầy đủ Tech Stack / Architecture / DB / Gameplay / Run Locally / Rules, Old Recommended Next Roadmap, Exact PR Plan, smoke detail table per module) tách ra [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md). **AI mới CHỈ cần đọc file này** — `ARCHIVE_HANDOFF.md` chỉ tra cứu khi cần.
>
> **Cap**: ≤ 250 dòng (HANDOFF REPORT STRUCTURE RULE — xem [`AI_WORKFLOW_RULES.md`](./AI_WORKFLOW_RULES.md)). Vượt cap → bắt buộc compact + đẩy entry cũ xuống ARCHIVE.

---

## 1. Current Executive Summary

- **Current `main` commit**: post PR #469 merged (Phase 12.8.E Story Dungeon Smoke Coverage) + PR #468 (Phase 12.8.D Story Dungeon UI Test Coverage) + PR #467 (Phase 12.8.C Story Dungeon FE + Dialogue/Reward Polish) + PR #466 (Phase 12.8.B Story Dungeon Runtime + Quest Integration) + PR #465 (Phase 12.8.A Story Dungeon Catalog + API Foundation) + PR #464 (Phase 12 Story Dialogue Foundation) + PR #463 (Phase 13.1.C Admin LiveOps Advanced Controls) + PR #462 (daily-login extended coverage) + PR #461 (docs sync handoff after Phase 13.1 sect systems) + PR #460 (daily-login multi-day smoke positive) + PR #459 (Phase 13.1.B Sect Missions + Sect Shop + Admin LiveOps Controls) + Phase 13.0 LiveOps & Retention Suite. Story design source live tại [`docs/story/`](./story/).
- **Current phase**: Phase 10 **CLOSED** ✅. Phase 11 Progression Depth **COMPLETE** ✅. Phase 11 nâng cao **CLOSED** ✅. Phase 12 World Map & Dungeon **CLOSED** ✅ (#397–#448). Phase 13.0 LiveOps & Retention Suite **CLOSED** ✅ (post #456 audit pass #5). **Phase 13.1.A/B/C Sect War / Missions / Shop / Admin LiveOps** **CLOSED** ✅ (PR #457–#463). **Daily-login multi-day smoke positive + extended coverage** **CLOSED** ✅ (PR #460 + PR #462 merged). **Phase 12 Story Dialogue Foundation (PR-7)** **CLOSED** ✅ (PR #464). **Phase 12.8.A Story Dungeon Catalog + API Foundation** **CLOSED** ✅ (PR #465). **Phase 12.8.B Story Dungeon Runtime + Quest Integration** **CLOSED** ✅ (PR #466). **Phase 12.8.C Story Dungeon FE + Dialogue/Reward Polish** **CLOSED** ✅ (PR #467). **Phase 12.8.D Story Dungeon UI Test Coverage** **CLOSED** ✅ (PR #468). **Phase 12 Story Dungeon Instance Suite COMPLETED** ✅ (12.8.A + 12.8.B + 12.8.C) + UI test coverage (12.8.D). **Phase 12.8.E Story Dungeon Smoke Coverage** **IN-FLIGHT** (this PR — `scripts/smoke-story-dungeon.mjs` 35-step Node smoke + `pnpm smoke:story-dungeon` wire + verification + docs sync).
- **In-flight**: this PR — **Phase 13.1.D Admin LiveOps Schedule Preview & Dry-run**. Append-only Phase 13.1.D dưới Phase 13.1.C: 2 endpoint admin mới (`GET /admin/liveops/schedule-preview` ADMIN/MOD read-only no-audit + `POST /admin/liveops/dry-run` ADMIN-only `@RequireAdmin()` audit `ADMIN_LIVEOPS_DRY_RUN`) + 2 panel FE mới (`AdminLiveOpsSchedulePreviewPanel.vue` + `AdminLiveOpsDryRunPanel.vue`) wire vào AdminView LiveOps tab. **KHÔNG mutate**: dry-run KHÔNG grant reward, KHÔNG spawn `WorldBoss`, KHÔNG upsert `LiveOpsEventOverride` — chỉ ghi 1 audit row. Schedule preview overlay `LiveOpsEventOverride` overrides → `effectiveEnabled`; trả `activeEvents`/`upcomingEvents` (top N slot/event 7d) + `bossScheduleToday`/`bossScheduleWeek` (group `localDate`, status `upcoming`/`active`/`completed`) + sect war season/status + full overrides[]. Dry-run event reuse `LIVE_OPS_EVENTS` catalog + override DB row, dry-run boss reuse `bossByKey` + level clamp 1..99 + `simulatedReward { baseLinhThach, topDropPool[], midDropPool[], lowDropPool[] }`. i18n vi/en parity (`adminLiveOpsPreview.*` + `adminLiveOpsDryRun.*`). Tests +9 api (3 schedulePreview + 6 dryRun event/boss với clamp + EVENT_NOT_FOUND/BOSS_NOT_FOUND audit no-mutation verify) + 8 web (4 panel render + 4 dry-run submit/reject/error). Verification: api typecheck ✅ / api `--run admin` 344 PASS ✅ / api `--run liveops` 32 PASS ✅ / web typecheck ✅ / web `--run AdminLiveOps` 18 PASS ✅ / web `--run Admin` 82 PASS ✅ / pnpm build ✅. KHÔNG migration / KHÔNG đụng `/admin/liveops` legacy / KHÔNG đụng force-spawn boss Phase 13.1.C / KHÔNG sect war runtime.

- **Phase 12.8.E (PR #469)**: test-only PR — `scripts/smoke-story-dungeon.mjs` 35-step smoke + `pnpm smoke:story-dungeon` wire. Bugs found 0. KHÔNG runtime change. Thêm `scripts/smoke-story-dungeon.mjs` 35-step Node smoke chạy real HTTP vs API local cover full player flow: auth gates (5 endpoint × no-auth → 401), register + onboard fresh char, GET list pre-onboard 404 NO_CHARACTER, target locked → start 403 DUNGEON_LOCKED, drive `phamnhan_main_01` to COMPLETED via real /quests/accept + /quests/progress (talk×2) + admin /quests-track (kill son_thu ×3), accept `phamnhan_realm_01` + progress step_01 explore, target now status=available, start ACTIVE run idempotent, premature clear 409 RUN_STEP_INVALID, advance×3, advance out-of-range 409, clear → CLEARED, re-clear 409 RUN_NOT_ACTIVE, GET /quests/me verify quest progress no-regression, claim → granted reward (80 LT + 150 EXP + 1 linh_lo_dan), double-claim 409 RUN_ALREADY_CLAIMED, oneTime re-start 409 DUNGEON_ALREADY_CLEARED, register player2 → advance/claim non-owner 403 RUN_NOT_OWNED, player2 list → activeRun=null target locked (data isolation). Wire `pnpm smoke:story-dungeon` trong root `package.json`. **Bugs found**: 0 (35/35 step PASS local). KHÔNG đụng runtime/migration/component code/i18n — chỉ thêm 1 smoke script + 1 package.json line. Verification: api typecheck ✅ / api `--run story` 59 PASS / api `--run dungeon` 82 PASS / web typecheck ✅ / web `--run StoryDungeon` 74 PASS / web `--run Quest` 36 PASS / pnpm build ✅ / `pnpm smoke:story-dungeon` 35/35 PASS local.
- **Phase 12.8.C (PR #467)**: biến Story Dungeon từ backend runtime (12.8.B) thành playable FE experience. Web API client `apps/web/src/api/storyDungeon.ts` (6 axios function envelope-based: `fetchStoryDungeonList`/`fetchStoryDungeon`/`startStoryDungeon`/`advanceStoryDungeon`/`clearStoryDungeon`/`claimStoryDungeon`). Pinia store `apps/web/src/stores/storyDungeon.ts` (state `dungeons[]/activeRun?/loaded/loading/lastError/submittingKey/submittingError/lastClaimResult` + computed `totalCount/availableCount/lockedCount/clearedCount/hasActiveRun/isRunCleared/isRunClaimable/isRunActive/hasAnyAvailable` + actions `load/start/advance/clear/claim/findDungeon/findDungeonForQuest`). UI Components (4 file mới): `StoryDungeonView.vue` (main list view với header counter + filter + dungeon cards + active run panel inline + dialogue/reward modal mounted on demand); `StoryDungeonRunPanel.vue` (active run với title + step progress + current monster + killed monsters list + advance/clear/claim button); `StoryDungeonDialoguePanel.vue` (light Teleport modal + Esc/backdrop close — read-only NPC dialogue trước/sau dungeon, KHÔNG branching); `StoryDungeonRewardModal.vue` (reward grant display sau claim: linhThach/tienNgoc/exp/items với item name lookup). Integration: `QuestView.vue` CTA "Vào bí cảnh cốt truyện" khi quest có available story dungeon + `HomeView.vue` CTA card khi `hasAnyAvailable` || `hasActiveRun` + `AppShell.vue` nav link `/story-dungeons` + router lazy-load route. Light backend polish: controller `getStoryDungeonList` enrich `activeRun?: StoryDungeonRunView` + service `findActiveRunForCharacter` helper — KHÔNG sửa runtime/migration. i18n vi/en parity 90+ key. Tests +16 (`stores/__tests__/storyDungeon.test.ts` store actions). Verification: web typecheck ✅ + `--run StoryDungeon` 16 PASS / `--run Quest` 29 PASS / web full **1299** PASS / api typecheck ✅ / api `--run story` 59 PASS / pnpm build ✅.
- **Phase 12.8.B (PR #466)**: biến Story Dungeon từ catalog read-only (12.8.A) thành playable runtime. Prisma migration `20260603000000_phase_12_8_b_story_dungeon_runtime` thêm enum `StoryDungeonRunStatus { ACTIVE, CLEARED, CLAIMED, FAILED }` + table `StoryDungeonRun` + 3 index. 4 endpoint mới `POST /story/dungeons/:key/start` (idempotent + reject `DUNGEON_LOCKED` + `DUNGEON_ALREADY_CLEARED`) + `POST /story/dungeons/:runId/advance` (ownership + `RUN_NOT_ACTIVE` + `RUN_STEP_INVALID`) + `POST /story/dungeons/:runId/clear` (CAS guard ACTIVE→CLEARED + clear hook `applyQuestStepAdvance` fail-soft) + `POST /story/dungeons/:runId/claim` (CAS guard CLEARED→CLAIMED race-safe + reward grant atomic qua `CurrencyService.applyTx` + `InventoryService.grantTx` reason `STORY_DUNGEON_REWARD`). Quest integration fail-soft: retry clear KHÔNG double progress. Tests +15 cover 15 spec case. KHÔNG sửa `DungeonRun` flow.
- **Phase 12.8.A (PR #465)**: shared catalog `STORY_DUNGEONS` + 4 catalog seed gắn 4 main quest đầu tiên + 2 endpoint read-only `GET /story/dungeons` + `GET /story/dungeons/:key`.
- **Test baseline (post this PR)**: web full **1372** PASS ✅ (+73 case từ Phase 12.8.D: 13 StoryDungeonView + 23 StoryDungeonRunPanel + 11 StoryDungeonDialoguePanel + 11 StoryDungeonRewardModal + 7 QuestView CTA extension + 8 HomeView CTA extension). api full **2188** PASS ✅ (kept from PR #466/#467). Shared 1504 PASS ✅.
- **Top priority next session**: (1) **Phase 12 Story Dialogue Branch advanced** — multi-step branching tree + flag effects + give_reward expansion; (2) **Phase 13.2 Cross-sect seasonal expansion** — multi-sect tournament + cross-server leaderboard; (3) CSP production verify khi deploy (M7); (4) Admin LiveOps advanced controls nếu còn thiếu (schedule editor / event preview / dry-run mode). Detail §6.
- **Open Critical/High issues**: none. Live medium issues: M7 (CSP production verify khi deploy). Detail §4.
- **Blocker**: none.

---

## 2. Recent Changes

10 PR gần nhất merged trên main (newest đầu, mỗi entry 1 dòng). PR cũ hơn → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Snapshots.

| PR | Title | Type |
|---|---|---|
| this PR | `test(e2e): add Story Dungeon smoke coverage. Phase 12.8.E test-only PR, no runtime change. Thêm `scripts/smoke-story-dungeon.mjs` 35-step Node smoke chạy real HTTP vs API local cover full player flow Story Dungeon: auth gates 6 step (5 endpoint × no-auth → 401 ignoring leading runId-routed advance/clear/claim auth gates), register player1 + GET /story/dungeons pre-onboard 404 NO_CHARACTER, onboard player1 phamnhan default sect, GET /story/dungeons post-onboard → list với target locked, POST /story/dungeons/:key/start pre-quest → 403 DUNGEON_LOCKED, drive `phamnhan_main_01` to COMPLETED via /quests/accept + /quests/progress (talk×2 step_01/step_02) + admin login + admin /api/admin/users/:id/quest-track (kill son_thu ×3 step_03), GET /quests/me → phamnhan_main_01 COMPLETED, accept `phamnhan_realm_01` + progress step_01 explore, GET /story/dungeons → target now status=available, POST /story/dungeons/story_dgn_phamnhan_back_mountain/start → 200 ACTIVE, idempotent retry → same runId, premature clear 409 RUN_STEP_INVALID, advance×3 → currentStep=3, advance out-of-range 409 RUN_STEP_INVALID, clear → 200 CLEARED, re-clear 409 RUN_NOT_ACTIVE, GET /quests/me → phamnhan_realm_01 ACCEPTED step_01 progress=1 (no regression), claim → 200 granted reward (80 LT + 150 EXP + 1 linh_lo_dan), double-claim 409 RUN_ALREADY_CLAIMED, oneTime re-start 409 DUNGEON_ALREADY_CLEARED, register player2 onboard → POST /story/dungeons/:runId/advance non-owner 403 RUN_NOT_OWNED + claim non-owner 403 RUN_NOT_OWNED, player2 GET /story/dungeons → activeRun=null target locked (data isolation). Wire `pnpm smoke:story-dungeon` trong root `package.json`. Env vars `SMOKE_API_BASE` (default http://localhost:3000), `SMOKE_TIMEOUT_MS`, `SMOKE_VERBOSE`, `SMOKE_SECT_KEY`, `SMOKE_ADMIN_EMAIL`, `SMOKE_ADMIN_PASSWORD`. Yêu cầu local: `pnpm infra:up` + `pnpm --filter @xuantoi/api exec prisma migrate deploy` + `pnpm --filter @xuantoi/api bootstrap` + `pnpm --filter @xuantoi/api dev`. **Bugs found**: 0 (35/35 step PASS local). KHÔNG đụng runtime/migration/component code/i18n — chỉ thêm 1 smoke script + 1 package.json line. Verification: api typecheck ✅ / api `--run story` 59 PASS ✅ / api `--run dungeon` 82 PASS ✅ / web typecheck ✅ / web `--run StoryDungeon` 74 PASS ✅ / web `--run Quest` 36 PASS ✅ / pnpm build ✅ / `pnpm smoke:story-dungeon` 35/35 PASS local. KHÔNG migration / KHÔNG runtime / KHÔNG dialogue branch advanced / KHÔNG dungeon mới / KHÔNG Phase 13.2.` | test |
| #468 | `test(web): Phase 12.8.D story dungeon UI test coverage. Test-only PR, no runtime change. Filled §F web test coverage gap left after PR #467 (Phase 12.8.C Story Dungeon FE merged on main): 4 component test file mới (`apps/web/src/views/__tests__/StoryDungeonView.test.ts` 13 case render locked/available/cleared status badges + start button calls API + run panel advance + claim flow + reward modal + API error fallback không crash; `apps/web/src/components/__tests__/StoryDungeonRunPanel.test.ts` 23 case render title/progress/monster/boss/killed/reward/realm + advance/clear/claim button state logic + emit verification + dialogue button emits open-dialogue + disabled state based on submittingKey; `apps/web/src/components/__tests__/StoryDungeonDialoguePanel.test.ts` 11 case render khi nodeId set + Esc/backdrop/button close emits + render NPC name + dialogue text từ catalog + locale switching; `apps/web/src/components/__tests__/StoryDungeonRewardModal.test.ts` 11 case render reward chips conditional khi >0 + linhThach/tienNgoc/exp/items chips + close emits + item fallback to raw key) + extend `apps/web/src/views/__tests__/QuestView.test.ts` +7 case Story Dungeon CTA (quest ACCEPTED+match → render CTA, AVAILABLE+match → render, LOCKED+match → hide, CLAIMED+match → hide, no-match → hide, store.load fail-soft no-crash, click CTA → router.push('/story-dungeons')) + extend `apps/web/src/views/__tests__/HomeView.test.ts` +8 case Home CTA (loaded=true+hasAnyAvailable → render với count, loaded=true+hasActiveRun → render desc active, loaded=false → hide, both false → hide, no-character → hide, click CTA → router.push, store.load throw → fail-soft, onMounted gọi store.load). KHÔNG đụng runtime/migration/component code/i18n — chỉ thêm 4 test file + extend 2 view test. Mock pattern: @/api/storyDungeon + @/stores/auth/game/toast/storyDungeon + vue-router; Pinia store integration; factory functions buildDungeon/buildRun/buildTemplate; Teleport-to-body via attachTo: document.body; i18n setup matching component keys. Verification: shared typecheck/test 1504 PASS ✅ / web typecheck ✅ / web `--run StoryDungeon` 74 PASS ✅ / web `--run Quest` 36 PASS ✅ / web full **1372** PASS ✅ (+73 case) / api typecheck ✅ / api `--run story` 59 PASS ✅ / api `--run dungeon` 82 PASS ✅ / api `--run quest` 69 PASS ✅ / api full 2188 PASS ✅ / pnpm build ✅. KHÔNG migration / KHÔNG runtime / KHÔNG dialogue branch / KHÔNG dungeon mới / KHÔNG Phase 13.2.` | test |
| #467 | `feat(web): Phase 12.8.C story dungeon UI and reward polish. Biến Story Dungeon từ backend runtime (12.8.B) thành playable FE experience. Web API client `apps/web/src/api/storyDungeon.ts` (6 axios function envelope-based: `fetchStoryDungeonList`/`fetchStoryDungeon`/`startStoryDungeon`/`advanceStoryDungeon`/`clearStoryDungeon`/`claimStoryDungeon`). Pinia store `apps/web/src/stores/storyDungeon.ts` (state `dungeons[]/activeRun?/loaded/loading/lastError/submittingKey/submittingError/lastClaimResult` + computed `totalCount/availableCount/lockedCount/clearedCount/hasActiveRun/isRunCleared/isRunClaimable/isRunActive/hasAnyAvailable` + actions `load/start/advance/clear/claim/findDungeon/findDungeonForQuest`). UI Components 4 file mới: `StoryDungeonView.vue` (main list view với header counter + filter all/available/locked/cleared + dungeon cards với region/realm/required quest/monsters/boss/reward hint + start/resume button + active run panel inline + dialogue/reward modal mounted on demand); `StoryDungeonRunPanel.vue` (active run title + step progress + current monster + killed monsters list + advance/clear/claim button + loading/error states); `StoryDungeonDialoguePanel.vue` (light Teleport modal + Esc/backdrop close — read-only NPC dialogue trước/sau dungeon, KHÔNG branching); `StoryDungeonRewardModal.vue` (reward grant display sau claim: linhThach/tienNgoc/exp/items với item name lookup). Integration: `QuestView.vue` CTA "Vào bí cảnh cốt truyện" khi quest có available story dungeon (helper `shouldShowStoryDungeonCta(q)` + `gotoStoryDungeons()` route push) + `HomeView.vue` CTA card optional khi `hasAnyAvailable` || `hasActiveRun` + `AppShell.vue` nav link `Bí Cảnh Cốt Truyện` → `/story-dungeons` + router thêm route `/story-dungeons` lazy-load. Quest/Home `storyDungeonStore.load().catch(() => null)` fail-soft — request fail → silent (KHÔNG break Quest/Home view). Light backend polish: controller `getStoryDungeonList` enrich `activeRun?: StoryDungeonRunView` từ list response + service `findActiveRunForCharacter` helper — KHÔNG sửa runtime/migration/endpoint contract. i18n vi/en parity full: thêm `common.apiFallback.storyDungeon` + `shell.nav.storyDungeons` + `home.storyDungeon.{title,descAvailable,descActive,openBtn}` + `quest.storyDungeonCta` + 90+ key trong `storyDungeon.*` namespace (title/heading/loading/error/empty/filter/status badge/region/realm/required quest/start/advance/clear/claim button/dialogue/reward/error code mapping). Tests +16 (`stores/__tests__/storyDungeon.test.ts`): load happy path / load error sets lastError / start success → activeRun + reload list / start error sets submittingError / advance updates step / clear sets cleared status / claim returns lastClaimResult + clears activeRun / claim error preserves cleared run / findDungeon match / findDungeonForQuest filter available + cleared / reset / clearLastClaimResult. Verification: shared build ✅ / web typecheck ✅ / `--run StoryDungeon` 16 PASS ✅ / `--run Quest` 29 PASS ✅ / web test full **1299** PASS ✅ / api typecheck ✅ / api `--run story` 59 PASS ✅ / pnpm build ✅. Phase 12 Story Dungeon Instance Suite COMPLETED ✅. KHÔNG dialogue branch advanced (chỉ light modal read-only); KHÔNG cutscene/event; KHÔNG PvP/pet/gacha; KHÔNG rewrite story dungeon backend runtime; KHÔNG add new story dungeon (12.8.A catalog kept 4 entries).` | feat |
| #466 | `feat(api): Phase 12.8.B story dungeon runtime and quest integration. Biến Story Dungeon từ catalog read-only (12.8.A) thành playable runtime. **Design**: chọn **Option B (new `StoryDungeonRun` model)** thay vì reuse `DungeonRun` vì story dungeon có lifecycle khác hẳn farm dungeon (one-time + auto-advance quest step + reward differentiation `STORY_DUNGEON_REWARD`); reuse sẽ phải nhồi nhiều flag conditional vào `DungeonRun.start/claim` paths gây regression risk. Prisma migration `20260603000000_phase_12_8_b_story_dungeon_runtime`: enum `StoryDungeonRunStatus { ACTIVE, CLEARED, CLAIMED, FAILED }` + table `StoryDungeonRun` (`id`/`characterId` FK cascade/`templateKey`/`status` default ACTIVE/`currentStep` Int default 0/`killedMonsters` Json default `[]`/`startedAt`/`clearedAt?`/`claimedAt?`/`updatedAt` + 3 index `(characterId, status)`/`(characterId, templateKey, status)`/`(characterId, startedAt)`). 4 endpoint mới: `POST /story/dungeons/:key/start` (idempotent reuse ACTIVE run + reject `DUNGEON_LOCKED` khi quest chưa accept/step chưa tới/realm chưa đủ + reject `DUNGEON_ALREADY_CLEARED` cho `oneTime=true` khi đã có row CLAIMED) + `POST /story/dungeons/:runId/advance` (ownership check + `RUN_NOT_ACTIVE` + `RUN_STEP_INVALID` + track `killedMonsters[]`) + `POST /story/dungeons/:runId/clear` (verify `currentStep === monsters.length` + CAS guard `updateMany({where: {id, status: ACTIVE}, data: {status: CLEARED, clearedAt: now}})` count=0 → idempotent return existing + clear hook `applyQuestStepAdvance` fail-soft) + `POST /story/dungeons/:runId/claim` (verify status=CLEARED + CAS guard `updateMany({where: {id, status: CLEARED}, data: {status: CLAIMED, claimedAt: now}})` race-safe → exactly 1 winner; reward grant SAU CAS qua `CurrencyService.applyTx` reason `STORY_DUNGEON_REWARD` cho linhThach/tienNgoc/exp + `InventoryService.grantTx` reason `STORY_DUNGEON_REWARD` cho items theo `template.rewardHint`). Quest integration: clear hook gọi `QuestService.advanceStep(charId, questKey, stepId)` chỉ khi `QuestProgress.status === ACCEPTED` + `stepProgress[stepId] < requiredCount` — fail-soft try-catch (quest already COMPLETED/CLAIMED → no-op KHÔNG throw 500); retry clear KHÔNG double quest progress nhờ CAS guard. Anti-abuse: start idempotency (return existing ACTIVE run) / claim idempotency (CAS guard) / concurrent claim race only 1 winner / no double reward (reward grant nằm SAU CAS) / no double quest progress / locked dungeon reject `DUNGEON_LOCKED` / non-owner reject `RUN_NOT_FOUND`. New ledger reasons `STORY_DUNGEON_REWARD` thêm vào `LedgerReason` + `ItemLedgerReason` phân biệt với `DUNGEON_RUN_REWARD`. Tests +15 (`story-dungeon.service.test.ts` runtime suite): list available story dungeon / locked-cannot-start / start success + idempotent / one-time cannot start again after clear/claim / advance step success / invalid advance reject (RUN_NOT_ACTIVE + RUN_NOT_FOUND + RUN_STEP_INVALID) / clear success + invalid clear / clear updates quest progress (full + partial) / claim reward success / double claim reject + no double reward verify ledger sum / concurrent claim race 1-winner (Promise.all 2x) / non-owner cannot advance/clear/claim run / retry clear no-double-quest-progress / fail-soft quest service throw. Verification: shared typecheck ✅ + test 1504 PASS ✅ / api typecheck ✅ + `--run story` 59 PASS ✅ / `--run dungeon` 82 PASS ✅ / `--run quest` 69 PASS ✅ / api test full **2188** PASS ✅ / pnpm build ✅. KHÔNG FE map view (Phase 12.8.C), KHÔNG dialogue branch UI lớn, KHÔNG event/cutscene, KHÔNG PvP/pet/gacha, KHÔNG rewrite `DungeonRun`.` | feat |
| #465 | `feat(api,shared): Phase 12.8.A story dungeon catalog and API foundation. Story Dungeon Instance riêng cho main quest — tách khỏi dungeon farm chung (`combat.ts:DUNGEONS` / `dungeon-run`). Shared catalog `STORY_DUNGEONS` (`packages/shared/src/story-dungeons.ts`) với `StoryDungeonTemplateDef` (key + i18n title/desc + `requiredQuestKey` + `requiredQuestStep?` + `regionKey: RegionKey` + `recommendedRealm` + `minRealmKey?` + `monsterKeys[]` + `bossKey?` + `entryDialogueKey?` + `clearDialogueKey?` + `rewardHint?` + `oneTime` + `enabled`) + 4 catalog seed (Phàm Nhân Hậu Sơn / Luyện Khí Hắc Lâm / Trúc Cơ Mộc Huyền Lâm / Kim Đan Kim Sơn) reuse 14 monster + 4 region + 4 dialogue node hiện có. Helpers pure: `storyDungeonByKey`, `storyDungeonsForQuest`, `computeStoryDungeonStatus` returns `'locked'/'available'/'cleared'`, `availableStoryDungeonsForQuestState`. Invariant validator `validateStoryDungeonCatalog` check key uniqueness/snake_case + quest/step/region/realm/monster/boss/dialogue resolve + reward integer ≥ 0. API `StoryDungeonModule` (chỉ depend `AuthModule` + `PrismaService`, KHÔNG re-enter QuestService/DungeonRunService): 2 endpoint read-only `GET /story/dungeons` (list `enabled=true` + status compute từ `Character.realmKey` + `QuestProgress.stepProgress`) + `GET /story/dungeons/:key` (single template view). Response `StoryDungeonView { key, title*, description*, requiredQuestKey, requiredQuestStep, regionKey, recommendedRealm, minRealmKey, monsters[{ key, name, element, level }], boss?, rewardHint?, oneTime, status }`. KHÔNG Prisma migration, KHÔNG mutation, KHÔNG runtime advance/claim, KHÔNG FE — Phase 12.8.B sẽ wire `StoryDungeonRun` Prisma + 4 endpoint runtime + reward atomic `STORY_DUNGEON_REWARD` + auto-advance quest step. Tests +36: shared +25 (catalog ≥ 4 entries / key unique / `validateStoryDungeonCatalog` returns `[]` / `computeStoryDungeonStatus` x6 (locked / available / step-short / COMPLETED → available / CLAIMED → cleared / minRealm gate) / `availableStoryDungeonsForQuestState` filter), api +11 (story-dungeon.service: NO_CHARACTER / baseline locked / quest accepted + step done → available / step-short → locked / CLAIMED → cleared / COMPLETED → available / minRealm gate / view shape hydrated / getByKey 200 + DUNGEON_NOT_FOUND + mutation reflect). Verification: shared typecheck ✅ + `--run story-dungeons` 25 PASS ✅ / api typecheck ✅ + `--run story` 38 PASS ✅ / pnpm build cần verify trước PR.` | feat |
| #464 | `feat(api,web,shared): add Phase 12 story dialogue foundation. Branching NPC dialogue cho main quest. Shared `story-dialogues.ts` catalog (`DialogueNodeDef`/`DialogueChoiceDef` + conditions `quest_status`/`flag`/`seen_node`/`realm_min` + effects `mark_seen`/`set_flag`/`give_reward`/`advance_quest_step`). Prisma migration `20260602000000_phase_12_story_dialogue_foundation` thêm 2 cột Json `Character.storyDialogueSeen` + `Character.storyFlags`. API `StoryDialogueModule`: 2 endpoint `GET /story/dialogue/:npcKey` + `POST /story/dialogue/:npcKey/choice` atomic transaction. FE: `StoryDialogueModal.vue` + Pinia store + axios client + i18n vi/en parity. Tests +33 (shared +5 / api +16 / web +12).` | feat |
| #462 | `test(api,docs): add daily login extended coverage — 30-day streak + race-condition + controller-level DB smoke. Mở rộng coverage sau PR #460: `apps/api/src/modules/daily-login/daily-login.extended.test.ts` (+6 vitest, total api 2129). 3 nhóm: (1) chuỗi 30 ngày liên tục → streak monotonic 1→30, +3000 LT, 30 ledger row reason `DAILY_LOGIN`, 30 `DailyLoginClaim` row — lock catalog flat 100 LT/ngày; (2) race-condition `Promise.all` 5 claim cùng `(userId, now)` → 1 winner +100 LT, 4 loser idempotent (composite UNIQUE `(characterId, claimDateLocal)` + P2002 fallback path), và 10 claim trên 2 ngày liên tiếp → streak monotonic 1→2 sau race; (3) controller-level DB-backed smoke `DailyLoginController.me()`/`.claim()` với `AuthService` stub → envelope `{ ok:true, data }` + state DB đồng nhất (balance, ledger, claim row), 401 khi không cookie, 404 NO_CHARACTER mapping. KHÔNG sửa service/controller/endpoint/Prisma schema. api typecheck ✅ / api test full **2129** (+6) ✅ / pnpm build ✅.` | test |
| #461 | `docs: sync handoff after Phase 13.1 sect systems. Docs-only sync sau khi Phase 13.1.A/B + daily-login multi-day smoke merged. AI_HANDOFF_REPORT.md (170 dòng, under 250 cap) + CHANGELOG.md replace stale `(this PR)` labels với PR numbers concrete (#449/#450/#452/#455/#460). KHÔNG sửa code runtime.` | docs |
| #460 | `test(api): add daily login multi-day smoke positive coverage. Bổ sung integration test `apps/api/src/modules/daily-login/daily-login.multi-day.test.ts` (5 case, +5 vitest) cho `DailyLoginService.claim(userId, now)` test-clock injection (service signature đã hỗ trợ `now: Date` param từ Phase 11 — KHÔNG mở endpoint dev mới). Cover: (1) chuỗi 7 ngày liên tục → streak monotonic 1→7, +700 LT, 7 row ledger reason `DAILY_LOGIN`, 7 row `DailyLoginClaim`; (2) reward ngày 7 = `DAILY_LOGIN_LINH_THACH` flat catalog (100 LT/ngày); (3) double-claim cùng ngày giữa chuỗi → idempotent (claimed=false, linhThachDelta='0', streak preserved); (4) missed day giữa chuỗi → streak reset về 1 (anti free-streak); (5) timezone boundary VN ICT (cross 17:00 UTC + tz-shift double-claim guard). KHÔNG sửa service/controller, KHÔNG endpoint mới, KHÔNG Prisma migration. api typecheck ✅ / api test full 2123 (+5) ✅ / pnpm build ✅.` | test |
| #459 | `feat(api,web,shared): Phase 13.1.B Sect Missions + Sect Shop + Admin LiveOps Controls. Server-audited contribution mission ledger (5 mission daily/weekly, idempotent claim, ledger `SECT_MISSION_CLAIM`); contribution-currency Sect Shop (5 stackable items, daily/weekly caps, NON_STACKABLE_QTY_GT_1 guard, RATE_LIMITED 30 req/60s qua FailoverRateLimiter, atomic CAS spend `Character.contribBalance`, ledger `SECT_SHOP_BUY`); Admin LiveOps controls (`/admin/liveops` status + `/admin/liveops/event/toggle` upsert+audit, `/admin/sect-war/{status,recalculate}`). Prisma migration `20260601000000_phase_13_1_b_sect_missions_shop_liveops`: `Character.contribBalance`/`Character.contribLifetime` + `SectMissionClaim` composite UNIQUE `(characterId, missionKey, periodKey)` + `SectShopPurchase` ledger + `LiveOpsEventOverride` upsert. FE: 5-tab `/sect-war` (overview/leaderboard/missions/shop/rewards), 3 panel mới (Mission/Shop/AdminLiveOps), Home/Sect/Admin integration, i18n vi/en parity. Tests: +12 SectShopService + 10 AdminLiveOpsService + 12 SectMissionService + 5 SectMissionPanel + 7 SectShopPanel + 4 AdminLiveOpsPanel + 8 SectWarView tab migration. Shared 1463 ✅ / api 2118 ✅ / web 1265 ✅; typecheck shared/api/web ✅; pnpm build ✅. KHÔNG rewrite SectWar core (Phase 13.1.A read-only).` | feat |
| [#458](https://github.com/hoathienmenh-01/xuantoi/pull/458) | `fix(api,web,shared): post Phase 13.1.A sect war hardening audit — daily cap window timezone alignment (ICT 00:00 thay vì UTC 00:00) + doc UNIQUE order fix (`docs/API.md`). Audit `SectWarService.addContributionTx` phát hiện daily cap query dùng `setUTCHours(0,0,0,0)` (= 07:00 ICT) thay vì `startOfLocalDay(now, MISSION_RESET_TZ)` (= 00:00 ICT) — drift +7h, không đồng nhất với dungeon dailyLimit / mission DAILY / daily-login streak. Hệ quả: `dungeon_clear` cap 50/ngày có thể lách bằng cách clear trước 07:00 ICT, vì rows hôm trước @ 23:00 ICT (= 16:00 UTC, vẫn cùng ISO week, weekKey filter qua) bị tính nhầm vào hôm nay. Fix: import `startOfLocalDay` từ `combat.service` + `getMissionResetTz` từ `mission.service`. Doc fix: `docs/API.md` ghi sai composite UNIQUE order `(weekKey, sourceType, sourceId, characterId)` (thiếu `activityKey`) — sửa thành `(weekKey, characterId, activityKey, sourceType, sourceId)` match Prisma schema. +1 regression test `sect-war.service.test.ts` reproduce ICT 00:00 boundary: pre-insert 5 dungeon_clear contributions @ Mon 23:00 ICT (cap exact = 50), call addContributionTx với now=Tue 01:00 ICT (cùng ISO week 20) → OLD: cap hit reject; NEW: cap reset accept. Verification: shared 1463 ✅ / web 1249 ✅ / api 2078 ✅ (cũ 2077 +1 regression, 6 pre-existing Redis fail giữ nguyên trên main); typecheck shared/api/web ✅; pnpm build ✅. KHÔNG sửa scope ngoài audit (KHÔNG Sect Missions / Sect Shop / Admin LiveOps Controls / SectWar service rewrite).` | fix |
| #457 | `feat(api,web,shared): Phase 13.1.A Sect War Core — Tông Môn Chiến tuần lễ. Shared `sect-war.ts` catalog: 5 activity (`daily_login` 5pt cap 7/tuần, `dungeon_clear` 10pt cap 50/ngày, `boss_participation` 15pt cap 120/tuần, `boss_top_damage` 25pt cap 100/tuần, `quest_complete` 8pt cap 80/tuần) + 4 reward tier (rank 1 / 2-3 / 4-10 / participation), helpers `sectWarWeekKey()`/`currentSectWarSeason()`/`sectWarActivityByKey()`/`sectWarRewardTierForRank()`; title +1 `sect_war_champion`. Prisma migration `20260524000000_phase_13_1_a_sect_war_core`: 2 table mới `SectWarContribution` (composite UNIQUE `(weekKey, characterId, activityKey, sourceType, sourceId)` chống double-add) + `SectWarWeeklyRewardClaim` (composite UNIQUE `(weekKey, characterId)` chống double-claim). API: `SectWarService` + `SectWarController` + 4 endpoint `/sect-war/{current,leaderboard,me,claim}` (atomic CAS race-safe). 5 hook gameplay defensive try-catch: DungeonRun.claim, Boss.distributeRewards (participation + topDamage), DailyLogin.claim, Quest.claim — character không sect → no-op. FE: `/sect-war` route + 4 panel + Home CTA + i18n vi/en đầy đủ. Tests: shared +38, api +14, web +10. KHÔNG đụng Sect Missions / Sect Shop / Admin LiveOps (deferred 13.1.B).` | feat |
| #451 | `feat(api,shared): Phase 11.10.E Title/Buff gameplay reward hooks — Pill→Buff wire. Audit confirm 4/5 reward source đã wire trước (realm breakthrough → realm milestone title; achievement claim → title; boss kill → title gián tiếp qua achievement; tribulation/breakthrough FAIL → tam_ma debuff). Gap thực sự: Pill use → Buff apply. Shared: extend `ItemEffect` thêm `buffKey?: string`; thêm 4 pill items mới (`cuong_luc_dan`/`thiet_bich_dan`/`sinh_co_dan`/`linh_tam_dan`) wire 1-1 với 4 buff `pill_*_buff_t1` đã có; helper `buffForItem(itemKey)` cross-ref. NPC shop thêm 4 entries dailyLimit=5/người/ngày. API: `InventoryService` constructor 4th param `buffs?: BuffService` optional (forwardRef inject) — `use()` pre-resolve `buffDef = buffForItem(...)` trước tx (catalog drift guard throw KHÔNG decrement) → trong tx sau decrement + character update gọi `buffs.applyBuffTx(tx, charId, buffKey, 'pill')` (atomic rollback nếu tx fail; idempotent qua `CharacterBuff` composite UNIQUE: non-stackable refresh `expiresAt`, stackable +1 stack cap `maxStacks`). Backward-compat: `buffs?` undefined skip silently — legacy bootstrap test fixtures không phải sửa. Tests: +9 shared `buffForItem` (drift guard mọi item.effect.buffKey lookup được; no orphan buff; 4 pill round-trip), +4 inventory service (cuong_luc_dan→pill_atk_buff_t1+decrement, 4 pill distinct rows, 2 use liên tiếp non-stackable refresh `expiresAt` cùng row id, no buffs inject legacy bootstrap). Update 2 catalog test (catalog.test.ts + items-balance.test.ts) accept `buffKey` là valid pill effect. KHÔNG Prisma migration — `CharacterBuff` schema đã có từ Phase 11.8.B. Verification: shared 1389/1389 + api 2039/2039 + web 1216/1216 + typecheck/lint xanh.` | feat |
| #450 | `feat(api,web,shared): M10 Shop daily purchase limit per user/item + per-user rate limit cho /shop/buy. Shared `ShopEntryDef` thêm `dailyLimit?: number` opt-in (closed beta defaults: pills HP/MP=20, exp đan + ore=10, equipment phàm phẩm=5); `ShopEntryView` thêm `dailyLimit: number \| null` cho FE badge. `ShopService.buy()` 2 tầng pre-check trước transaction (KHÔNG trừ tiền khi reject): (1) **per-user rate limit** 30 req/60s qua `RateLimiter` injection (token `SHOP_BUY_RATE_LIMITER`, default `InMemorySlidingWindowRateLimiter`); (2) **per-item daily cap** sum `qtyDelta` ItemLedger reason='SHOP_BUY' + `qtyDelta>0` + `createdAt >= startOfLocalDay(now, MISSION_RESET_TZ)` qua `prisma.itemLedger.aggregate({ _sum })`, throw `SHOP_DAILY_LIMIT` (409) khi `current + qty > dailyLimit`. `shop.module.ts` factory bind `RedisSlidingWindowRateLimiter('rl:shop-buy', 30 req/60s)` wrap `FailoverRateLimiter` (mirror chat.module pattern, Redis disconnect → in-memory fallback, KHÔNG 500). Controller maps `SHOP_DAILY_LIMIT → 409`, `RATE_LIMITED → 429`. FE: `api/shop.ts ShopEntry` thêm `dailyLimit: number \| null`; ShopView render badge "Hạn mức hôm nay: N" khi dailyLimit !== null; i18n vi/en thêm `shop.dailyLimit` + 2 error code (`SHOP_DAILY_LIMIT`, `RATE_LIMITED`). KHÔNG Prisma migration — daily count derive từ ledger với index `(reason, createdAt)` đã có. Tests: +3 shared catalog (`dailyLimit` integer dương / closed beta all entries / `toShopEntryView` map), +11 service-level (limit hit, exact cap pass, per-item isolation, per-character isolation, hôm qua không count, không trừ tiền khi SHOP_DAILY_LIMIT, non-stackable enforce, item-không-có-limit smoke; rate limit hit, per-user isolation, không trừ tiền khi RATE_LIMITED, rate runs trước character lookup), +2 controller (HTTP mapping `SHOP_DAILY_LIMIT 409` + `RATE_LIMITED 429`), +4 ShopView (badge render khi có / không có, SHOP_DAILY_LIMIT toast, RATE_LIMITED toast). Total +20 vitest. Closes M10 backlog "Shop daily limit + rate-limit per user".` | medium-sized api+shared+web feature + 20 tests, no Prisma migration, FE shop UI badge added |
| [#449](https://github.com/hoathienmenh-01/xuantoi/pull/449) | `feat(api,web,shared): Phase 11.8.D Buff HUD + 11.9.C Title catalog/equip — server-authoritative wire. Backend đã có 4 endpoint live trên main: `GET /character/titles` (owned + 26-title catalog + equipped), `POST /character/title/equip` (validate ownership + set `Character.title`, throws `TITLE_NOT_FOUND` 404 / `TITLE_NOT_OWNED` 409), `POST /character/title/unequip` (idempotent), `GET /character/buffs` (auto-prune expired + def metadata). Shared `CharacterStatePayload` thêm `title: string \| null`; `Character.toState()` expose. PR này wire toàn bộ FE: 2 api client modules (`api/titles.ts`, `api/buffs.ts` Envelope+fallbackError), 2 Pinia stores (`stores/titles.ts` 145 LOC server-authoritative race-protected `inFlight` flag + pre-checks `TITLE_NOT_OWNED/ALREADY_EQUIPPED/NOT_EQUIPPED`; `stores/buffs.ts` 59 LOC read-only HUD), `views/TitleView.vue` (full catalog grid với 3 filter source/rarity/status, equipped banner + quick-unequip, equip/unequip toast feedback i18n error code), `components/shell/BuffBar.vue` HUD (auto-refetch 1s loop detect expiry, polarity emerald/rose pill, countdown `<60s/<3600s/≥3600s` format), AppShell topbar wire (equipped title amber-200 text via `getTitleDef(character.title)` + BuffBar `lg+` screens + `/titles` nav link 號 prefix), router lazy-load `/titles`. i18n vi/en đầy đủ (`titles.*` 8 subsection + 9 error code, `buffs.bar.aria`, `shell.nav.titles`, `apiFallback.titles*` / `apiFallback.buffsState`). Test coverage: +12 view test `TitleView` (auth gate, loading, render, filter, equipped banner, equip/unequip click, error toast), +18 controller test `character.controller.title-buff.test.ts` (envelope + ISO date + error→HTTP mapping cho 4 endpoint), +stub fix cho controller constructor positions (4 file: alchemy/skill-book/spiritual-root/talents/tribulation-log/achievements add `undefined, // title` + `undefined, // buff`), +stub fix cho `CharacterStatePayload` test (`ws-events.test.ts` 4 snapshot + `OnboardingChecklist.test.ts` `makeChar()` thêm `title: null`). Không có Prisma migration — dùng `Character.title` field đã có sẵn trong schema. NO breaking change vì backend đã merged trên main và FE chỉ wire UI thiếu.` | medium-large feature: 0 Prisma migration + FE wire 4 endpoint + 2 store + view + HUD + i18n + 30 new test |
| [#448](https://github.com/hoathienmenh-01/xuantoi/pull/448) | `feat(api,web,shared,boss): Phase 12.6 boss-by-region auto-spawn — partial unique per region + heartbeat fan-out + multi-region UI. Prisma migration `20260523000000_phase_12_6_world_boss_region_key` adds `WorldBoss.regionKey TEXT NOT NULL DEFAULT 'world'`, drops the old single-row partial unique `WorldBoss_status_active_unique`, creates `WorldBoss_status_region_active_unique ON (status, regionKey) WHERE status='ACTIVE'` so DB enforces ≤1 ACTIVE per region while allowing each region to spawn in parallel. BossService refactor: `heartbeat()` loops `bossSpawnRegions()` (catalog union of `BossDef.regionKey` ∪ `'world'` for legacy null), per-region `expire+spawn` swallows error per region; `spawnNew({regionKey?})` filters via `bossesByRegion` and rotates per-region; `adminSpawn({regionKey?, bossKey?})` resolves region (explicit → derived from def → `'world'`), validates def↔region mismatch → `INVALID_BOSS_KEY`; new `listActive()` + `getCurrentByRegion()` with backwards-compat `getCurrent()`; `attack(bossId?)` lets multi-region clients disambiguate. New endpoints `GET /boss/active` + `GET /boss/region/:regionKey` (regex validate `^[a-z][a-z0-9_]{0,63}$`). FE `BossView.vue` shows region tab strip when multiple ACTIVE, computed-driven boss view, region badge in header, i18n vi/en (`boss.regionTabsLabel`, `boss.regionBadge`, `boss.region.world`, +6 error codes). +8 region tests `boss.service.region.test.ts` (cross-region race, heartbeat fan-out, per-region skip, admin isolation, def↔region mismatch, listActive sort, getCurrentByRegion filter, schema default backfill); concurrency suite rewritten for per-region invariants; +6 shared tests for `bossSpawnRegions/bossesByRegion/WORLD_BOSS_REGION_KEY`. Closes Phase 12 exit criterion "Auto-spawn boss working with idempotency test".` | large feature: 1 Prisma migration + multi-module api refactor + new endpoints + FE region tabs + i18n + 14 new tests |
| [#447](https://github.com/hoathienmenh-01/xuantoi/pull/447) | `fix(api,boss): spawn cron auto race — partial unique index `WorldBoss_status_active_unique` (Prisma migration `20260522000000_concurrency_boss_active_unique`) enforces ≤1 ACTIVE row at any time; `spawnNew()` catches Prisma `P2002` on the active partial unique index → returns `null` (benign no-op) instead of crashing the heartbeat tick; `adminSpawn(force=true)` race-loses → throws `BOSS_ALREADY_ACTIVE` (KHÔNG ghi audit log nói dối là admin spawned). Plus in-process `heartbeatRunning` re-entry guard (skip overlap nếu previous tick còn distribute reward). Closes Concurrency phase 2 final remainder. +4 concurrency test.` | small-medium api fix + 1 Prisma migration + 4 tests, no UI change |
| [#446](https://github.com/hoathienmenh-01/xuantoi/pull/446) | `feat(api,common,chat): FailoverRateLimiter Redis-down failover + 7 test — ChatService.send() trong production wire `RedisSlidingWindowRateLimiter` ở chat.module factory; nếu Redis sống lúc bind nhưng chết runtime (pod restart, network partition, NOAUTH after rotation), `pipeline.exec()` throw → ChatService propagate 500 → user mất quyền chat. Thêm `FailoverRateLimiter(primary, fallback, logger)` wrapper try-primary-catch-fallback, log warn 1× to avoid spam, in-memory degrade per-instance trong khoảng Redis down, auto-recover khi Redis up. +6 unit + 1 integration real Redis disconnect.` | small-medium api fix + tests, no Prisma migration, no UI change |
| [#445](https://github.com/hoathienmenh-01/xuantoi/pull/445) | `fix(api,cultivation): tick CAS guard + 2 race backstop test — CultivationProcessor.process() trước fix dùng `prisma.character.update({ data: { exp, realmStage } })` ABSOLUTE write của `c.exp + gain` từ snapshot findMany. 2 worker race cùng baseline → CAS-less last-writer-wins prevents double-grant nhưng vẫn double-track mission/achievement/realtime spurious. Sau fix: `updateMany` với CAS guard `where: { id, exp: c.exp, realmStage: c.realmStage, cultivating: true }` — count=0 → continue skip side effects. +2 backstop test.` | small-medium api fix + tests, no Prisma migration, no UI change |
| [#444](https://github.com/hoathienmenh-01/xuantoi/pull/444) | `fix(api,realtime): ban during WS connection + RealtimeService.kickUser — RealtimeGateway.handleConnection thiếu User.banned check sau JWT verify, banned user vẫn connect/nhận state:update/chat:msg/cultivate:tick đến khi access-token TTL ~15min expire. Fix: post-JWT-verify query user.banned → emit error{code:ACCOUNT_BANNED} + disconnect(true). Thêm RealtimeService.kickUser(userId, reason) snapshot+emit+disconnect, idempotent. Wire AdminService.setBanned(banned=true) gọi kickUser. +2 race test gateway + 3 unit test service.kickUser.` | small-medium api fix + tests, no Prisma migration, no UI change |
| [#443](https://github.com/hoathienmenh-01/xuantoi/pull/443) | `fix(api,inventory): atomic revoke() qty decrement + 2 concurrency tests — InventoryService.revoke() JS-capture race (data: { qty: r.qty - take } reads from findMany before tx commit; 2 admin call song song → cả 2 thread cùng update qty=N-take, row leak under-revoke + ledger lệch DB delta). Fix: per-row guarded `updateMany` (where qty: { gte: take }, decrement) + guarded `deleteMany` (where qty: take). count=0 → throw INSUFFICIENT_QTY → tx rollback. Pattern parity với `use()` atomic decrement. +2 race test trong inventory.service.concurrency.test.ts (3.A: 30× Promise.all([revoke 5, revoke 5]) on qty=10 — ledger sum khớp DB delta invariant; 3.B: 30× Promise.all([revoke 7, revoke 7]) over-subscribe — exactly 1 succeed + 1 INSUFFICIENT_QTY). Concurrency phase 2 progress.` | small-medium api fix + tests, no Prisma migration, no UI change |
| [#442](https://github.com/hoathienmenh-01/xuantoi/pull/442) | `balance(shared): Phase 12.5 tune late-game story dungeon monsters — stat tuning HP/ATK/DEF/SPD/level + monsterType promotion (HUMANOID→ELITE tich_thien_sat_thu, SPIRIT→ELITE tam_ma_nguyen_anh, HUMANOID→BOSS huyet_anh) + 3 lootTable override Phase 12.4 convention + 11 invariant test dungeons-balance.test; ky_uc_meo giữ nguyên là story-hard intentional tier gap (Nguyên Anh stat trong Trúc Cơ dungeon moc_huyen_lam, document ở BALANCE_MODEL §5.4 appendix)` | medium shared catalog + tests, no API change, no Prisma migration, no UI change |
| [#441](https://github.com/hoathienmenh-01/xuantoi/pull/441) | `docs(handoff): sync post PR #440 merged state — AI_HANDOFF_REPORT.md current main = post PR #440 merged + PHASE12_STORY_PROGRESS QuestView dungeon hint CLOSED ✅ + Recommended Next Roadmap reorder (Phase 12.5 → Concurrency phase 2 → Shop daily limit → CSP)` | docs-only sync |
| [#440](https://github.com/hoathienmenh-01/xuantoi/pull/440) | `feat(shared,web): Phase 12 Story discoverability — QuestView dungeon hint cho kill+monster step (shared helper findDungeonsForQuestPlaceholder resolve dungeon qua direct key match + MonsterDef.questTargetIds alias dedupe theo dungeon.key + FE QuestView.vue render line "📍 Tìm tại: {names}" inline dưới step + i18n vi/en parity quest.stepHint.foundIn + 5 shared test + 4 FE test); UX gap close: player giờ thấy ngay dungeon đi cho mỗi kill+monster step (8 late-game placeholder + 7 PR-6 critical-path) — KHÔNG cần tự tra catalog` | small shared+FE feature + tests, no API change, no Prisma migration |

**Phase summary tables (PR #1 → #396) + smoke detail per-module + PR #414/#415 + PR #430/#437 entries**: tách sang [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Phase Summary Migrated 2026-05-05 + § Smoke Detail Migrated 2026-05-05.

---

## 3. Current Phase Status

| Phase | Title | Status | Note |
|---|---|---|---|
| 0–8 | Foundation: schema + auth + core gameplay | **Done** ✅ | Full feature catalog ở [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Completed Features. |
| 9 | Beta readiness (Phase 9.A→9.E sub-phases polish) | **11/15 Done, 3 Partial** | Detail [`BETA_CHECKLIST.md`](./BETA_CHECKLIST.md). Mail UI gap closed by PR #391. |
| 10 | Content scale | **5/5 CLOSED** ✅ | All sub-tracks merged. |
| 11 | Progression Depth | **catalog 11/11 + runtime 10/10 + UI merged** | Phase 11 nâng cao (§2/§3/§5/§6 + 11.6.C/D/E + 11.1.E) tất cả CLOSED. |
| 11.X | UI E2E smoke Playwright | **DONE** | Spec #19 talent learn→cast→cooldown + #20 breakthrough flow merged via #394/#420. |
| 12 | World Map & Dungeon | **CLOSED** ✅ | 12.1 catalog CLOSED (#397). 12.2.A `dailyLimit` enforcement CLOSED (#421). Story PR-1→PR-6 + Foundation Extension + Late-game wire CLOSED (#425–#433). **12.2.B/C + 12.3** CLOSED ✅ (#434–#436). **E2E spec #22 dungeon-run flow** CLOSED ✅ (#437). **12.4 per-monster `MonsterDef.lootTable` polish** CLOSED ✅ (#438). **Story Foundation Late-game encounter wire** CLOSED ✅ (#439). **Story discoverability — QuestView dungeon hint cho kill+monster step** CLOSED ✅ (#440). **docs handoff sync** CLOSED ✅ (#441). **12.5 dungeon balance tuning — 8 late-game story monster stat + lootTable** CLOSED ✅ (#442). **12.6 Boss-by-region auto-spawn** CLOSED ✅ (#448 — closes Phase 12 exit criterion "Auto-spawn boss working with idempotency test"). Next (optional): dialogue branch / DungeonTemplate story-instance. |
| Concurrency phase 2 | Race condition fix sweep | **CLOSED** ✅ | All 6 races fixed: Inventory `use()` (#422), Inventory `revoke()` (#443), Realtime ban-during-connection (#444), Cultivation tick CAS (#445), Chat Redis failover (#446), Boss spawn cron auto race (#447). Concurrency phase 2 **DONE**. |
| 13.0 | LiveOps & Retention Suite | **CLOSED** ✅ | Daily-login + Quest cadence + Title catalog + Buff HUD live trên main (post #456 audit pass #5). |
| 13.1.A | Sect War Core / Contribution / Leaderboard / Weekly Reward | **CLOSED** ✅ | PR #457 merged + PR #458 hardening audit (daily cap timezone fix + doc UNIQUE order fix). |
| 13.1.B | Sect Missions + Sect Shop + Admin LiveOps Controls | **CLOSED** ✅ | PR #459 merged. Server-audited mission ledger + contribution-currency shop với caps/CAS/RATE_LIMITED + admin event toggle + sect-war recalc. Migration `20260601000000_phase_13_1_b_sect_missions_shop_liveops`. |
| 13+ | Real-time PvP / pet gacha / voice / streaming | **Not started** | Per [`LONG_TERM_ROADMAP.md`](./LONG_TERM_ROADMAP.md) §0 — explicitly DO NOT build yet. |

**Smoke coverage**: 28 module (~15 step avg, +`smoke:quest` 20 step PR-3, +`smoke:npc` 11 step PR-4, +`smoke:dungeon-run` 16 step PR #434). 8 module có cả negative + positive path: skill / shop / mail / inventory / spiritual-root / breakthrough / auth / cultivation-method. Defer: daily-login multi-day positive (cần admin advance-day). Full per-module step count + endpoint coverage → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Smoke Detail Migrated 2026-05-05.

---

## 4. Known Issues / Risks

### Live (Open) — cần action

| # | Severity | Issue | Status / Plan |
|---|---|---|---|
| M7 | Medium | CSP production-ready nhưng chưa test deploy với CDN/asset domain khác. | **Open** — khi deploy production cần review `script-src`, `connect-src`. |

### Resolved (recent — full list ở ARCHIVE)

- M10 Shop daily limit + per-user rate limit → Resolved PR #450 (`ShopEntryDef.dailyLimit` opt-in + `rl:shop-buy` 30 req/60s qua FailoverRateLimiter; pre-check trước tx, KHÔNG trừ tiền khi reject; +20 vitest).
- Inventory `use()` JS-capture race → Resolved PR #422 (atomic `updateMany` + post-decrement delete + INVENTORY_ITEM_NOT_FOUND translation + 5 regression test).
- Phase 12.2.A daily limit enforcement → Resolved PR #421.
- M9 Settings logout-all `passwordVersion` → Resolved PR #154/#155 (intentional trade-off documented `docs/SECURITY.md §1`).
- M11 `GET /character/profile/:id` rate-limit → Resolved PR #62 (`PROFILE_RATE_LIMITER` 120 req/IP/15min).
- M8 Admin guard MOD broad quyền → Resolved PR E (`@RequireAdmin()` decorator + reflector AdminGuard).
- M6 LogsModule chưa build → Resolved PR #88 BE + #91 FE (`/logs/me` keyset pagination + ActivityView.vue + 24 ledger reason i18n).

> **Full historical issues** (~50 entries Critical/High/Medium/Low + Resolved) ở [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Section 16 Known Issues / Risks.

---

## 5. Tests / CI / Smoke / E2E

### Vitest baseline (post this PR)

| Workspace | Test count | Notes |
|---|---|---|
| `apps/api` | **2129** | +6 từ this PR (`daily-login.extended.test.ts`: 30-day streak + race-condition + controller DB smoke) lên trên 2123 post-PR-#460 baseline. Pre-existing `chat.service.test.ts` rate-limit window test occasionally flakes khi chạy parallel; pass cô lập (`pnpm --filter @xuantoi/api test -- --run chat.service`). 6 redis-dependent tests (rate-limiter + health) require Redis up. |
| `packages/shared` | **1463** | Không thay đổi vs PR #457/#459/#460/this PR. |
| `apps/web` | **1265** | Không thay đổi vs Phase 13.1.B (SectMissionPanel 5 + SectShopPanel 7 + AdminLiveOpsPanel 4 + 8 SectWarView migration). PR #460/#461/this PR không đổi web. |
| **Total** | **4857 vitest** | All green local (PG+Redis up). |

### CI

- Workflow `.github/workflows/ci.yml`: `build` + `e2e-smoke` jobs (PG+Redis services). Chạy mỗi PR + push (no path filter — docs-only PR vẫn trigger CI). Yêu cầu xanh trước khi merge.
- 5 redis-dependent test (rate-limiter + health controller) cần Redis local — pass khi Redis container up.

### Smoke Scripts (manual, không nằm CI matrix)

29 smoke scripts, ~15 step avg. Yêu cầu local stack: `pnpm infra:up` + `pnpm --filter @xuantoi/api exec prisma migrate deploy` + `pnpm --filter @xuantoi/api run bootstrap` + `pnpm --filter @xuantoi/api dev`. Module list: achievement, admin, auth, beta, boss, breakthrough, chat, combat, cultivation, cultivation-method, daily-login, **dungeon-run** (Phase 12.2.B — 16/16 step, positive flow tới claim + double-claim reject), economy, giftcode, inventory, leaderboard, mail, market, mission, next-action, npc, **quest** (Phase 12 PR-2 — 16/16 step, negative-path heavy), sect, shop, skill, spiritual-root, **story-dungeon** (Phase 12.8.E — 35/35 step, full player flow: auth gates + register/onboard + drive prereq quest COMPLETED + accept story quest + start ACTIVE idempotent + advance×3 + clear + claim reward + double-claim/re-clear/re-start reject + non-owner reject + data isolation), topup, ws.

**Positive-path coverage** (8 module): skill (PR #388/#390), shop (#390), mail (#391), inventory (#385), spiritual-root (#386), breakthrough (#417), auth (#384), cultivation-method (#394). Còn defer: daily-login multi-day positive — pending admin advance-day endpoint hoặc service helper extension.

**Per-module detail table** (step count, negative/positive flag, notes) → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Smoke Detail Migrated 2026-05-05.

### E2E (Playwright)

- `apps/web/e2e/golden.spec.ts` — 22 spec golden path (auth smoke + 21 full-stack gated `E2E_FULL=1`). Notable: #19 talent learn→cast→cooldown badge (`golden.spec.ts:759-895`), #20 breakthrough attempt → outcome banner + history reload-persist (`golden.spec.ts:898-981`), #21 Phase 12 Story PR-5 main storyline Chapter 1 playable (`golden.spec.ts:1031-1125`), **#22 Phase 12.3 DungeonRun flow** (`golden.spec.ts:1162-end` — son_coc start→next×3→claim, verify per-encounter loot span + reward delta).
- CI job `e2e-smoke`: chạy spec #1 AuthView smoke mỗi PR (matrix postgres+redis, build api+web, `E2E_SMOKE=1`).
- `E2E_FULL=1` gate cho 19 full-stack spec **chưa wire CI** — runtime manual với `pnpm infra:up` + `pnpm --filter @xuantoi/api dev` + `pnpm --filter @xuantoi/web dev`.

---

## 6. Recommended Next Roadmap

Per [`AI_WORKFLOW_RULES.md`](./AI_WORKFLOW_RULES.md) §SESSION PR LIMIT (1-3 PR/session) + §GOM TRƯỚC KHI TÁCH 4b. Ưu tiên Medium PR > Hotfix > Large.

### Top priority — next session

1. **Phase 12 Story dialogue branch / story dungeon instance** (optional, Medium-Large PR) — 8 placeholder kill milestone hiện có stat tuned (Phase 12.5) nhưng không có dialogue branch sau kill; hoặc tạo `DungeonTemplate` story-instance tách biệt từ farm dungeon.

2. **CSP production verify** (M7) — Hotfix khi deploy production. Test `script-src` / `connect-src` với CDN domain khác.

3. **Admin LiveOps advanced controls** (Medium PR) — Phase 13.1.B đã ship minimal (`/admin/liveops` toggle event override + audit + sect-war recalc). Mở rộng: manual force-spawn boss override, season schedule editor, mission/shop catalog hot-swap.

4. **Phase 13.2 Cross-sect seasonal expansion / auction / diplomacy** (Large PR, không gấp) — Phase 13.1.B đã đóng Sect Missions + Sect Shop + Admin LiveOps. Mở rộng cross-sect: seasonal cross-sect war bracket, sect-vs-sect auction, sect diplomacy / alliance / declaration of war.

### Phase 12 Story chain status

Design source ở [`docs/story/TU_TIEN_LO_STORY_BIBLE.md`](./story/TU_TIEN_LO_STORY_BIBLE.md) + [`docs/story/PHASE12_STORY_PROGRESS.md`](./story/PHASE12_STORY_PROGRESS.md). 5-PR core roadmap + Foundation Late-game wire + encounter wire + discoverability hint **all CLOSED** ✅:

- **PR-1** → **PR-6** — Catalog foundation, Quest runtime, Claim, NPC dialogue UI, Chapter 1 playable, Combat kill hook. **CLOSED** ✅ (#425–#432).
- **Foundation Late-game wire** — 8 monster catalog (`tich_linh_anh`, `tam_ma_anh`, `tich_linh_quy`, `tich_thien_sat_thu`, `tam_ma_nguyen_anh`, `chap_niem_anh`, `ky_uc_meo`, `huyet_anh`). **CLOSED** ✅ (#433).
- **Foundation Late-game encounter wire** — wire 8 placeholder vào 4 dungeon `monsters[]`. **CLOSED** ✅ (#439).
- **QuestView dungeon hint** — close discoverability gap. **CLOSED** ✅ (#440).
- **Phase 12.5 dungeon balance tuning** — stat HP/ATK/DEF/SPD/level + monsterType promotion (3 ELITE/BOSS) + 3 lootTable override + 11 invariant test. **CLOSED** ✅ (#442).

Next nâng cao Phase 12 Story (optional, nếu cần): thêm dialogue branch cho 8 placeholder kill milestone HOẶC tạo `DungeonTemplate` story-instance tách biệt từ farm dungeon (instance riêng → boss-rush single-shot). Sau mỗi PR Phase 12 Story merged, AI **bắt buộc** cập nhật `docs/story/PHASE12_STORY_PROGRESS.md` trong cùng PR (DOCS UPDATE RULE).

### Backlog (low priority)

- **Doc compaction maintenance**: khi Recent Changes vượt 10 entry → đẩy entry cũ nhất xuống ARCHIVE § Recent Changes Legacy. Khi `AI_HANDOFF_REPORT.md` vượt **250 dòng** → compact ngay theo HANDOFF REPORT STRUCTURE RULE (cap mới 2026-05-05).
- **Concurrency tests phase 2 (low priority remainder)**: Boss spawn cron auto race.

### Anti-feature-creep (DO NOT BUILD YET)

Per [`LONG_TERM_ROADMAP.md`](./LONG_TERM_ROADMAP.md) §0:
- Real-time PvP (Phase 14)
- Party / co-op dungeon (Phase 12 — wait Phase 11 ≥ 95%)
- Pet / wife gacha (Phase 16)
- Voice chat
- Video streaming

---

## 7. Archive (đã tách file riêng)

Toàn bộ Archive (Snapshots PR #33→#396, Recent Changes Legacy, Completed Features, Project Reference đầy đủ Tech Stack / Architecture / DB / Gameplay / Run Locally / Rules, Old Recommended Next Roadmap, Exact PR Plan, Phase Summary Migrated 2026-05-05, Smoke Detail Migrated 2026-05-05) đã tách ra file riêng để giảm token cost mỗi session.

Chi tiết: [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md). **AI mới KHÔNG cần đọc file đó mỗi session — chỉ tra cứu khi cần điều tra PR/history cụ thể.**
