# AI Handoff Report — Xuân Tôi

> 👉 **AI/dev mới: ĐỌC [`docs/START_HERE.md`](./START_HERE.md) TRƯỚC.** File đó định tuyến tới đúng doc theo task type (state / vision / roadmap / economy / content / balance / live ops / story).
>
> **Cấu trúc**: file này chỉ chứa **trạng thái live hiện tại** (Executive Summary + Recent Changes + Phase Status + Known Issues + Tests/CI/Smoke + Next Roadmap). **Toàn bộ lịch sử PR cũ** (Snapshots PR #33→#396, Recent Changes Legacy, Completed Features, Project Reference đầy đủ Tech Stack / Architecture / DB / Gameplay / Run Locally / Rules, Old Recommended Next Roadmap, Exact PR Plan, smoke detail table per module) tách ra [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md). **AI mới CHỈ cần đọc file này** — `ARCHIVE_HANDOFF.md` chỉ tra cứu khi cần.
>
> **Cap**: ≤ 250 dòng (HANDOFF REPORT STRUCTURE RULE — xem [`AI_WORKFLOW_RULES.md`](./AI_WORKFLOW_RULES.md)). Vượt cap → bắt buộc compact + đẩy entry cũ xuống ARCHIVE.

---

## 1. Current Executive Summary

- **Current `main` commit**: post PR #484 merged (Phase 12.10.B NPC Gift + Quest Affinity Rewards) + PR #483 (Phase 14.3.B Tribulation Support Providers + Redirect UX) + PR #482 (Phase 14.2.B Elemental Combat Data + Balance) + PR #481 (Phase 14.0.B Sect Territory Settlement + Region Ownership) + PR #480 (docs handoff sync) + PR #479 (Phase 14.0.A Sect Territory Influence Foundation) + PR #478 (Phase 14.3.A Breakthrough Tribulation Foundation) + PR #477 (Phase 14.2.A Elemental Combat Foundation) + PR #476 (Phase 12.10.A NPC Affinity Foundation) + Phase 13.2.A/B/C Sect Season + Phase R1 Production Readiness + Phase 13.0 LiveOps & Retention Suite. Story design source live tại [`docs/story/`](./story/).
- **Current phase**: Phase 10 **CLOSED** ✅. Phase 11 Progression Depth **COMPLETE** ✅. Phase 12 World Map & Dungeon **CLOSED** ✅. Phase 13.0/13.1.A–D LiveOps **CLOSED** ✅. Phase 13.2.A/B/C Sect Season **CLOSED** ✅. Phase R1 Production Readiness **CLOSED** ✅. Phase 12 Story Dialogue Foundation + 12.8.A–E + 12.9 **CLOSED** ✅. **Phase 12.10.A NPC Affinity Foundation** **CLOSED** ✅ (PR #476). **Phase 12.10.B NPC Gift + Quest Affinity Rewards** **CLOSED** ✅ (PR #484). **Phase 14.0.A Sect Territory Influence Foundation** **CLOSED** ✅ (PR #479). **Phase 14.0.B Sect Territory Settlement + Region Ownership** **CLOSED** ✅ (PR #481). **Phase 14.0.C Sect Territory Region Buff + Influence Decay** **CLOSED** ✅ (this PR). **Phase 14.2.A Elemental Combat Foundation** **CLOSED** ✅ (PR #477). **Phase 14.2.B Elemental Combat Data + Balance** **CLOSED** ✅ (PR #482). **Phase 14.3.A Breakthrough Tribulation Foundation** **CLOSED** ✅ (PR #478). **Phase 14.3.B Tribulation Support Providers + Redirect UX** **CLOSED** ✅ (PR #483).
- **In-flight**: none. Roadmap kế tiếp ưu tiên **Phase 14.0.D Territory Weekly War Loop** + **14.3.C Tribulation Attempt Item Consumption** + **12.10.C NPC Affinity Shop + Hidden Unlocks** + **13.2.D Season Settlement Automation** (xem §6).

- **Phase 12.10.B (PR #484)**: NPC Gift + Quest Affinity Rewards. **Shared (`packages/shared/src/npc-gift.ts` mới)**: `NpcGiftPreferenceDef` catalog 5 NPC × `acceptedItems[{itemKey, affinityMin, affinityMax, flavor*}]` + `dailyLimit` per-NPC (cap `NPC_GIFT_DAILY_LIMIT_CAP=5`); helpers `npcGiftPreferenceForKey`/`acceptedGiftItemFor`/`computeGiftAffinityDelta` (clamp per-gift `NPC_GIFT_AFFINITY_DELTA_CAP_PER_GIFT=8`); `validateNpcGiftCatalog` ép key uniqueness + item resolve + cap. **Quest reward**: `QuestRewardDef.affinity?: { npcKey, delta }` + `claim()` apply qua `addAffinityTx` cap `AFFINITY_DELTA_CAP_PER_QUEST_REWARD=40`. **Prisma migration `20260609000000_phase_12_10_b_character_npc_gift_log`**: table `CharacterNpcGiftLog` (`id, characterId, npcKey, itemKey, dayBucket, sequence, affinityDelta, createdAt`) + composite UNIQUE `(characterId, npcKey, dayBucket, sequence)` race-safe + 2 index. **API mới `POST /story/npc-affinity/:npcKey/gift`**: `NpcAffinityService.giftNpcTx` atomic — count daily logs (UTC `YYYY-MM-DD` bucket) + compare `dailyLimit` reject `DAILY_LIMIT_EXCEEDED`; consume 1 item qua `inventory.consumeTx` reject `ITEM_NOT_FOUND`/`WRONG_ITEM`; `addAffinityTx` apply delta; insert log row P2002 retry once với updated count. Quest claim idempotent qua existing CAS guard `updateMany({status, claimedAt})` — affinity reward KHÔNG double-apply. **FE**: `apps/web/src/api/npcAffinity.ts` thêm `giftNpc()` fetcher; `stores/npcAffinity.ts` thêm `giftAction` in-place update (no full reload) + `giftToast{npcKey,delta,newTier?}` auto-dismiss 3.5s + `giftError` code map; `NpcAffinityPanel.vue` thêm gift section per-NPC: select item (badge affinity range + flavor), gift button locked khi `dailyState.locked`, daily counter `used/limit` tabular-nums, success toast với delta + tier-up nếu có, error message dismiss-on-success. **Tests +52**: shared `--run npc-gift` 9 PASS (catalog seed + helper + cap invariant); api `--run npc-affinity` 25 PASS (gift success / no item / wrong item / daily limit / race UNIQUE retry / multi-NPC isolation); api `--run quest` 73 PASS (+4 affinity reward idempotent: claim once apply, claim twice KHÔNG double, multi-NPC reward, low-tier null-NPC skip); web `--run NpcAffinity` 18 PASS (+6 gift flow: button enabled/disabled, select renders accepted items, success toast, daily limit lock, error display, dismiss toast). Verification: shared typecheck + 1732 PASS / api typecheck + `--run npc-affinity` 25 PASS + `--run quest` 73 PASS / web typecheck + `--run NpcAffinity` 18 PASS / pnpm build ✅. Daily bucket UTC `YYYY-MM-DD` reset 00:00 UTC (cân nhắc local-day Phase 12.10.C nếu cần); KHÔNG ledger riêng (consume + insert log thay thế).

- **Phase 14.3.B (PR #483)**: Tribulation Support Providers + Redirect UX. **Shared (`packages/shared/src/tribulation-support-providers.ts` mới)**: 4 provider pure read-only — `collectItemTribulationSupports`/`collectBuffTribulationSupports`/`collectEquipmentTribulationSupports`/`collectTalentTribulationSupports`. **API**: `TribulationService.previewTribulation()` populate `successChance.supportBonus` (cap total `0.30`) + `supports[]` per source `{source, key, bonus, label?, element?}`. **KHÔNG consume item / KHÔNG decrement buff / KHÔNG mutate state** — preview read-only; consumption defer Phase 14.3.C. **FE — BreakthroughView**: catch `TRIBULATION_REQUIRED` 409 → toast info + `router.push('/tribulation')`. **FE — TribulationView preview**: render mỗi support entry badge source + element + bonus%; cap warning ceil/floor. **Verification**: shared 23 provider test PASS / api `--run tribulation` 78 PASS / web `--run Tribulation` 147 PASS + `--run Breakthrough` 49 PASS / pnpm build ✅. KHÔNG migration / KHÔNG schema.

- **Phase 14.2.B (PR #482)**: nâng Phase 14.2.A Elemental Combat Foundation từ pipeline trống thành gameplay thật. **Shared catalog**: `boss.ts` thêm `elementalResist` 6 boss (floor 0.7); `monsters.ts` thêm 8 monster mid/late; `items.ts` thêm `bonuses.elementalAtkBonus` 6 equipment (per-item ceil 0.10). **Invariant tests** `elemental-data.test.ts` 35 case ép catalog tuân floor/ceil + gear-stack ≤ 0.20 + countered + max bonus + min resist < 1.0× anti-power-creep. **API combat tests** +5 case: baseline / equipment bonus / monster resist / no double-apply (Math.random=0.5 deterministic 659) / null-element noop. **FE InventoryView**: tooltip `elementalAtkBonus` parity vi/en; +3 test. KHÔNG migration / KHÔNG đụng combat pipeline runtime. Verification: shared 1685+ PASS / api `--run combat` PASS / web `--run InventoryView` 57 PASS / pnpm build ✅.

- **Phase 14.0.C (this PR)**: Region Buff + Influence Decay. **Shared (`packages/shared/src/territory-buffs.ts` mới)**: `TerritoryRegionBuffDef` interface + `TERRITORY_REGION_BUFFS` catalog 5 buff (son_coc EXP_BONUS 5% / hac_lam LINH_THACH_BONUS 5% / moc_huyen_lam ELEMENTAL_DAMAGE 5% moc / kim_son_mach ELEMENTAL_DAMAGE 5% kim / hoang_tho_huyet DEFENSE_BONUS 5%); helpers `territoryRegionBuffsForRegion`/`territoryRegionBuffForOwner`/`activeTerritoryBuffsForSect`/`validateTerritoryBuffCatalog`/`computeTerritoryDecay`; envelope `TERRITORY_BUFF_VALUE_MAX=0.10` (cap value), `TERRITORY_DECAY_DEFAULT_BPS=2500` (25%), `TERRITORY_DECAY_MAX_BPS=5000` (50%); 32 unit test PASS (catalog UNIQUE + region exists + value ≤ cap + decay deterministic floor + helper coverage). **Prisma + Migration `20260610000000_phase_14_0_c_territory_decay_log`**: model `SectTerritoryDecayLog` (`id, periodKey UNIQUE, decayBps, rowsAffected, pointsBefore, pointsAfter, delta, triggeredAt, triggeredBy?`) — race-safe + idempotent qua UNIQUE `(periodKey)`. **API runtime**: `TerritoryDecayService.runDecay({periodKey, decayBps})` validate range `1..5000` reject `DECAY_BPS_INVALID`; insert log row P2002 retry → fetch tồn tại → return `{skipped:true}`; aggregate influence decay theo `computeTerritoryDecay` (floor, không âm); cập nhật `SectTerritoryInfluence.points`; 17 test PASS. `DungeonRunService.claimRun()` integrate `applyTerritoryDungeonRewardBuffs(rewards, regionKey, ownerSectId, characterSectId)` fail-soft owner-only, KHÔNG double-apply khi retry; 6 test mới (no-region/no-sect/non-owner/owner-EXP/owner-LinhThach/double-claim-no-double). **API endpoints mới/extend**: `POST /admin/territory/decay` (admin-only, `RequireAdmin`); `GET /territory/regions` extend `buffs/ownerBuffActive/currentPeriodKey/previousPeriodKey`; `GET /territory/me` extend `activeBuffs/currentPeriodKey`. **FE**: `apps/web/src/api/territory.ts` thêm `TerritoryRegionBuffPreviewLite/TerritoryDecayResult/adminTerritoryDecay`; `stores/territory.ts` thêm `decayLoading/decayError/lastDecayResult/adminDecay()` invalidate leaderboard cache + refetch regions/me; `TerritoryView.vue` thêm region buff list per-region (active/inactive badge dựa `ownerBuffActive`) + my active buffs panel + admin decay panel role-gated với input `decayBpsInput` + skipped/error/result render; i18n vi/en parity full `territory.overview.buffSection*/buff.*/myBuffs.*/admin.decay*/errors.DECAY_BPS_INVALID`. **Tests +9 web (15→24)** + 32 shared + 23 api (17 decay + 6 dungeon buff). Verification: shared typecheck + `--run territory` 55 PASS / api typecheck + `--run territory` 66 PASS + `--run dungeon` 88 PASS + `--run combat` 126 PASS / web typecheck + `--run Territory` 24 PASS / pnpm build ✅. **KHÔNG cron** (defer 14.0.D); **KHÔNG combat-elemental wire** (catalog ship sẵn nhưng giữ Phase 14.2.x envelope, defer); **KHÔNG xóa influence cũ** (decay aggregate qua tính lại points, log per-period idempotent).
- **Phase 14.0.B (PR #481)**: Settlement & Region Ownership. Migration `20260608000000_phase_14_0_b_territory_settlement` thêm `SectTerritoryRegionState` (1 row/region) + `SectTerritorySettlementSnapshot` UNIQUE `(regionKey, periodKey)` race-safe. `TerritorySettlementService.settleRegion/settleAllRegions/getRegionHistory/getOwnerStateMap` idempotent + tie-break `sectId.localeCompare`; admin endpoint `POST /admin/territory/settle` + per-region. FE: TerritoryView overview owner badge + history panel + admin panel role-gated. Tests +56 (shared 6 + api 49 + admin 12 + web 15). **Region buff khi rank 1 chưa wire** (defer 14.0.C); decay/season reset chưa có (defer 14.0.C/13.2.D).
- **Phase 12.10.A (PR #476)**: NPC Affinity Foundation. `CharacterNpcAffinity` per-(char, npcKey) row atomic; `addAffinityTx` cap clamp `[minScore, maxScore]`; tier unlocks; FE NpcAffinityPanel read-only view. Foundation cho 12.10.B gift + quest reward.
- **Test baseline (post Phase 14.0.C this PR)**: shared **1764** PASS ✅ (1732 + 32 territory-buffs) / api `--run territory` **66** PASS / `--run dungeon` **88** PASS / `--run combat` **126** PASS / `--run npc-affinity` **25** PASS / `--run quest` **73** PASS / `--run tribulation` **78** / web `--run Territory` **24** PASS ✅ / `--run NpcAffinity` **18** / `--run Tribulation` **147** / `--run Breakthrough` **49** / `--run InventoryView` **57** / pnpm build ✅.
- **Top priority next session**: (1) **Phase 14.0.D Territory Weekly War Loop** — cron auto-settle + auto-decay theo ISO week, hall-of-fame archive, sect war reward integration; (2) **Phase 14.3.C Tribulation Attempt Item Consumption** — 14.3.B preview read-only; consumption khi attempt thật (decrement item + apply buff); (3) **Phase 12.10.C NPC Affinity Shop + Hidden Unlocks** — affinity tier unlock hidden dialogue branch + NPC affinity shop với contrib currency; (4) **Phase 13.2.D Season Settlement Automation** — cron auto-settle territory periodKey + Hall of Fame archive. Detail §6.
- **Open Critical/High issues**: none. Live medium issues: M7 (CSP production verify khi deploy). Detail §4.
- **Blocker**: none.

---

## 2. Recent Changes

10 PR gần nhất merged trên main (newest đầu, mỗi entry 1 dòng). PR cũ hơn → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Snapshots.

| PR | Title | Type |
|---|---|---|
| #484 | feat(api,web,shared): Phase 12.10.B NPC gift and quest affinity rewards. Shared `npc-gift.ts` catalog 5 NPC × accepted item + dailyLimit cap 5 + per-gift cap 8; `validateNpcGiftCatalog`. Migration `20260609000000_phase_12_10_b_character_npc_gift_log` thêm `CharacterNpcGiftLog` UNIQUE `(characterId, npcKey, dayBucket, sequence)` race-safe (P2002 retry once). API `POST /story/npc-affinity/:npcKey/gift` atomic — daily count UTC `YYYY-MM-DD` + reject `DAILY_LIMIT_EXCEEDED`/`ITEM_NOT_FOUND`/`WRONG_ITEM`; consume 1 item + `addAffinityTx` cap 8. Quest integration: `QuestRewardDef.affinity?{npcKey, delta}` + claim apply qua existing CAS guard idempotent. FE NpcAffinityPanel gift section: select item (badge affinity range), gift button locked daily, success toast (delta + tier-up) auto-dismiss, error display. Tests +52: shared 9 / api 25 + 73 quest / web 18. Verification: shared 1732 ✅ / api `--run npc-affinity` 25 + `--run quest` 73 / web `--run NpcAffinity` 18 / pnpm build ✅. Daily bucket UTC 00:00; KHÔNG ledger riêng. | feat |
| #483 | feat(api,web,shared): Phase 14.3.B tribulation support providers + redirect UX. Shared `tribulation-support-providers.ts` 4 provider pure read-only (item/buff/equipment/talent) + catalog seed `lei_kiep_phu` + `thien_lei_phu`. API `previewTribulation()` populate `successChance.supportBonus` cap total 0.30 + `supports[]` per source. KHÔNG consume / KHÔNG decrement / KHÔNG mutate state — preview read-only (defer 14.3.C). FE BreakthroughView catch `TRIBULATION_REQUIRED` 409 → toast info + router redirect `/tribulation`. TribulationView preview render badge source + element + cap warning. Tests: shared 23 provider / api `--run tribulation` 78 / web `--run Tribulation` 147 + `--run Breakthrough` 49 ✅. KHÔNG migration. | feat |
| #482 | feat(api,web,shared): Phase 14.2.B elemental combat data + balance. Shared catalog: `boss.ts` 6 boss `elementalResist` (floor 0.7); `monsters.ts` 8 monster mid/late; `items.ts` 6 equipment `bonuses.elementalAtkBonus` (per-item ceil 0.10). Invariant tests `elemental-data.test.ts` 35 case ép catalog tuân floor/ceil + 6-slot stack ≤ 0.20 + max-bonus + counter + min-resist < 1.0× anti-power-creep. API `--run combat` +5 case (no double-apply Math.random=0.5 deterministic 659; null-element noop). FE InventoryView tooltip `elementalAtkBonus` + 3 test. KHÔNG migration / KHÔNG đụng combat pipeline runtime. Verification: shared 1685+ ✅ / api combat / web `--run InventoryView` 57 / pnpm build ✅. | feat |
| #481 | feat(api,web,shared): Phase 14.0.B sect territory settlement + region ownership. Migration `20260608000000_phase_14_0_b_territory_settlement` thêm 2 model `SectTerritoryRegionState` (1 row/region, owner FK SET NULL) + `SectTerritorySettlementSnapshot` UNIQUE `(regionKey, periodKey)` race-safe. `TerritorySettlementService.settleRegion/settleAllRegions/getRegionHistory/getOwnerStateMap` idempotent + tie-break `sectId.localeCompare` ASC + skip empty regions. Admin route `POST /admin/territory/settle` + per-region (`@RequireAdmin`). Shared period regex (ISO 8601 week + manual_xx) + helpers. FE TerritoryView owner badge + history panel + admin panel role-gated. Tests +56 (shared 6 + api 49 + admin 12 + web 15). Idempotency invariant + race safety invariant. KHÔNG xóa influence cũ. | feat |
| #480 | docs: sync handoff after PR 479 territory influence foundation merged. AI_HANDOFF_REPORT current main = post #479 + Phase Status table cập nhật + Recent Changes prepend #479. KHÔNG runtime. | docs |
| #479 | `feat(api,web,shared): Phase 14.0.A sect territory influence foundation. Lớp **Lãnh Địa Tông Môn** mới — mỗi region trong `MAP_REGIONS` (9 region) có Sect Influence Leaderboard riêng. Boss/dungeon trong region tự cộng điểm cho Tông character clear/tham chiến. **KHÔNG** settlement capture / siege / region-wide buff / decay (defer 14.0.B+). **Shared (`packages/shared/src/territory.ts`)** module mới: `TerritoryInfluenceSourceDef` 3 entry (`dungeon_clear` 8pts daily 60 weekly 420; `boss_participation` 12pts weekly 96; `boss_top_damage` 20pts weekly 80; soft envelope ≈ 596 pts/character/region/week); `TerritoryRegionDef` 1-1 parity với `MAP_REGIONS` `influenceCap=+Infinity` Phase 14.0.A; helpers `territorySourceByKey`/`territoryRegionByKey`/`isTerritoryInfluenceSourceKey`/`validateTerritoryCatalog`/`territoryMaxPersonalPointsPerWeek` pure deterministic; 6 DTO interface mirror server response; 16 unit test PASS. **Prisma**: `SectTerritoryInfluence` model mới `(regionKey, sectId, characterId, sourceKey, sourceType, sourceId, points, createdAt)` với composite UNIQUE `(regionKey, characterId, sourceKey, sourceType, sourceId)` cho idempotency hook + index `(regionKey, sectId)` leaderboard groupBy + `(characterId, regionKey)` personal view; migration `20260607000000_phase_14_0_a_sect_territory_influence` thuần thêm 1 table mới KHÔNG đụng schema cũ. **API mới (`apps/api/src/modules/territory`)**: `TerritoryService.addInfluenceTx(tx, params)` entry point duy nhất tx-aware idempotent qua composite UNIQUE; cap enforcement compute trước insert reject = no-op; character không có sect → no-op skip; KHÔNG throw — gameplay path không phá nếu territory ghi điểm fail. `getRegions()` list 9 region + total influence + top sect snapshot sort theo `sortOrder`. `getRegionLeaderboard(regionKey)` top 10 sect `points` desc tie-break `sectId` asc throw `REGION_INVALID`. `getMyTerritory(userId)` personal view per-region rank/points sect user + personal contribution; no-sect → `hasSect=false` regions list đầy đủ; throw `NO_CHARACTER`. Endpoint `GET /territory/regions` + `GET /territory/regions/:regionKey/leaderboard` (404 REGION_INVALID) + `GET /territory/me` (404 NO_CHARACTER); 16 test mới (idempotency / cap enforcement / leaderboard correctness / personal view / no-sect / cross-region isolation). **Integration hooks (fail-soft pattern mirror sect-war)**: `DungeonRunService.claimRun()` sau sect-war hook gọi `territory.addInfluenceTx` với `regionKey` từ `dungeonByKey(template).regionKey`, sourceId=run.id, try/catch swallow; legacy/non-region dungeon skip. `BossService.distributeRewards()` mọi participant rank ≥ 1 gọi `boss_participation`; rank 1 thêm `boss_top_damage` bonus; regionKey từ `boss.regionKey` (legacy `'world'` skip vì không thuộc `MAP_REGIONS`); sourceId=`${bossId}:${characterId}`; try/catch swallow + warn log. KHÔNG hook sect-mission (mission không có region — defer 14.0.B+). **FE**: `apps/web/src/api/territory.ts` 6 interface DTO + 3 fetcher read-only (`getTerritoryRegions`/`getTerritoryRegionLeaderboard`/`getTerritoryMe`); `apps/web/src/stores/territory.ts` Pinia store `useTerritoryStore` 3 fetcher race-protected (IN_FLIGHT guard) + leaderboard cache theo `regionKey`; `TerritoryView.vue` 3 tab (overview region list + total influence + top sect; leaderboard region picker + top 10 table highlight my-sect; me per-region rank table fallback no-sect) + auth gate redirect `/auth` + deep-link query `?tab=…&region=…` + data-testid đầy đủ; route `/territory` đăng ký; i18n vi/en parity full `territory.title/subtitle/loading/tab/overview/leaderboard/me/source/errors`; 7 FE test mới (auth gate / overview render / leaderboard fetch / region pick switch / me rank table / no-sect fallback / load error fallback). Tests: shared `--run territory` 16 PASS / api `--run territory` 16 PASS / api `--run dungeon-run` 50 PASS / api `--run boss` 117 PASS / api `--run sect` 146 PASS / web `--run Territory` 7 PASS ✅. Out of scope: decay/season reset (14.0.B+); settlement capture/siege/region-wide buff (14.x); region-buff khi rank 1 (14.0.C+); sect mission hook (defer redesign); admin tooling. KHÔNG đụng dungeon/boss/sect-war reward path (fail-soft hook only). Migration thuần thêm table mới — rollback = revert PR + drop table.` | feat |
| #478 | `feat(api,web,shared): Phase 14.3.A breakthrough tribulation foundation. Lớp foundation mỏng trên Phase 11.6.A/B (catalog + runtime đã có) — KHÔNG rewrite tribulation system. **Shared (`packages/shared/src/tribulation-foundation.ts`)** module mới: `tribulationRequiredForBreakthrough(from, to)` lookup catalog `TRIBULATIONS`; `composeTribulationSupports(entries)` cap per-entry `0.10` + total `0.30`; `computeTribulationSuccessChance({def, primaryElement, supportTotalBonus})` base by severity (`minor=0.75/major=0.55/heavenly=0.35/saint=0.20`) + affinity ±0.05 dùng `elementalAdvantage` từ Phase 14.2.A + supports + clamp envelope `[0.05, 0.95]`; `summarizeTribulationRewardHint`/`summarizeTribulationPenaltyHint` shape compactor cho FE preview; 25 unit test PASS. **API**: `TribulationService.previewTribulation(characterId)` read-only deterministic snapshot KHÔNG roll RNG/log — fetch character → tính nextRealmKey → nếu transition không có catalog entry trả `null` (low-tier hoặc realm cuối) → có entry trả `TribulationPreview` shape; `GET /character/tribulation/preview` (auth) wrap helper; `CharacterService.breakthrough()` add `TRIBULATION_REQUIRED` gate trước cấp realm mới khi catalog có entry — FE catch redirect tới `/tribulation`; low-tier (luyenkhi→truc_co, truc_co→kim_dan) tiếp tục path cũ; 18 test mới (13 preview + 5 gate), tổng 114 character/tribulation test PASS. **FE**: `apps/web/src/api/tribulation.ts` thêm 7 interface mirror server preview shape (`TribulationPreviewView` + 5 sub-shape) + `fetchTribulationPreview()` BigInt-safe (expBonus là string); `apps/web/src/stores/tribulation.ts` thêm `preview: undefined|null|TribulationPreviewView` 3-state + `previewLoading` + `previewError` + `fetchPreview()` race-protected; `TribulationView.vue` `onMounted` gọi `fetchPreview()` song song `fetchHistory()` + thêm preview panel hiển thị `final %` round + affinity badge ±5% (chỉ render khi affinity ≠ 0) + supports list (foundation phase rỗng → empty hint) với `data-testid` đầy đủ; i18n vi/en parity `tribulation.field.previewTitle/successChance/affinity/supports/supportsEmpty`. Tests: shared 25 PASS / api `--run tribulation` 70 PASS / api `--run character.service` 44 PASS / web `--run tribulation` 143 PASS (+16 mới: 7 store fetchPreview + 9 view preview panel) ✅. Out of scope: per-source support provider (item/buff/talent/equipment registry — defer 14.3.B/C); auto-refresh preview khi state thay đổi; advanced tribulation UI (countdown / element pictogram lớn). KHÔNG migration / KHÔNG đụng RNG/log path / KHÔNG break breakthrough cũ.` | feat |
| #477 | `feat(api,web,shared): Phase 14.2.A elemental combat foundation. Wire Ngũ Hành đi vào combat damage flow qua một foundation layer mỏng — KHÔNG phá balance hiện tại. Shared `packages/shared/src/elemental.ts` mới: `ElementType` alias EN + converter + `elementalAdvantage`/`elementalMultiplier` + `composeMonsterElementalResist` (floor 0.7) + `composeEquipmentElementalAtkBonus` (per-item 0.10, total 0.20) + `applyElementalCombatAdjustment` 3-layer pipeline clamp `[0.5, 1.6]`. Balance dial mới: `ELEMENT_MONSTER_RESIST_FLOOR/EQUIPMENT_ATK_BONUS_CEIL/TOTAL_CEIL/COMBAT_ADJUSTMENT_FLOOR/CEIL`. Shared types extend `MonsterDef.elementalResist?` + `ItemBonus.elementalAtkBonus?`. API combat wire pipeline vào `action()` skill path + `actionViaActiveTalent()` talent path; combat log thêm event kháng/buff hệ. API inventory thêm `equipElementalAtkBonus()`. FE `ElementBadge.vue` atom mới + integrate `SkillBookView` + `DungeonView`. Tests shared 51 elemental + web 26 ElementBadge.` | feat |
| #473 | `chore(api,docs): add production readiness CSP and healthcheck verification. Phase R1 chuẩn bị repo cho staging/production deploy an toàn hơn: extract pure config helper sang `apps/api/src/bootstrap-config.ts` (`assertProductionSecrets`/`corsConfig`/`helmetConfig`) — testable không cần boot Nest, env truyền qua param. `main.ts` simplify import 3 helper, behavior unchanged. Tests +26 `bootstrap-config.test.ts` (assertProductionSecrets dev no-op + production missing JWT_ACCESS/REFRESH + insecure default `change-me-*`/`dev-*` cho ACCESS/REFRESH; corsConfig prod missing→throw + dev fallback `localhost:5173` + csv parse + single origin; helmetConfig dev=false + prod 11 directive đầy đủ default-src/script-src/style-src/img-src/font-src/connect-src/object-src/base-uri/form-action/frame-ancestors/upgrade-insecure-requests + KHÔNG unsafe-inline/eval/wildcard + HSTS 15552000 includeSubDomains preload=false + referrer-policy no-referrer + CORP same-site + COEP false). Tests +6 `security-secret-leak.test.ts` chống commit `.env` thật: `apps/api/.env.example` chỉ chứa placeholder length<32 hoặc trong `ALLOWED_PLACEHOLDERS`; `apps/web/.env.example` không có key match `SECRET\|PASSWORD\|TOKEN\|PRIVATE_KEY` không-prefix `VITE_`; `.env` actual paths được `git check-ignore` reject; root `.gitignore` có rule `.env` + whitelist `!.env.example`. Docs mới `docs/PRODUCTION_CHECKLIST.md` (5 section: env list bắt buộc/khuyến nghị/anti-pattern; deploy smoke checklist 6 mục health probe + CSP header curl + auth+session + core gameplay + admin+audit + cleanup; CSP troubleshooting 6 case inline script/WebSocket cross-domain/CDN style+font/image data: URI/iframe embed/verify local; lock-in test inventory; rollback). DEPLOY.md + SECURITY.md companion banner cross-link. KHÔNG migration / KHÔNG schema / KHÔNG đụng gameplay/SectSeason/SectWar / KHÔNG đổi secret thật / KHÔNG commit .env thật / KHÔNG deploy prod thật.` | chore |
| #472 | feat(api,web,shared): add Phase 13.2.A sect season foundation | feat |

**Phase summary tables (PR #1 → #396) + smoke detail per-module + PR #414/#415 + PR #430/#437 entries**: tách sang [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Phase Summary Migrated 2026-05-05 + § Smoke Detail Migrated 2026-05-05.

---

## 3. Current Phase Status

| Phase | Title | Status | Note |
|---|---|---|---|
| 0–8 | Foundation: schema + auth + core gameplay | **Done** ✅ | Full feature catalog ở [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Completed Features. |
| 9 | Beta readiness (Phase 9.A→9.E sub-phases polish) | **11/15 Done, 3 Partial** | Detail [`BETA_CHECKLIST.md`](./BETA_CHECKLIST.md). Mail UI gap closed by PR #391. |
| 10 | Content scale | **5/5 CLOSED** ✅ | All sub-tracks merged. |
| 11 | Progression Depth | **catalog 11/11 + runtime 10/10 + UI merged** | Phase 11 nâng cao (§2/§3/§5/§6 + 11.6.C/D/E + 11.1.E) tất cả CLOSED. |
| 11.X | UI E2E smoke Playwright | **DONE** | Spec #19 talent learn→cast→cooldown + #20 breakthrough flow merged via #394/#420. |
| 12 | World Map & Dungeon | **CLOSED** ✅ | 12.1 catalog CLOSED (#397). 12.2.A `dailyLimit` enforcement CLOSED (#421). Story PR-1→PR-6 + Foundation Extension + Late-game wire CLOSED (#425–#433). **12.2.B/C + 12.3** CLOSED ✅ (#434–#436). **E2E spec #22 dungeon-run flow** CLOSED ✅ (#437). **12.4 per-monster `MonsterDef.lootTable` polish** CLOSED ✅ (#438). **Story Foundation Late-game encounter wire** CLOSED ✅ (#439). **Story discoverability — QuestView dungeon hint cho kill+monster step** CLOSED ✅ (#440). **docs handoff sync** CLOSED ✅ (#441). **12.5 dungeon balance tuning — 8 late-game story monster stat + lootTable** CLOSED ✅ (#442). **12.6 Boss-by-region auto-spawn** CLOSED ✅ (#448 — closes Phase 12 exit criterion "Auto-spawn boss working with idempotency test"). Next (optional): dialogue branch / DungeonTemplate story-instance. |
| Concurrency phase 2 | Race condition fix sweep | **CLOSED** ✅ | All 6 races fixed: Inventory `use()` (#422), Inventory `revoke()` (#443), Realtime ban-during-connection (#444), Cultivation tick CAS (#445), Chat Redis failover (#446), Boss spawn cron auto race (#447). Concurrency phase 2 **DONE**. |
| 13.0 | LiveOps & Retention Suite | **CLOSED** ✅ | Daily-login + Quest cadence + Title catalog + Buff HUD live trên main (post #456 audit pass #5). |
| 13.1.A | Sect War Core / Contribution / Leaderboard / Weekly Reward | **CLOSED** ✅ | PR #457 merged + PR #458 hardening audit (daily cap timezone fix + doc UNIQUE order fix). |
| 13.1.B | Sect Missions + Sect Shop + Admin LiveOps Controls | **CLOSED** ✅ | PR #459 merged. Server-audited mission ledger + contribution-currency shop với caps/CAS/RATE_LIMITED + admin event toggle + sect-war recalc. Migration `20260601000000_phase_13_1_b_sect_missions_shop_liveops`. |
| 13.2.A/B/C | Sect Season Foundation / Milestones+Rewards / History+Hall of Fame | **CLOSED** ✅ | PR #472 + #475 + #474 merged. `SectSeasonClaim` UNIQUE CAS guard + 5-tier milestone reward grant atomic. |
| R1 | Production Readiness CSP/env/healthcheck | **CLOSED** ✅ | PR #473 merged. Helmet 11 directive prod CSP + bootstrap-config helper + secret-leak guard. M7 deploy verify still open §4. |
| 12.10.A | NPC Affinity Foundation | **CLOSED** ✅ | PR #476 merged. `CharacterNpcAffinity` per-(char, npcKey) + `addAffinityTx` cap clamp + tier unlocks; FE NpcAffinityPanel read-only. |
| 12.10.B | NPC Gift + Quest Affinity Rewards | **CLOSED** ✅ | PR #484 merged. `npc-gift.ts` catalog + `POST /story/npc-affinity/:npcKey/gift` atomic + Quest reward `affinity?{npcKey, delta}` idempotent + FE gift panel. Migration `20260609000000_phase_12_10_b_character_npc_gift_log`. Daily bucket UTC 00:00. |
| 14.0.A | Sect Territory Influence Foundation | **CLOSED** ✅ | PR #479 merged. Region leaderboard + dungeon/boss hooks fail-soft + idempotent UNIQUE + 9 region. Migration `20260607000000_phase_14_0_a_sect_territory_influence`. |
| 14.0.B | Sect Territory Settlement + Region Ownership | **CLOSED** ✅ | PR #481 merged. `SectTerritoryRegionState` + `SectTerritorySettlementSnapshot` UNIQUE `(regionKey, periodKey)` race-safe + `settleRegion`/`getOwnerStateMap` + admin route. Region buff khi rank 1 chưa wire (defer 14.0.C); decay/season reset chưa có. Migration `20260608000000_phase_14_0_b_territory_settlement`. |
| 14.2.A | Elemental Combat Foundation | **CLOSED** ✅ | PR #477 merged. `applyElementalCombatAdjustment` pipeline clamp `[0.5, 1.6]` wire skill + active talent path. |
| 14.2.B | Elemental Combat Data + Balance | **CLOSED** ✅ | PR #482 merged. Catalog `elementalResist` 6 boss + 8 monster + `bonuses.elementalAtkBonus` 6 equipment + invariant tests floor/ceil/anti-power-creep + InventoryView tooltip. KHÔNG migration. |
| 14.3.A | Breakthrough Tribulation Foundation | **CLOSED** ✅ | PR #478 merged. `TRIBULATION_REQUIRED` gate + `previewTribulation` + supports signature rỗng. |
| 14.3.B | Tribulation Support Providers + Redirect UX | **CLOSED** ✅ | PR #483 merged. 4 provider read-only (item/buff/equipment/talent) + `successChance.supportBonus` cap 0.30 + FE redirect `/tribulation` + preview badge source/element/cap warning. KHÔNG consume / KHÔNG migration — preview read-only, item consumption defer Phase 14.3.C. |
| 13+ | Real-time PvP / pet gacha / voice / streaming | **Not started** | Per [`LONG_TERM_ROADMAP.md`](./LONG_TERM_ROADMAP.md) §0 — explicitly DO NOT build yet. |

**Smoke coverage**: 28 module (~15 step avg, +`smoke:quest` 20 step PR-3, +`smoke:npc` 11 step PR-4, +`smoke:dungeon-run` 16 step PR #434). 8 module có cả negative + positive path: skill / shop / mail / inventory / spiritual-root / breakthrough / auth / cultivation-method. Defer: daily-login multi-day positive (cần admin advance-day). Full per-module step count + endpoint coverage → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Smoke Detail Migrated 2026-05-05.

---

## 4. Known Issues / Risks

### Live (Open) — cần action

| # | Severity | Issue | Status / Plan |
|---|---|---|---|
| M7 | Medium | CSP production-ready nhưng chưa test deploy với CDN/asset domain khác. | **Open** — khi deploy production cần review `script-src`, `connect-src`. |

### Implementation notes / debt (open) — không phải bug, là defer

- **NPC Gift daily bucket** (Phase 12.10.B / PR #484): hiện reset theo UTC 00:00 (`YYYY-MM-DD` slice từ ISO timestamp). Cân nhắc chuyển sang local-day (Asia/Ho_Chi_Minh ICT) parity với mission/daily-login Phase 12.10.C nếu player phản hồi reset 07:00 ICT bất tiện.
- **Tribulation support preview read-only** (Phase 14.3.B / PR #483): `previewTribulation()` populate `supports[]` từ item/buff/equipment/talent NHƯNG KHÔNG consume / KHÔNG decrement / KHÔNG mutate state. Khi player bấm tribulation attempt thật, item gắn `tribulationSupport` chưa bị tiêu hao — defer Phase 14.3.C (consume + apply buff effect khi attempt).
- **Territory region buff + decay** (Phase 14.0.B / PR #481): `getOwnerStateMap()` đã expose owner per-region; settlement snapshot ghi periodKey OK. NHƯNG region-wide buff khi sect rank 1 (e.g., +5% drop / +10% influence cho member) chưa wire vào combat layer; influence decay/season reset chưa có cron — defer Phase 14.0.C / 13.2.D.
- **Quest claim affinity reward idempotency** (Phase 12.10.B): apply qua existing `QuestProgress.claim()` CAS guard `updateMany({status: COMPLETED → CLAIMED, claimedAt})` — tested OK. Nếu sau này thêm un-claim hoặc retry path mới, cần verify lại affinity KHÔNG double-grant.

### Resolved (recent — full list ở ARCHIVE)

- M10 Shop daily limit + per-user rate limit → Resolved PR #450 (`ShopEntryDef.dailyLimit` opt-in + `rl:shop-buy` 30 req/60s qua FailoverRateLimiter; pre-check trước tx, KHÔNG trừ tiền khi reject; +20 vitest).
- Inventory `use()` JS-capture race → Resolved PR #422 (atomic `updateMany` + post-decrement delete + INVENTORY_ITEM_NOT_FOUND translation + 5 regression test).
- Phase 12.2.A daily limit enforcement → Resolved PR #421.
- M9 Settings logout-all `passwordVersion` → Resolved PR #154/#155 (intentional trade-off documented `docs/SECURITY.md §1`).
- M11 `GET /character/profile/:id` rate-limit → Resolved PR #62 (`PROFILE_RATE_LIMITER` 120 req/IP/15min).
- M8 Admin guard MOD broad quyền → Resolved PR E (`@RequireAdmin()` decorator + reflector AdminGuard).
- M6 LogsModule chưa build → Resolved PR #88 BE + #91 FE (`/logs/me` keyset pagination + ActivityView.vue + 24 ledger reason i18n).

> **Full historical issues** (~50 entries Critical/High/Medium/Low + Resolved) ở [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Section 16 Known Issues / Risks.

---

## 5. Tests / CI / Smoke / E2E

### Vitest baseline (post this PR)

| Workspace | Test count | Notes |
|---|---|---|
| `apps/api` | **2129** | +6 từ this PR (`daily-login.extended.test.ts`: 30-day streak + race-condition + controller DB smoke) lên trên 2123 post-PR-#460 baseline. Pre-existing `chat.service.test.ts` rate-limit window test occasionally flakes khi chạy parallel; pass cô lập (`pnpm --filter @xuantoi/api test -- --run chat.service`). 6 redis-dependent tests (rate-limiter + health) require Redis up. |
| `packages/shared` | **1463** | Không thay đổi vs PR #457/#459/#460/this PR. |
| `apps/web` | **1265** | Không thay đổi vs Phase 13.1.B (SectMissionPanel 5 + SectShopPanel 7 + AdminLiveOpsPanel 4 + 8 SectWarView migration). PR #460/#461/this PR không đổi web. |
| **Total** | **4857 vitest** | All green local (PG+Redis up). |

### CI

- Workflow `.github/workflows/ci.yml`: `build` + `e2e-smoke` jobs (PG+Redis services). Chạy mỗi PR + push (no path filter — docs-only PR vẫn trigger CI). Yêu cầu xanh trước khi merge.
- 5 redis-dependent test (rate-limiter + health controller) cần Redis local — pass khi Redis container up.

### Smoke Scripts (manual, không nằm CI matrix)

29 smoke scripts, ~15 step avg. Yêu cầu local stack: `pnpm infra:up` + `pnpm --filter @xuantoi/api exec prisma migrate deploy` + `pnpm --filter @xuantoi/api run bootstrap` + `pnpm --filter @xuantoi/api dev`. Module list: achievement, admin, auth, beta, boss, breakthrough, chat, combat, cultivation, cultivation-method, daily-login, **dungeon-run** (Phase 12.2.B — 16/16 step, positive flow tới claim + double-claim reject), economy, giftcode, inventory, leaderboard, mail, market, mission, next-action, npc, **quest** (Phase 12 PR-2 — 16/16 step, negative-path heavy), sect, shop, skill, spiritual-root, **story-dungeon** (Phase 12.8.E — 35/35 step, full player flow: auth gates + register/onboard + drive prereq quest COMPLETED + accept story quest + start ACTIVE idempotent + advance×3 + clear + claim reward + double-claim/re-clear/re-start reject + non-owner reject + data isolation), topup, ws.

**Positive-path coverage** (8 module): skill (PR #388/#390), shop (#390), mail (#391), inventory (#385), spiritual-root (#386), breakthrough (#417), auth (#384), cultivation-method (#394). Còn defer: daily-login multi-day positive — pending admin advance-day endpoint hoặc service helper extension.

**Per-module detail table** (step count, negative/positive flag, notes) → [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md) § Smoke Detail Migrated 2026-05-05.

### E2E (Playwright)

- `apps/web/e2e/golden.spec.ts` — 22 spec golden path (auth smoke + 21 full-stack gated `E2E_FULL=1`). Notable: #19 talent learn→cast→cooldown badge (`golden.spec.ts:759-895`), #20 breakthrough attempt → outcome banner + history reload-persist (`golden.spec.ts:898-981`), #21 Phase 12 Story PR-5 main storyline Chapter 1 playable (`golden.spec.ts:1031-1125`), **#22 Phase 12.3 DungeonRun flow** (`golden.spec.ts:1162-end` — son_coc start→next×3→claim, verify per-encounter loot span + reward delta).
- CI job `e2e-smoke`: chạy spec #1 AuthView smoke mỗi PR (matrix postgres+redis, build api+web, `E2E_SMOKE=1`).
- `E2E_FULL=1` gate cho 19 full-stack spec **chưa wire CI** — runtime manual với `pnpm infra:up` + `pnpm --filter @xuantoi/api dev` + `pnpm --filter @xuantoi/web dev`.

---

## 6. Recommended Next Roadmap

Per [`AI_WORKFLOW_RULES.md`](./AI_WORKFLOW_RULES.md) §SESSION PR LIMIT (1-3 PR/session) + §GOM TRƯỚC KHI TÁCH 4b. Ưu tiên Medium PR > Hotfix > Large.

### Top priority — next session

1. **Phase 14.0.C Region Buff + Territory Decay** (Medium PR) — Phase 14.0.B đã ship `SectTerritoryRegionState` owner per-region + settlement snapshot. Tiếp theo cần: wire `getOwnerStateMap()` vào combat layer (rank-1 sect → +5% drop / +10% influence cho member) qua hook fail-soft; influence decay weekly halve hoặc monthly reset (catalog `TerritoryDecayConfig`); Hall of Fame archive snapshot cũ. Cap influence finalize từ `+Infinity` → e.g., `100000`.

2. **Phase 14.3.C Tribulation Attempt Item Consumption** (Medium PR) — Phase 14.3.B đã ship `previewTribulation()` populate `supports[]` từ 4 provider NHƯNG read-only. Tiếp theo cần: khi attempt thật, decrement item qty=1 cho mỗi entry source `item` + apply buff `BuffService.applyBuffTx` cho entry source `buff` (idempotent qua existing CharacterBuff UNIQUE); audit ledger `TRIBULATION_ITEM_CONSUME`; rollback nếu attempt fail (refund hoặc giữ — design call).

3. **Phase 12.10.C NPC Affinity Shop + Hidden Unlocks** (Medium PR) — Phase 12.10.A/B đã ship affinity foundation + gift + quest reward. Tiếp theo cần: NPC affinity tier unlock hidden dialogue branch (`condition: affinity_min`); NPC affinity shop (currency = affinity, contrib hoặc linhThach + affinity gate); cân nhắc local-day reset cho NPC gift daily bucket parity với mission timezone.

4. **Phase 13.2.D Season Settlement Automation** (Medium PR) — Phase 14.0.B đã ship admin manual `POST /admin/territory/settle?periodKey=…`. Tiếp theo cần: cron tuần auto-settle territory ISO week + season reset hook; Hall of Fame snapshot vào table riêng (extend `SectSeasonHistory` hoặc tách); Phase 13.2 sect season + Phase 14.0 territory settlement cùng schedule.

### Phase 12 Story chain status

Design source ở [`docs/story/TU_TIEN_LO_STORY_BIBLE.md`](./story/TU_TIEN_LO_STORY_BIBLE.md) + [`docs/story/PHASE12_STORY_PROGRESS.md`](./story/PHASE12_STORY_PROGRESS.md). 5-PR core roadmap + Foundation Late-game wire + encounter wire + discoverability hint **all CLOSED** ✅:

- **PR-1** → **PR-6** — Catalog foundation, Quest runtime, Claim, NPC dialogue UI, Chapter 1 playable, Combat kill hook. **CLOSED** ✅ (#425–#432).
- **Foundation Late-game wire** — 8 monster catalog (`tich_linh_anh`, `tam_ma_anh`, `tich_linh_quy`, `tich_thien_sat_thu`, `tam_ma_nguyen_anh`, `chap_niem_anh`, `ky_uc_meo`, `huyet_anh`). **CLOSED** ✅ (#433).
- **Foundation Late-game encounter wire** — wire 8 placeholder vào 4 dungeon `monsters[]`. **CLOSED** ✅ (#439).
- **QuestView dungeon hint** — close discoverability gap. **CLOSED** ✅ (#440).
- **Phase 12.5 dungeon balance tuning** — stat HP/ATK/DEF/SPD/level + monsterType promotion (3 ELITE/BOSS) + 3 lootTable override + 11 invariant test. **CLOSED** ✅ (#442).

Next nâng cao Phase 12 Story (optional, nếu cần): Phase 12.10 Story dialogue smoke + Playwright E2E để khoá branching coverage cuối; HOẶC mở rộng Hàn Dạ tree thành multi-arc reward (give_reward gắn theo path); HOẶC thêm dialogue branch cho 8 placeholder kill milestone (Phase 12 Foundation Late-game). Sau mỗi PR Phase 12 Story merged, AI **bắt buộc** cập nhật `docs/story/PHASE12_STORY_PROGRESS.md` trong cùng PR (DOCS UPDATE RULE).

### Backlog (low priority)

- **Doc compaction maintenance**: khi Recent Changes vượt 10 entry → đẩy entry cũ nhất xuống ARCHIVE § Recent Changes Legacy. Khi `AI_HANDOFF_REPORT.md` vượt **250 dòng** → compact ngay theo HANDOFF REPORT STRUCTURE RULE (cap mới 2026-05-05).
- **Concurrency tests phase 2 (low priority remainder)**: Boss spawn cron auto race.

### Anti-feature-creep (DO NOT BUILD YET)

Per [`LONG_TERM_ROADMAP.md`](./LONG_TERM_ROADMAP.md) §0:
- Real-time PvP (Phase 14)
- Party / co-op dungeon (Phase 12 — wait Phase 11 ≥ 95%)
- Pet / wife gacha (Phase 16)
- Voice chat
- Video streaming

---

## 7. Archive (đã tách file riêng)

Toàn bộ Archive (Snapshots PR #33→#396, Recent Changes Legacy, Completed Features, Project Reference đầy đủ Tech Stack / Architecture / DB / Gameplay / Run Locally / Rules, Old Recommended Next Roadmap, Exact PR Plan, Phase Summary Migrated 2026-05-05, Smoke Detail Migrated 2026-05-05) đã tách ra file riêng để giảm token cost mỗi session.

Chi tiết: [`ARCHIVE_HANDOFF.md`](./ARCHIVE_HANDOFF.md). **AI mới KHÔNG cần đọc file đó mỗi session — chỉ tra cứu khi cần điều tra PR/history cụ thể.**
